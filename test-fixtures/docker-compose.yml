services:
  mailpit:
    image: axllent/mailpit:v1.30.0
    ports:
      - '${ARXIC_MAILPIT_SMTP_PORT:-1025}:1025'
      - '${ARXIC_MAILPIT_API_PORT:-8025}:8025'
    environment:
      MP_SMTP_AUTH_ACCEPT_ANY: '1'
      MP_SMTP_AUTH_ALLOW_INSECURE: '1'
