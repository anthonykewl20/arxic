id: nextjs-token-verify
language: TypeScript
message: Constant-time token verification
severity: info
rule:
  pattern: timingSafeEqual($SUPPLIED, $EXPECTED)
metadata:
  arxic:
    category: token-verify
    semver: 1.0.0
    frameworkVersions: '>=15 <17'
    precision: Direct constant-time comparison with both operands captured
    fallback: Library verification wrappers need another structural rule
    license: MIT
    provenance: original-arxic
