# Arxic — SYNC (living progress bookmark)

> **Read this first when resuming a session. Update it before closing one.**
> This is the peg. It tells you where we are, where we stopped, and where we pick up.
> The public architecture record is [`001-arxic-architecture.md`](./adr/001-arxic-architecture.md) (summary; the detailed internal ADR is private). The issues board is at <https://github.com/anthonykewl20/arxic/issues>.

---

## 🔖 RESUME HERE

**Status:** **Milestone 1 IN PROGRESS (14/15) — only the #27 M1-EXIT gate remains.** #26 (M1-12) adds the ephemeral isolated worker: `@arxic/environment` owns the isolation mechanics (internal Docker network, non-root, read-only rootfs, read-only source bind, tmpfs `/work`, memory/swap/pids/cpu quotas, `no-new-privileges`, `cap-drop ALL`, no socket or daemon access) and `@arxic/worker` owns the fail-closed policy layer plus the `WorkerClient` seam implementation, with content ingested strictly as data. #43 (M1-15) adds the stage-8 exploration loop (plan → policy check → human approval → explore → journal → budget → normalize) in `@arxic/orchestrator-langgraph/exploration.ts`, with `@arxic/policy-engine` authorizing origin + action class before anything executes and a new `PlaywrightExplorationDriver` driving read-only steps in real Chromium. Exploration yields `observed` at best and may never originate `verified`. #25 (M1-11) adds `@arxic/cli`: `arxic run --config arxic.yaml` validates the ADR §19 config, submits through a CLI-owned `RunExecutor` port, streams frozen `Diagnostic`s (never raw model prose), and writes the ADR §20.1 run directory. #42 (M1-14) adds stage-4 LLM candidate inference to `@arxic/orchestrator-langgraph` via the new `inference.ts` service layer. Hypothesized workflow candidates are produced from the smallest sufficient source-evidence neighbourhood through the `ModelAdapter` structured-output boundary: neighbourhood selection is deterministic, deduped and bounded (source-only); inference messages are content-as-data (a metadata projection — no source bytes or operator prompt); candidates are emitted `hypothesized` only, enforced by construction because the stage-4 schema has no status field. `#infer` owns the retry loop and truth-state classification. Frozen §10 contracts and LangGraph topology unchanged.

**Parallel batch (charter §10) — COMPLETE.** Four slices plus an M1-EXIT de-risk spike were built concurrently, one agent per worktree, with the merge queue serialized by the integrator. All merged: #26 CLI↔worker seam types (#84), #42 M1-14 (#92), the #27 de-risk spike (#91), #25 M1-11 (#94), #43 M1-15 (#85), #26 M1-12 (#93). Every worker reported "done" at least once before it was — four of five needed a fix round — so **verify slice claims against CI and the disk artifact, never against the report**. Two systemic causes, both now closed: charter §10 landed after the workers had already started (so `docs/_slice-notes/` and `_TEMPLATE.md` did not exist and three agents invented `docs/_slice_notes/`), and no check enforces the §10.2 path — the format gate caught the wrong directory twice by luck, and #91 slipped through green with it. That CI guard was subsequently added (see Wave 0 below) and the second batch had zero slice-note defects.

**⚠️ GitHub auto-close trap — closed #27 three times.** GitHub's closing-keyword parser is **not negation-aware**, and it runs over **commit messages on the default branch** as well as PR titles and bodies. `does not close #<n>` contains the keyword and closes the issue. It caught us three times: from a PR body; from a PR body that literally said "do not close #27"; and finally from the commit message of the commit that documented the trap, which quoted the forbidden phrase four times while explaining it. **Never put a closing verb adjacent to an issue number anywhere — PR title, PR body, or commit message — even negated, even when quoting.** Use `refs #<n>`, or "#<n> stays open". To discuss the keyword itself, break the string up or omit the number.

**🎉 M1-EXIT ACHIEVED — #27 PASSES (PR #121).** Both fixture apps measure 14/14 MET against ADR §23 (§23.12 failed-run-preserves-prior-bundle now proven end-to-end for Express via #109). The full M1 security/integrity chain landed CI-green: #111 trace sanitization, #115 screenshot privacy, #112 bundle integrity, #108 Service Worker containment, #114 Dependabot remediation, #109 §23.12 proof. **Open work — 2 issues** (both M2): [#103](https://github.com/anthonykewl20/arxic/issues/103) (worker-backed CLI — **groundwork landed** via PR #122: `--executor local\|worker` selection + `WorkerRunExecutor` fail-closed wiring; **full sandbox-hosted pipeline still blocked** on a Node-22 pipeline image + artifact/peer transport + a pipeline-result protocol — #103 stays open), [#116](https://github.com/anthonykewl20/arxic/issues/116) (intent-backed pseudocode — **ADR-004 (Proposed) landed** via PR #123: the IntentSpec design deciding form (b) unfrozen app-local, oracle provenance `domain-rule|repository-specification|human-approved|observed-only`, owners/dependency direction, the 8-step flow; **full implementation + two-app proof are a tracked M2 build, #116 stays open**). **Carried post-M1 follow-ups (non-blocking):** (1) §23.13 — add the CycloneDX SBOM to the promoted bundle (currently a CI-only artifact; bundle ships NOTICE + provenance + checksums); (2) Dependabot alert remeasurement pending GitHub re-scan (vulnerable versions gone from the merged graph); (3) **independent HUMAN visual inspection of retained screenshots** (#115 residual — an LLM cannot prove arbitrary pixels secret-free); (4) the `plan`↔`plan.md` binding gap (B2 can reach atomic-replace with a desynchronized plan; non-blocking, recorded in #109 evidence).

**Live correction — RESOLVED by #108 (PR #113).** An earlier revision of this file (and an earlier code claim) said the crawler denied mutation and cross-origin egress unconditionally at the network layer. Adversarial review (codex) had refuted that: the aborts were page-scoped `page.route('**/*')` and the launch context never set `serviceWorkers: 'block'`, so per the [Playwright network docs](https://playwright.dev/docs/network) a Service Worker could "take over the network requests," making them invisible to `page.route()` and bypassing both `ARXIC-SURFACE-001` and `-008` **without producing a diagnostic**. **#108 fixed this**: Crawlee discovery now sets `serviceWorkers: 'block'` (keeping `useIncognitoPages: false`), so a registered worker never installs and the cross-origin/non-safe requests flow through the normal interception and ARE denied WITH their diagnostics. Real-Chromium proof: an allowed hostile worker reaches both sinks; with the fix, discovery reaches neither and emits both blocked `ARXIC-SURFACE-001`/`-008` diagnostics. This was the §23.10 gap; it is now covered.

_#104 (CLI accepted policy values the worker refuses) was resolved by #107 — see the `2026-08-07 (14)` session-log row for the full record, including a claim I got wrong while assessing it._

**Carried follow-ups (not blocking #27):**

- Image-local account **name aliases** that resolve to uid/gid 0 without being the literal string `root` (e.g. `toor`) are not rejected by `assertSafeSpec`. Detecting them requires resolving names against the image's account database, which would break the reject-before-any-Docker-call property that makes the check worth having. Documented in code and accepted as out of scope for pre-flight string validation; revisit only if adversarial caller-supplied `workerUser` names become part of the threat model.
- ~~`assertSafeSpec` root-group (`uid:0`) gap~~ — **resolved by #101/#106**, which also closed a wider hole nobody had filed.
- ~~worker-sandbox hardcoded uid~~ — **resolved by #95/#96.**
- ~~#93's over-broad read-only assertion~~ — **resolved by #95/#96**, which also reverted the masking `chmod`.
- ~~M1-14 `minLength` + bare `catch`~~ — **resolved by #97/#98.**

**Wave 0 for the second parallel batch (landed).** CI now has a `Verify slice notes` step that rejects `docs/_slice_notes` (underscore) outright and requires every note to carry all six `_TEMPLATE.md` sections — the two failures that cost four fix rounds and that the format gate only caught by luck. Retained proof artifacts moved to [`docs/evidence/<SLICE-ID>/`](./evidence/README.md) so `docs/_slice-notes/` can stay empty between milestones per §10.2; `M1-15-evidence/` now lives at `docs/evidence/M1-15/`. `AGENTS.md` carries a reporting-discipline section derived from the first batch's failures, and auto-loads for every agent.

**Second parallel batch — COMPLETE, 4 of 4 merged.** Split so that #86/#87/#89 (all in `packages/playwright-compiler`) ran **sequentially in one terminal as one PR** rather than three agents in one file; alongside #88 (`auth-domain-pack`), the worker-sandbox uid fix (`environment` + `worker`), and the M1-14 follow-ups (`orchestrator-langgraph`). Four terminals, zero file overlap — and unlike the first batch, **zero slice-note defects**: all four notes landed at the correct hyphenated path with all six sections, caught by the Wave 0 CI guard rather than by hand. **All merged:** #96 (worker uid, → #95), #98 (M1-14 follow-ups, → #97), #99 (#88 domain pack), #100 (#86/#87/#89 compiler generality). #100 needed two attempts — its first CI run failed on a stale base, described below.

**What the batch showed about parallel execution.** The T1/T2 dependency resolved by corroboration rather than conflict: #99 and #100 independently fixed the same broken Express login path from different packages and produced the same answer. #100's only CI failure was its stale base asserting `contradicted` where its own compiler fix yielded `verified` — precisely what #99's rewritten test expected. Two agents, separate lanes, no communication, converging on the same truth is stronger evidence than either PR alone. Worth reaching for deliberately: when a claim matters, have two slices arrive at it from different directions.

**Next action:** [#116 slice B](https://github.com/anthonykewl20/arxic/issues/116) — wire the `@arxic/intent` Service into the orchestrator as a stage-8/9 oracle-resolution **Action** boundary (ADR-004 §4.5): a new orchestrator option consumes slice A's `normalizeIntentSpec`/`resolveAssertion`/`canonicalizeIntentSpec`/`detectStaleness`, persists an opaque hash-verified IntentSpec stage artifact under ADR §20, and classifies the run-level truth state (block/characterize/reconcile). Slice A (the package + Service mechanics) is merged; B composes it. Full #116 slice plan: A (done) → B (orchestrator wiring) → C (ADR §6 locator policy) → D (compiler integration, provenance in run-local artifact) → E (ADR §7 sensitivity probe) → F (two-app real-browser proof matrix + ADR-004 Accepted flip). #103 (M2-WORKER-CLI) remains blocked on a Node-22 pipeline worker image + transport + result protocol.

**Last session:** 2026-08-10 — **#116 slice A — `@arxic/intent` Service mechanics landed (PR pending, CI pending; #116 stays OPEN).** New private package `@arxic/intent` implements the unfrozen app-local IntentSpec/OracleSpec layer ADR-004 §1/§3 authorizes — types + Service capability blocks (`validateOracle`/`resolveAssertionKind`/`resolveAssertion`/`normalizeIntentSpec`/`buildIntentSpec`/`canonicalizeIntentSpec`/`detectStaleness`); no frozen-contract or JSON-Schema change; depends only on `@arxic/contracts`. Resolves the ADR-004 open schema/lifecycle questions for this slice: `kind` is always derived from oracle kinds (never input — source-as-acceptance is structurally impossible), acceptance requires ≥1 independent oracle **and** non-empty source+runtime evidence (ADR §2), divergent acceptance outcomes on the same id are `contradicted`, duplicate ids / stale lineage / malformed oracles / non-64-lowercase-hex digests / hostile extra keys (incl. `truthState:"verified"`) are stripped or `blocked` with stable `ARXIC-INTENT-*` diagnostics loop-closed through frozen `validateDiagnostic`. Real-data proof drives the real `authCandidates` pack over both apps' real captured `authSurface` (acceptance via a pack-rule identity oracle — no laundering; observed-only → characterization; the two apps' real divergent login outcomes → contradicted); live-browser proof deferred to slice F. reviewer-hy3 REQUEST-CHANGES→APPROVE after six empirically-reproduced blocking findings (verified-key leak via non-closed-world spread, canonical Date/Map/Set collision + BigInt throw, zero-evidence acceptance, tautological loop-close, circular oracle digest, lost buildIntentSpec diagnostics) were fixed; a second targeted confirm re-verified each fix on disk. Gates green (typecheck/lint/format/test 98 files / 807 tests, 34 in `@arxic/intent`). Then **#116 ADR-004 (Proposed) intent-backed pseudocode design landed (PR #123, CI `success`; #116 stays OPEN for implementation).** ADR-004 decides `IntentSpec` = unfrozen app-local proposal layer (not a frozen contract, not a Workflow extension — preserves the freeze + the LLM-can't-originate-`verified` invariant); oracle provenance `domain-rule|repository-specification|human-approved` (acceptance) vs `observed-only` (characterization only) so implementation-derived behavior can't launder into an acceptance oracle; owners/dependency direction per charter §1; the 8-step flow mapped to stages; Gherkin/locator/sensitivity boundaries (clean-room). Full implementation + two-app proof are a tracked M2 build. Then **#103 (M2-WORKER-CLI) groundwork landed (PR #122, CI `success`; #103 stays OPEN).** `arxic run` accepts `--executor local|worker` (local default); a CLI-owned `WorkerRunExecutor implements RunExecutor` drives the existing `WorkerClient` with fail-closed classification (`ARXIC-EXEC-WORKER-*`) and writes the same ADR §20.1 failed-run schema, honestly blocking with `ARXIC-EXEC-WORKER-PROTOCOL` (no fabricated pipeline result). Real Docker proves selection + cleanup; #26 isolation proofs intact. **Full stages-0–12 sandbox execution remains blocked** on a Node-22 pipeline worker image (workspace/native/browser/`git`/`sg`), writable artifact + internal-network peer transport, and a structured pipeline-result protocol payload — a larger M2 build. Then **🎉 M1-EXIT ACHIEVED — #27 PASSES (PR #121, CI `success`).** #109 closed the last gap: `promotion-real-world.test.ts` drives BOTH fixture apps (incl. Express) through compile → two-pass real-Chromium verify → coherent projection (#112) → promote B1 → blocked subsequent promotion → independent reread proving prior bytes byte-identical — §23.12 now MET for both apps. Full §23 remeasurement: reference 14/14 · vulnerable 14/14 MET (§23.3/§23.4 Express accepted MET, generic/vacuous per the 2026-08-08 baseline; §23.13 SBOM tracked as a post-M1 packaging follow-up). VERSION → 0.2.0 (M1-EXIT per RELEASES.md). Then **#114 (DEPENDABOT) remediation DONE (PR #120, CI `success` under Node 22.22.0)**: Nodemailer 7→9.0.5, Next 15→16.3.0 (PostCSS 8.5.23 + sharp 0.35.3), Testcontainers 12/Dockerode 5 (uuid absent); Next-16 ESLint-flat + proxy migration; rate-limit made process-global to survive Turbopack module-dedup; real Testcontainers/Mailpit smoke (vars unset) + real sharp image-optimization proof. Dependabot alert remeasurement pending GitHub re-scan (vulnerable versions gone from the merged graph). Then **#108 (M2-SERVICE-WORKERS) Service Worker containment DONE (PR #113, CI `success`)**: Crawlee discovery now sets `serviceWorkers: 'block'` (keeping `useIncognitoPages: false`), closing the gap where a registered SW could hide cross-origin/non-safe requests from `page.route()`. Real-Chromium proof: an allowed hostile worker reaches both same-origin-POST and cross-origin sinks; with the fix, discovery reaches neither and emits both `ARXIC-SURFACE-001`/`-008` diagnostics. Rebased over merged #111/#115/#112; the previously-retained raw trace AND the supplementary screenshot were both removed (raw traces never retained; the screenshot would need #115 attestation and the proof is the diagnostic assertions). Then **#112 (M1-BUNDLE-INTEGRITY) staged workflow/manifest integrity DONE (PR #119, CI `success`)**, rebased+composed over the merged #111/#115: a cross-field validator rejects mismatched workflow↔manifest `id`/`status` (truth-state) before promotion (`ARXIC-PROMOTION-VALIDATION-FAILED`, prior bytes preserved), and a deterministic `verified-projection` projects the verifier outcome coherently into BOTH workflow and manifest — an LLM still cannot originate `verified` (only the deterministic verifier result drives the projection). Promotion gate order preserved: caller gates → integrity (#112) → trace (#111) → screenshot (#115) → freeze → atomic publish; `verified-projection` carries all new artifact kinds. reviewer-hy3 APPROVE (no blocking); full local suite 758/758. Then **#115 (M1-SCREENSHOT-PRIVACY) screenshot pixel privacy DONE (PR #118, CI `success`)**, composed over the just-merged #111: new `@arxic/playwright-screenshot-privacy` Service (action-owned approved-region/masking policy + adjacent `.privacy.json` provenance + strict PNG/attestation validation) is wired into the shared `retainCaptureArtifacts` capture Service so verifier + M0 retain only policy-attested (masked) screenshots; assembly/promotion independently validate PNG bytes + provenance and reject raw/forged/malformed/polyglot/mismatched artifacts. Without a policy the system fails closed (`SCREENSHOT_PRIVACY_REQUIRED`); #111's trace sanitization + checkpoint binding + canonical-byte determinism are preserved (Service extended, not forked). reviewer-hy3 consensus caught a blocking P1 (m0 bundle dropped `screenshot-capture-runtime`), then CI caught the cross-package blast radius (auth-domain-pack/reconciler now require an operator-supplied policy — added `AuthDomainPackOptions.screenshotPrivacyPolicy` production hook) and a CI-slower-FS timeout. Workspace package count bumped 22→23. **Honest residual:** arbitrary page pixels / IDAT steganography CANNOT be mechanically proven secret-free — retained screenshots require independent HUMAN visual sign-off (tracked, not blocking). Then **#111 (M1-TRACE-SANITIZATION) Playwright trace retention boundary DONE (PR #117, CI `success`).** Raw Playwright ZIPs are deleted at capture; retained evidence is a deterministic, privacy-preserving **action-timeline projection** (projection, not filtering — a closed vocabulary, so credentials/cookies/sessions cannot survive by construction) plus adjacent checksum/provenance and independent bounded re-inspection at the promotion boundary. New shared package `@arxic/playwright-trace-sanitizer` (the Service/capability block) is consumed by verifier, M0, exploration, and bundle-promoter; Actions own failure classification (`ARXIC-VERIFY-TRACE-SANITIZATION-FAILED` / promoter redaction / m0 blocked). Verifier/M0/exploration sanitization failure blocks eligibility; assembly/promotion classify bounded bytes rather than trusting kind/extension and preserve prior output on failure. reviewer-hy3 APPROVE + open-code-review-delegate consensus drove two blocking fixes before merge: **B1** (charter §1) ~95 lines of duplicated capture logic across verifier/m0 extracted into one Service fn `retainCaptureArtifacts` (structured return, no `ARXIC-*` leak); **B2** a #111 regression that dropped the screenshot↔checkpoint binding and used an unsound positional `slice()` — restored checkpoint-bearing retained names + fail-closed injected seam. A CI-discovered **determinism bug** was then fixed: `yazl@3.3.1` computed DOS timestamps from the host timezone, so PST-generated evidence failed the canonical rebuild under CI's UTC — patched yazl to UTC (committed `patches/yazl@3.3.1.patch`), regenerated the 3 evidence ZIPs (entry bytes unchanged), and added a red-first cross-timezone fixed-point test. Real Playwright 1.62.1 Chromium proof exercised both auth apps + the pinned Trace Viewer; CI green (full suite, real Mailpit, SBOM, command-guard). **Honest residual:** artifact discovery assumes freshly Action-owned roots + a quiescent workspace; concurrent same-UID intermediate-directory replacement stays UNVERIFIED (worker/process isolation) — the collector is NOT called race-safe. Source digest is capture-reported linkage, not origin-authenticated; timeline inspection is a type-confusion boundary, not screenshot pixel privacy (#115 owns that). Frozen §10 contracts and LangGraph topology unchanged. Next: #115 (rebase over this head).

---

## At a glance

| Thing | Where |
|---|---|
| ADR (public summary) | `docs/adr/001-arxic-architecture.md` (§2 truth states, §8 diagram, §9 pipeline, §10 contracts, §22 milestones) — detailed internal ADR is private/local |
| Engineering charter | `docs/engineering-charter.md` — TDD red-first + Actions/Service layering + **mandatory sad-path-first**. Every slice follows this. |
| Contracts schemas | `schemas/{evidence,workflow,manifest,diagnostics}/` + adapter TS interfaces — **ALL contracts frozen** (#2–#6); `@arxic/contracts` is the capability boundary (ADR §10.5) |
| Packages | `packages/*` (16 adapters/engines, including the M0 capstone pipeline) — see ADR §18 |
| Apps | `apps/{cli,worker}/` (scaffolded) |
| Rule packs | `rulepacks/{nextjs,express}/` versioned auth AST rules **DONE (#9)**; `rulepacks/react` remains empty until #22 |
| Test fixture apps | `test-fixtures/{reference-auth-app (Next.js 15 + sqlite + Mailpit + otplib),vulnerable-auth-app (Express + sqlite)}/` — **DONE (#13)**: real booting apps, real Mailpit, attestation, seed API; the §6 real-world surface for spikes #8–#12 + M1 |
| Issues | <https://github.com/anthonykewl20/arxic/issues> — milestones "Milestone 0" / "Milestone 1" |
| Tooling | pnpm workspaces (Node ≥22 via corepack `packageManager` pnpm 11), TS strict, ESLint flat-config, Prettier; **per-workspace `tsconfig.json` + `typecheck` across all 19 package/app workspaces**; gates `pnpm lint/typecheck/typecheck:packages/format:check/test` are CI-green; **source-only — no build/emit in M0** (ADR-003) |
| Versioning | `VERSION` (0.0.0) is single source of truth → `package.json`; `RELEASES.md` (SemVer; 0.1.0=M0-EXIT #14, 0.2.0=M1-EXIT #27); `CHANGELOG.md` updated EVERY slice |
| Repo & CI | GitHub: squash-only merges + delete-branch-on-merge, Dependabot + security alerts ON, `main` protected (PR flow). CI `ci.yml` (required check `ci`): lint/typecheck/format/test + metadata guards + **license gate** (`scripts/license-gate.mjs`, rejects GPL/AGPL/SSPL) + **CycloneDX SBOM** artifact + fixture-app tests (real Mailpit); issue/PR templates; `CODEOWNERS`=@anthonykewl20 |
| Maturity docs | `CONTRIBUTING.md`, `CODE_OF_CONDUCT.md`, `SECURITY.md`, `SUPPORT.md`, `GOVERNANCE.md` |

---

## Milestone tracker

### Milestone 0 — Contracts & Spikes — COMPLETE (17/17)  (issue milestone "Milestone 0 - Contracts and Spikes")
_Goal: freeze contracts; prove each gear behind an adapter; atomic promotion; threat model. Exit: one manually-supplied login candidate compiles, verifies twice, and promotes with evidence (ADR §22)._

| # | Issue | Status |
|---|---|---|
| #1 | [M0-00] Monorepo & tooling bootstrap | ☑ done |
| #2 | [M0-01] Freeze contract: EvidenceRef | ☑ done |
| #3 | [M0-02] Freeze contract: Workflow v1 IR | ☑ done |
| #4 | [M0-03] Freeze contract: Diagnostics | ☑ done |
| #5 | [M0-04] Freeze contract: Bundle manifest | ☑ done |
| #6 | [M0-05] Freeze contract: Adapter interfaces | ☑ done |
| #7 | [M0-06] License gate + SBOM automation | ☑ done |
| #8 | [M0-07] Spike: Understand-Anything subset extraction | ☑ done |
| #9 | [M0-08] Spike: ast-grep rule fixtures (Next.js + Express) | ☑ done |
| #10 | [M0-09] Spike: PlaywrightAgentAdapter handshake + fallback | ☑ done |
| #11 | [M0-10] Spike: Atomic promotion + last-known-good | ☑ done |
| #12 | [M0-11] Threat model + target-attestation | ☑ done |
| #13 | [M0-12] Test-fixture apps scaffold | ☑ done |
| #40 | [M0-13] Spike: ModelAdapter | ☑ done |
| #41 | [M0-14] Policy engine: action classes + fail-closed | ☑ done |
| #44 | [M0-15] Arxic target-attestation accepts reference apps (depends #12, #13) | ☑ done |
| #14 | [M0-EXIT] Gate: login candidate verifies twice + promotes | ☑ done |

### Milestone 1 — Authentication Vertical Slice  (issue milestone "Milestone 1 - Authentication Vertical Slice")
_Goal: end-to-end evidence-driven auth bundles. Exit: two structurally different reference apps produce independently replayable bundles without app-specific generator code (ADR §22, §23)._

| # | Issue | Status |
|---|---|---|
| #15 | [M1-01] Static inventory + evidence graph | ☑ done |
| #16 | [M1-02] Crawlee breadth surface map | ☑ done |
| #17 | [M1-03] LangGraph orchestrator (pipeline 0-12) | ☑ done |
| #18 | [M1-04] Candidate reconciliation + coverage matrix | ☑ done |
| #19 | [M1-05] Fixture adapters (persona/inbox/otp) | ☑ done |
| #20 | [M1-06] Playwright compiler: IR → plan + spec | ☑ done |
| #21 | [M1-07] Verifier: runner, screenshots, traces, 2-pass | ☑ done |
| #22 | [M1-08] Auth domain pack (login/logout/reset/change/TOTP) | ☑ done |
| #23 | [M1-09] Coverage matrix + blocked reporting | ☑ done |
| #24 | [M1-10] Bundle packaging (hashes/NOTICE/provenance) | ☑ done |
| #25 | [M1-11] CLI app | ☑ done |
| #26 | [M1-12] Worker (ephemeral isolated) | ☑ done |
| #42 | [M1-14] Stage 4: LLM candidate inference (no promotion) | ☑ done |
| #43 | [M1-15] Stage 8: intent exploration + human-approval | ☑ done |
| #27 | [M1-EXIT] Gate: two apps replay, no app-specific code | ☑ **PASS — M1-EXIT achieved** (PR #121): reference 14/14 · vulnerable 14/14 MET against ADR §23. §23.12 (failed-run-preserves-prior-bundle) now proven end-to-end for BOTH apps (#109); §23.3/§23.4 Express accepted MET (generic/vacuous — same standard as the 2026-08-08 #110 baseline); §23.13 SBOM tracked as a post-M1 packaging follow-up (CI-produced, not yet in the bundle) |

### Milestone 2 — Hardening (in flight; GitHub milestone not yet filed)

_M2 work has begun on the two open issues below. The GitHub "Milestone 2" object is not yet created (ADR §22 deferred filing until contracts stabilized; M1 contracts are now stable, so filing is reasonable whenever the next batch is planned). #116 is decomposed into lettered slices; #103 groundwork is merged but the full sandbox pipeline is blocked on infrastructure._

| # | Issue | Status |
|---|---|---|
| #116 | [M2 design + impl] Intent-backed pseudocode (IntentSpec) — ADR-004 design landed; **slice A (`@arxic/intent` Service mechanics) landed**; slices B–F pending | 🚧 in progress (1 of 6 slices) |
| #103 | [M2] Worker-backed CLI (`WorkerClient`-backed `RunExecutor`) — groundwork landed (`--executor local\|worker`, fail-closed classification); full stages-0–12 sandbox execution blocked on Node-22 pipeline image + transport + result protocol | 🚧 in progress (groundwork only) |

_#116 slice plan (per ADR-004 §3/§4): **A** (done) `@arxic/intent` Service mechanics → **B** orchestrator stage-8/9 oracle-resolution Action boundary → **C** ADR §6 locator policy (semantic + execution locator → same element) → **D** compiler integration (reject unresolved assertions; provenance in run-local artifact, not frozen Workflow) → **E** ADR §7 sensitivity probe → **F** two-app real-browser proof matrix (≥1 source/runtime conflict + ≥1 observed-only characterization) + ADR-004 Accepted flip. Each slice is a vertical PR with sad-path-first TDD + real proof._

_Milestone 3 (service mode) remains NOT yet filed — deferred (ADR §22)._

_Notes: pipeline **stage 11 (healing)** is intentionally deferred to M2 (only #10's healer-policy rejection and #43's exploration policy touch it today). M1 issue keys skip `M1-13` by design — the M1 gate is `M1-EXIT` = #27._

---

## Decisions (resolved)

- **License:** **MIT** — confirmed (`LICENSE`, `package.json`). Third-party notices in `NOTICE`.
- **Package manager:** **pnpm** workspaces via corepack (`packageManager` field). Node **≥22** (pnpm 11 requirement).
- **Build strategy (M0):** **source-only — no emit.** Packages consume each other via `main: src/index.ts` (raw `.ts`), checked by `tsc --noEmit` and compiled on demand by vitest. No `build`/`dist` in M0; emit deferred to when a package ships a published artifact (ADR-003).
- **Git flow (active — discipline-based):** The repo is **private**, so GitHub **branch protection is OFF** (free-tier limit for private repos). Trunk-based: short-lived branches → PR → squash-merge to `main`. **Discipline rule: merge ONLY after the `ci` check is green** (`gh pr checks <N> --watch` → `pass`); rebase stale Dependabot branches onto `main` before evaluating CI. No direct pushes/force-push; linear history. Re-enable branch protection when the repo goes public (post-purge) or upgrades to Pro.
- **Branching strategy:** **trunk-based** (confirmed) — no `dev`/`release` branch; short-lived `feat/`·`fix/`·`docs/` branches → PR → `main`; releases cut from `main` via tags. See `CONTRIBUTING.md` → Branching model.
- **Repo visibility:** **PRIVATE during pre-1.0** (recipe owner-only; history not purged — see reminder below). Side effect: no branch protection on the free tier → CI enforcement is discipline-based (above). A history purge (Support/recreate) is performed before going public; protection is re-enabled then.

---

## How we work (conventions)

- **Truth states (ADR §2):** hypothesized · observed · **verified** · contradicted · blocked. An LLM may never assign `verified` — only the deterministic verifier can.
- **Adapters, not forks:** every borrowed engine sits behind a contract (ADR §10.5). Return contracts, never upstream types.
- **Evidence everywhere:** output-influencing graph edges carry ≥1 `EvidenceRef` (ADR §8.4). No evidence → no `verified`.
- **Safety:** untrusted repo + adversarial web content are hostile. Default-deny egress, leased fixtures, content-is-data (prompt-injection defense), redact PII/secrets (ADR §16).
- **Never weaken to pass:** no skip/fixme/only, no assertion weakening, no success-by-quarantine (ADR §7.3, §13.1, §15).
- **Git flow:** `main` is protected — every change is a PR (squash merge), CI check `ci` must pass, linear history, no force-push. Follow `.github/pull_request_template.md` + charter §8 on every slice.
- **Engineering method:** see [`engineering-charter.md`](./engineering-charter.md). TDD red-first in vertical slices (mock only at system boundaries); Actions vs Service layering (§10.5 adapters = service capability blocks; RunState/pipeline = orchestration that owns failure classification). **Sad-path-first** — every slice enumerates sad/edge cases mapped to a truth state before happy path.
- **Real-world proof (dogfooded):** every slice must prove itself with real, non-synthetic user-level tests against the reference apps + real engines (real Chromium / real `sg` CLI / real Mailpit+otplib / real Tree-sitter). "Works on my mock" is banned. Hard dependency: **#13 (reference fixture apps) must land before spikes #8–#12 or any M1 slice can claim real-world proof.**

---

## Session log

| Date | What happened |
|---|---|
| 2026-08-04 | Built §18 monorepo layout; wrote root tooling (pnpm, tsconfig.base, eslint/prettier stubs, LICENSE/NOTICE/README); filed milestones + 12 area labels + 27 issues (M0 14, M1 13); saved canonical end-to-end architecture diagram into ADR §8. |
| 2026-08-04 (2) | Release readiness: complete MIT licensing; versioning (`VERSION` + `RELEASES.md` + always-synced `CHANGELOG.md` wired into charter §8 + PR template); maturity docs (CONTRIBUTING/CODE_OF_CONDUCT/SECURITY/SUPPORT/GOVERNANCE) + expanded README; functional tooling (eslint flat + tsconfig, contracts entry, pnpm-lock); GitHub CI + release workflows, Dependabot, issue templates, CODEOWNERS; repo settings (squash-only, delete-branch-on-merge) + security alerts; **CI green**; `main` branch protection (PR flow). |
| 2026-08-04 (3) | Pre-dev audit (3 reviewers) + remediation: PRs #36 (release perms), #37 (CI guards + hygiene), #38 (mask ADR + remove reference-collection docs), #39 (SECURITY SLA + ADR-002), #45 (sync). Removed vendored reference code from the tree; full ADR → local-only (outside the repo), public summary in `docs/adr/001-…`; ADR-002 (EvidenceRef = opaque IDs). **Repo set PRIVATE during pre-1.0** — GitHub PR refs still expose pre-mask history, so purge deferred to go-public. Filed #40–44; split #13; §23.14 acceptance in adapter issues; filled real-world/layering in #1–#14. Totals M0=17, M1=15. **CI green.** |
| 2026-08-04 (4) | **#1 (M0-00) tooling bootstrap DONE.** Per-package `tsconfig.json` + `typecheck` script across all 16 workspace packages; root `typecheck:packages` (`pnpm -r typecheck`); ESLint bans `it.only`/`skip`/`xit`/`xdescribe` (ADR §13.1); `.env.example`; structural test guards the per-package tooling contract. **ADR-003: M0 source-only (no emit).** Gates green (lint/typecheck/typecheck:packages/format/test). Starting contract freeze (#2). |
| 2026-08-04 (5) | **#2 (M0-01) EvidenceRef contract frozen.** `schemas/evidence/{evidence-ref,source-revision,evidence-index}.schema.json` (JSON Schema 2020-12) + AJV 2020-12 strict validators + hand-written TS types in `@arxic/contracts`. Discriminated union (ADR §10.2), `SourceRevision` (§10.1), `evidence/index.json` + id grammar (ADR-002). `startLine≤endLine` via AJV `$data` (schema-pure). Stable diagnostics `ARXIC-EVIDENCE-*`. Sad-path-first + tautology guard; 30 tests green. **Scope boundaries:** runtime network/console gating → #21; dirty-tree blob-link manufacturing → #8. |
| 2026-08-04 (6) | **#3 (M0-02) Workflow v1 IR frozen.** `schemas/workflow/workflow.schema.json` (2020-12) + AJV validator + TS types (`Workflow`/`WorkflowTransition`/`TruthState`). **`status:"verified"` rejected unless every required transition carries `run:` runtime evidence** (ADR §2/§15/ADR-002 — an LLM may never assign verified); transitions `required:true` by default; optional non-blocking; 5 truth states; confidence descriptive-only. Stable diagnostics `ARXIC-WORKFLOW-*`; ADR §10.3 literal + sad-paths + tautology guard; 46 tests green. |
| 2026-08-04 (7) | **#4 (M0-03) Diagnostics frozen.** `schemas/diagnostics/diagnostics.schema.json` (2020-12) + AJV + widened `Diagnostic`/`DiagnosticSeverity` types (ADR §10.4). `severity` enum = 4 non-`verified` truth states; `code` pattern `^ARXIC-[A-Z0-9][A-Z0-9-]*$`; `evidenceRefs` accept arbitrary refs (ADR `config:idp-provider`). **Loop-closing contract test dynamically iterates every exported `ARXIC-*` code and validates each.** Sad-paths + tautology guard; 55 tests green. 4/5 contracts frozen. |
| 2026-08-04 (8) | **#5 (M0-04) Bundle manifest frozen.** `schemas/manifest/manifest.schema.json` (2020-12) + AJV + `BundleManifest` types (ADR §14 + issue #5 field list). `blockers` inline the frozen Diagnostic shape; loop-closing test proves a manifest blocker validates via `validateDiagnostic` (#4). Sad-paths (missing digest/commit/hashes, gate-missing, denominator-invalid, bad lengths, blocker `severity:"verified"`, extra property) + tautology guard; 70 tests green. **All JSON-Schema contracts frozen.** Boundary: cross-field totals + denominator immutability → #23/#24. |
| 2026-08-04 (9) | **#6 (M0-05) Adapter interfaces frozen → 🎯 M0 goal 1 (contract freeze) COMPLETE.** 7 ADR §10.5 TS interfaces + minimal supporting types in `@arxic/contracts` (reusing #2–#5 frozen types). Compile-time `@ts-expect-error` test (`adapters.test-d.ts`) proves adapters can't leak upstream types (fail-closed at type level — gate reds if an interface widens). `VerificationResult.outcome` = 5 `TruthState`s (no 6th "flaky" — verifier-internal #21, derivable from `runs[]`). Boundary-double runtime test exercises all 7 interfaces. 72 tests green. **All 6 contracts frozen.** Next: #13 fixture apps (large slice, checkpoint). |
| 2026-08-04 (10) | **#13 (M0-12) Test-fixture apps DONE.** Two REAL structurally-different auth apps: `reference-auth-app` (Next.js 15 App Router + better-sqlite3 + bcryptjs + nodemailer→Mailpit + otplib) — full §12.1 (login/logout/forgot-reset/change-password/MFA enroll+challenge), HMAC sessions, CSRF, rate limit; `vulnerable-auth-app` (Express + ejs + sqlite) — login/logout/reset with documented weaknesses (enumerating login, reused + 7-day tokens, no CSRF, no rate limit, verbose errors). Both serve attestation `environmentClass:"local-test"` + seed/reset API. Real Mailpit via `docker-compose.yml`. **REAL boot tests green against real Mailpit** (reset email delivered + token extracted; reference MFA needs real otplib TOTP; vulnerable enumeration + token-reuse weaknesses proven). Per-app typecheck/lint/test excluded from root gate; CI `ci` job gates both apps with a Mailpit service. `pnpm-workspace.yaml allowBuilds` for native deps. **Spikes #8–#12 unblocked.** |
| 2026-08-04 (11) | **#7 (M0-06) License gate + SBOM DONE.** `scripts/license-gate.mjs` scans the real pnpm graph (440 pkgs) against an allowlist (permissive + weak-copyleft MPL/LGPL) and rejects GPL/AGPL/SSPL/Commons-Clause/BSL/EUPL/CC-BY-SA/PolyForm/Unknown. `license-exceptions.json` (`thirty-two` → MIT upstream). CI: license gate + CycloneDX SBOM artifact + `third_party/` vendored-code guard. TDD sad-paths (AGPL/GPL/SSPL rejected; **LGPL allowed — distinguished from GPL**) + real-graph assertion (0 rejected). 93 tests green. 8/17 M0 done. |
| 2026-08-05 | **#8 (M0-07) Understand-Anything subset extraction DONE.** `@arxic/source-ua-adapter` adapts the reviewed deterministic scanner/structure seams behind frozen `SourceIndexer`: full Git SHA, bytewise manifest + SHA-256, dirty provenance guard, fail-closed gaps, and real native Tree-sitter symbols/imports/calls/Next.js+Express routes. Canonical output is byte-identical pre-timestamp; ADR §23.14 contract gate + separate MIT grammar-license test. Real proof scans both fixture-app source trees. 106 tests green. 9/17 M0 done. |
| 2026-08-05 (2) | **#9 (M0-08) ast-grep rule fixtures DONE.** Real ast-grep 0.45 process scans versioned Next.js and Express packs; committed source refs and stable diagnostics validate through frozen contracts. Real fixture-app login route→handler→guard chains connect as advisory `hypothesized`; per-rule positive/negative fixtures and fail-closed sad paths cover the seam. 10/17 M0 done. |
| 2026-08-05 (3) | **#10 (M0-09) PlaywrightAgentAdapter handshake + fallback DONE.** Exactly pinned Playwright Test 1.62.1 MCP handshake and nine-tool schema gate fail closed on seam drift; healer policy rejects weakening and unsafe boundaries. Real agent and generated fallback both execute seeded login against the reference app in real Chromium; runtime disposition remains `observed`. 11/17 M0 done. |
| 2026-08-05 (4) | **#11 (M0-10) Atomic promotion + last-known-good DONE.** Deterministic canonical bundle bytes, frozen manifest validation, staged SHA-256 and byte-count checks, same-directory atomic rename, `.lkg` snapshots, and exclusive locking ship behind the frozen `BundlePromoter`; real filesystem failures and contention are `blocked` without corrupting public bytes. 12/17 M0 done. |
| 2026-08-05 (5) | **#12 (M0-11) Threat model + target-attestation DONE.** Real HTTP handshakes against both fixture apps allow exact local-test attestations; production-looking, origin, nonce, and unsigned-receipt failures are `blocked`, while only static recorded human approval permits the production-shaped proof target. Worker, prompt-injection, action, privacy, and Docker/Testcontainers isolation requirements are documented. 13/17 M0 done; #44 unblocked. |
| 2026-08-05 (6) | **#40 (M0-13) ModelAdapter DONE.** Credentials resolve-at-call-time Bearer-only, structured output is schema-bound + real-AJV-validated, invalid output retries then blocks with no promotion, schema-version drift fails closed, content-as-data injection is blocked without policy mutation, and run records carry only request id/schema version/token+provider metadata behind a redaction gate; stable `ARXIC-MODEL-*` diagnostics loop-close; real local OpenAI-compatible stub + real AJV prove it. 14/17 M0 done. |
| 2026-08-05 (7) | **#41 (M0-14) Policy engine DONE.** One-argument `authorize(PolicyAuthorization)` plus configured `PolicyEngine.decide` enforce six registered actions across the four frozen action classes; exact origin allowlists, caller-owned lease and budget state, sandbox presence, and exact recorded approvals fail closed with stable `ARXIC-POLICY-*` diagnostics. Canonical snapshots hash full inputs, and live reference-app attestation covers all five required decisions. 15/17 M0 done. |
| 2026-08-05 (8) | **#44 (M0-15) PREFLIGHT target-attestation acceptance DONE.** The existing real handshake now runs across target sets; both real reference apps are accepted as `local-test`; production-styled and missing or malformed attestations are refused with blocked diagnostics; deterministic canonical run artifacts record every decision; and artifact write failures fail closed. 16/17 M0 done. |
| 2026-08-05 (9) | **#14 (M0-EXIT) Milestone 0 capstone DONE.** The thin `@arxic/m0-pipeline` orchestrator composes target attestation, committed login source evidence, Workflow compilation, two clean-fixture real-Chromium verification passes, and atomic promotion. The deterministic verifier produced `verified`; promoted screenshots, trace, spec, NOTICE, provenance, manifest, and hashes were independently checked; an injected third-run source-evidence failure preserved the second last-known-good bundle. **17/17 M0 complete; 0.1.0 release pending.** Next: #15 M1-01. |
| 2026-08-05 (10) | **#15 (M1-01) Static inventory + evidence graph DONE.** `@arxic/evidence-graph` (Graphology) ingests real `SourceUaAdapter` (Tree-sitter) + real `AstGrepAdapter` (`sg`) outputs into a typed, content-addressed graph (ADR §8.4). Output-influencing edges require ≥1 EvidenceRef at compile time (non-empty tuple) + runtime (`ARXIC-GRAPH-001` `blocked`); conflicting node/edge structure surfaces `ARXIC-GRAPH-002`/`-003` `contradicted`. Canonical sorted JSON/JSONL is SHA-256-addressed and byte-identical across rebuilds (property + real-fixture determinism tests). Real-world proof connects both real fixture apps' `/login` route→handler→guard with ≥2 real source EvidenceRefs per edge. The frozen §10 contracts are unchanged (local `GraphIngestEvent` bridges the semantics gap). Gates green (typecheck/lint/format/test, 262 tests). Dispositions: `contradicted`, `blocked`. Final classification deferred to orchestrator (#17)/reconciler (#18). **M1 1/15.** Next: #16 M1-02. |
| 2026-08-05 (11) | **#16 (M1-02) Crawlee breadth surface map DONE.** `@arxic/crawlee-adapter` runs a real Crawlee 3.18 `PlaywrightCrawler` (Playwright 1.62.1) behind the frozen `SurfaceDiscoverer` contract to enumerate routes/forms/controls/links/navigation-edges within `origin`/`maxUrls`/`maxDepth` budgets. ADR §7.4 prohibitions are fail-closed policy: destructive forms observed but never submitted (`ARXIC-SURFACE-002`), cross-origin browser requests aborted via request interception + cross-origin WebSockets closed before connect (`ARXIC-SURFACE-001`), mutable personas serialized through a per-identity mutex (`ARXIC-SURFACE-004`), frontier bounds (`ARXIC-SURFACE-003`), transient HTTP 408/425/429/≥500 retries give up bounded as `observed` never `verified` (`ARXIC-SURFACE-005`). Runtime EvidenceRefs withheld without a valid attested build digest (`ARXIC-SURFACE-007`); invalid origins rejected (`ARXIC-SURFACE-006`). Stable `ARXIC-SURFACE-001..008` loop-close through the frozen validator; `navigationEdges` are deduped (shallowest depth) and deterministically ordered so `serializeSurfaceMap` is byte-stable under concurrency. Real-world proof crawls the REAL `reference-auth-app` in real Chromium within budgets — `/`, `/login`, `/forgot-password` carry real runtime evidence; sqlite mutation budget (`users`/`reset_tokens`/`sessions`/`mfa_challenges`) is byte-for-byte unchanged after the crawl. Default-deny mutation is enforced at both inventory and network layers (same-origin non-safe HTTP methods aborted, `ARXIC-SURFACE-008`). Frozen §10 contracts unchanged (adapter-local `SurfaceMap`/`RouteSurface`/`NavigationEdge` bridge to `EvidenceEvent`). Gates green (typecheck/lint/format/test, 274 tests). **M1 2/15.** Next: #17 M1-03. |
| 2026-08-05 (12) | **#17 (M1-03) LangGraph orchestrator DONE.** Real LangGraph 1.4.9 `StateGraph` composes stages 0–12 with file/in-memory per-stage checkpoints and immutable SHA-256 artifact refs. Restart proof against the REAL `reference-auth-app` reconstructs during stage 4, reuses stages 0–3, then runs real Crawlee/Chromium; malformed output retries then blocks without promotion, empty coverage remains `observed`, hash/redaction mismatches fail closed, and stage 8 pauses for recorded approval. Checkpoints record tool/package versions, hashes, model request IDs without prompts, decisions/approvals, gates, and redaction outcomes. Stage 11 is a recorded M2-deferred no-op; only stage 10 may originate `verified`. Frozen §10 contracts unchanged. Gates green (292 tests). **M1 3/15.** Next: #18. |
| 2026-08-05 (13) | **#18 (M1-04) Candidate reconciliation + coverage matrix DONE.** `@arxic/reconciler` merges source/workflow candidates with runtime routes/forms/controls in a transient Graphology graph, prioritizes accountability potential, emits deterministic §11 coverage rows and overlap summaries, and guards frozen denominators. Source-only=`hypothesized`, runtime-only/static+runtime=`observed`, conflicts=`contradicted`, unsupported/unsafe=`blocked`; never `verified`. Real Tree-sitter + `sg` and Crawlee/Chromium proof covers both fixture apps and blocks the vulnerable app's unsupported CSRF coverage. Frozen contracts and orchestrator graph unchanged. Gates green (308 tests). **M1 4/15.** Next: #19. |
| 2026-08-06 | **#19 (M1-05) Fixture adapters DONE.** Real otplib OTP, Mailpit inbox, and app seed/reset persona service blocks implement the frozen `FixtureProvider`; the stage-7 coordinator provisions opaque leases, blocks missing providers and unsafe fixture paths, redacts secret/PII requirements, and detects/reset/releases leakage. Optional Testcontainers Mailpit launch remains in the trusted environment package. Real reference-app SMTP reset mail, seed/reset, Mailpit, and TOTP proof leaves no fixture leakage. Frozen contracts and LangGraph topology unchanged. Gates green (322 tests). **M1 5/15.** Next: #20. |
| 2026-08-06 (2) | **#20 (M1-06) Playwright compiler DONE.** `@arxic/playwright-compiler` implements the frozen `WorkflowCompiler.compile(workflow, observations)` → `StagedBundle` with validated manifest, evidence index, SHA-256 artifacts, and human-readable plan. Service-layer generators render a spec with §13.1 semantic locators (getByRole/Label/Text), transition-tied screenshot checkpoints, env-var input references (never literal secrets), and a serial clean-reset fixture (`workers:1`, cookie-clear `beforeEach`/`afterEach`). The action-layer compile-policy gate rejects `waitForTimeout`/`waitForLoadState`/`page.evaluate`, CSS/XPath without a diagnostic, and literal secret/PII as `blocked` with stable `ARXIC-COMPILE-*` diagnostics. Unsupported steps and missing source/runtime observations block without dropping. Real reference-app proof compiles a login Workflow IR and the real Playwright CLI lists the staged test. Frozen contracts and LangGraph topology unchanged. Gates green (335 tests). **M1 6/15.** Next: #21. |
| 2026-08-06 (3) | **#21 (M1-07) Verifier DONE.** `@arxic/verifier` implements the frozen `WorkflowVerifier.verify(bundle, policy)` → `VerificationResult` with deterministic truth-state classification. Service blocks run the real Playwright CLI, capture/SHA-256-hash screenshots + traces, and reset/seed fixtures between runs. The action-layer classifier maps all-clean-passes → `verified`, split runs → `contradicted` (flaky), all-fail → `contradicted` (app defect), and missing fixture/network/artifact/transition gates → `blocked` with stable `ARXIC-VERIFY-*` diagnostics. Staged bundle hashes are validated before execution; safe path resolution prevents artifact escapes. Real reference-app proof compiles a login workflow via the #20 compiler, verifies two real Chromium passes → `verified` with hash-checked artifacts, and proves injected locator drift → `contradicted` (no silent pass). Frozen contracts and LangGraph topology unchanged. Gates green (347 tests). **M1 7/15.** Next: #22. |
| 2026-08-06 (4) | **#20/#21 review fixes.** Independent hy3 + deepseek review of the merged compiler + verifier found 5 defects: (1) verifier evidence gate dead on default path (auto-observe transitions on pass); (2) compiler fabricated `passed:true` manifest run (→ `passed:false`); (3) no-secrets gate bypassable via assertion literals (extended scanning); (4) runner exit-code 2 misclassified as test failure (only code 1 now); (5) classification order inversion masking contradictions (reordered). All fixed in PR #80. |
| 2026-08-06 (5) | **#22 (M1-08) Auth domain pack DONE.** `@arxic/auth-domain-pack` defines 6 auth workflow candidates (login, logout, reset-request, reset-complete, password-change, TOTP) and assembles them into a domain pack via the #20 compiler + #21 verifier. Real reference-app proof verifies login, logout, and password-change in real Chromium; reset-request/reset-complete are `blocked` (inbox fixture); TOTP is `blocked` (totp fixture). Emits `domain-manifest.json` + `coverage-matrix.json`. Compiler hardened per hy3 review: camelCase labels split correctly, submit-button regex covers full §12.1 inventory, screenshots use unique step-indexed names, verifier plumbs arbitrary persona env vars. Fabricated `compilerBlocker` strings removed. Gates green (355+ tests). **M1 8/15.** Next: #23. |
| 2026-08-06 (6) | **#23 (M1-09) Coverage matrix + blocked reporting DONE.** `@arxic/reconciler` gains `report.ts`: `enrichSupportedFixes()` adds actionable `supportedFixes` to blocked diagnostics; `compileCoverageReport()` integrates reconciliation with verification outcomes (including `verified`) and reports accountability-verified gap; `detectPostFreezeDiscovery()` blocks post-freeze additions. `serializeCoverageReport()` produces canonical JSON. Real reference-app proof combines real auth-domain-pack outcomes (3 verified, 3 blocked) into a coverage report with concrete `supportedFixes`. Gates green (325 unit + 22 reconciler). **M1 9/15.** Next: #24. |
| 2026-08-06 (7) | **#24 (M1-10) Bundle packaging DONE.** `@arxic/bundle-promoter` gains `bundle-assembler.ts` + `redaction-gate.ts`. `assembleBundle()` writes the full §14 directory (manifest/workflow/plan/tests/fixtures/evidence/artifacts/provenance/NOTICE/checksums.sha256). `scanBundleForSensitiveData()` blocks real emails, bearer tokens, API keys, password literals, and session cookies while allowlisting `process.env`/`ARXIC_INPUT_*`. Real reference-app proof compiles + verifies login, assembles the bundle, passes redaction, and independently validates every checksum. Gates green (334 unit + 17 bundle-promoter). **M1 10/15.** Next: #25. |
| 2026-08-07 | **Charter §10 parallel slice protocol landed (#90).** Four M1 slices moved to concurrent execution, one agent per `git worktree`. §10.1 worktree isolation, §10.2 deferred doc ritual via `docs/_slice-notes/<SLICE-ID>.md` (slice branches must not touch `SYNC.md`/`CHANGELOG.md`/`VERSION`), §10.3 declared file ownership, §10.4 serialized merge queue. `AGENTS.md` + PR template carry the same rules for auto-loading agents. Landed later than the workers started, which cost two fix rounds — both agents invented `docs/_slice_notes/` (underscore) because the real directory and its `_TEMPLATE.md` did not yet exist on `main`. |
| 2026-08-07 (2) | **#26 (M1-12) CLI↔worker seam types landed (#84)** — partial deliverable, issue remains open. `apps/worker` gains the `run-spec` contract shared by the CLI (#25) and worker (#26) so both slices could proceed against a fixed seam instead of blocking on each other. |
| 2026-08-07 (3) | **#42 (M1-14) Stage 4: LLM candidate inference (no promotion) DONE.** `@arxic/orchestrator-langgraph` gains `inference.ts` (service layer) producing hypothesized workflow candidates from the smallest sufficient source-evidence neighbourhood via the `ModelAdapter` structured-output boundary: deterministic/deduped/bounded source-only neighbourhood selection, content-as-data messages (metadata projection — no source bytes or operator prompt), candidate mapping to a full schema-valid Workflow with `status` forced `hypothesized` and deduped pattern-valid evidence IDs, and a single-request adapter call that short-circuits an empty neighbourhood to zero candidates. `#infer` delegates into it (orchestrator owns retry + truth-state classification); `defaultInference` no longer fabricates (zero candidates when no model is configured). Candidates are emitted `hypothesized` only (never `verified` — the stage-4 schema has no status field); invalid structured output retries then `blocked` with no promotion; prompt-injection payloads are treated as data. Real Tree-sitter (`SourceUaAdapter`) + real `sg` (`AstGrepAdapter`) over the real `reference-auth-app` + a real `node:http` OpenAI-compatible endpoint prove ≥1 hypothesized candidate survives stage-6 reconciliation; malformed output → bounded retries → blocked. Gates green (typecheck/lint/format/test 387/license 0 rejected). Dispositions: hypothesized, observed, blocked. Integrator review raised two non-blocking follow-ups (empty-string `id`/`intent` gets a fabricated default because the stage-4 schema has no `minLength`; the bare `catch` in `stage4Infer` discards the cause so an adapter throw is reported as invalid output). **M1 11/15.** Next: #25. |
| 2026-08-07 (4) | **#27 (M1-EXIT) de-risk spike landed (#91)** — issue stays open; this measures the gate, it does not close it. `vulnerable-auth-app` (Express + ejs + sqlite, all four auth forms on `/`) was driven end-to-end through the **existing generic** compile → verify machinery alongside the Next.js reference app, with no app-specific code anywhere in `packages/**/src`. One generic compiler fix landed: form-scoped submit locators (`page.locator('form').filter({has: getByLabel(...)})`) resolve the Playwright strict-mode violations that multi-form pages caused, declared through the compile-policy gate's existing `nonSemanticLocatorDiagnostics` rationale mechanism rather than by weakening the gate. Express login now `verified` in two clean real-Chromium passes with hash-checked artifacts; all reference proofs stay green (a second app was added, not swapped in). New `packages/real-world-testkit` boots either app generically and the three real-world suites are parameterised via `describe.each(FIXTURE_APPS)`. Honest gaps measured rather than massaged: `authCandidates()` is over-fit to reference-app routes and yields `contradicted` on Express — filed, not hidden. Gaps filed as **#86** (single-input submit disambiguation), **#87** (goto route + `url:` assertion semantics from runtime observations), **#88** (domain pack over-fit — proper fix is evidence-driven inference via #42), **#89** (scope the locator rationale to the exact emitted pattern). §23 criteria 4, 12 and 14 remain unproven for Express. |
| 2026-08-07 (5) | **#25 (M1-11) CLI app DONE.** `@arxic/cli` provides `arxic run --config arxic.yaml`: ADR §19 config parsing + validation, run submission through a CLI-owned `RunExecutor` port, streamed frozen `Diagnostic`s (the audit record — never raw model prose), and an ADR §20.1 run directory (`run.json`, `diagnostics.jsonl`, `config.json`) alongside the orchestrator's per-stage `stages/NN.json` + `artifacts/NN.json`. Action/orchestration layer only (charter §1) — all pipeline mechanics delegate to `@arxic/orchestrator-langgraph`. Sad paths fail closed with stable codes: malformed/missing config and missing model configuration → exit 2 `ARXIC-CONFIG-*` (a diagnostic, never a stack trace); `environmentClass: production`, origins outside `allowedOrigins`, and URL userinfo → exit 2 `ARXIC-CONFIG-INVALID`; unreachable target → `blocked` with the run directory still written (exit 1). Real-world proof runs `runCli(['run','--config',…])` against the real `reference-auth-app` with real Chromium (Crawlee) + real `sg`/Tree-sitter. **Built before the #26 seam landed**, so the CLI defines no `ArxicConfig`/`RunSpec`/`WorkerClient` types of its own and drives the pipeline in-process via `LocalRunExecutor` (ADR §20 local-mode-first); `ParsedConfig` (`apps/cli/src/config/types.ts`) is marked PROVISIONAL and reconciles with the seam when #26 lands. **M1 12/15.** Next: #43. |
| 2026-08-07 (6) | **#43 (M1-15) Stage 8: intent exploration + human-approval interrupt DONE.** Implements ADR §8 TARGETED / §9 stage-8 loop (plan → policy check → human approval → explore → journal → budget → normalize to Workflow IR) in `@arxic/orchestrator-langgraph/exploration.ts`; `@arxic/policy-engine` authorizes origin + action class before any execution (forbidden / denied / budget-exhausted / budget-missing / origin-drift / step-failed → `blocked`); a new `PlaywrightExplorationDriver` service in `@arxic/playwright-agent-adapter` executes read-only steps in real Chromium and records runtime `EvidenceRef`s with a real `accessibilitySnapshotSha256` (CDP `Accessibility.getFullAXTree`). Exploration yields `observed` at best and may never originate `verified`; an unobserved required step stays `observed`, never promoted. New `ARXIC-EXPLORATION-*` diagnostics. Real Chromium (Playwright 1.62.1) proof against the real `reference-auth-app`: read-only `/login` yields an observed runtime EvidenceRef with a11y SHA-256 prefix `572fa81d921f`; destructive steps block without approval; budget exhaustion blocks gracefully. Gates green (typecheck/lint/format/test 385/license) with the full suite running, including the test/license/fixture-app steps that earlier runs had skipped. Merged second of the two `orchestrator.ts` slices, resolving the import + `OrchestratorOptions` conflict against #42 so the `#infer` and `#explore` lanes coexist. **M1 13/15.** Next: #26. |
| 2026-08-07 (7) | **#26 (M1-12) Worker (ephemeral isolated worker) DONE.** `@arxic/environment` gains the isolation mechanics (service): `docker-cli.ts` (structured Docker CLI wrapper — array args, no shell), `worker-sandbox.ts` (`createWorkerSandbox` — internal Docker network, non-root `--user`, `--read-only` rootfs, `--tmpfs /work`, read-only source bind, memory/swap/pids/cpu quotas, `--security-opt no-new-privileges`, `--cap-drop ALL`, no socket / daemon env / privileged / host network; `assertSafeSpec` rejects unsafe specs before any Docker call; idempotent `stop()` records `ARXIC-WORKER-CLEANUP-FAILED`), and `worker-diagnostics.ts` (7 stable `ARXIC-WORKER-*` codes, loop-closed). `@arxic/worker` gains the action layer: `worker-policy.ts` (`validateWorkerSecurity` fail-closed on socket / daemon env / privileged / host-net / root / host credentials / dangerous caps / writable source; `freezePolicy` immutable `RunPolicy`; `ingestContentAsData` content-is-data) and `worker-client.ts` (`createLocalWorkerClient` implementing the #84 `WorkerClient` seam; outcome classification maps OOM → `ARXIC-WORKER-QUOTA-EXCEEDED`, timeout → `ARXIC-WORKER-TIMEOUT`, generic exit → `ARXIC-WORKER-RUN-FAILED`; `stream()` ingests source as data and always tears the sandbox down). Real adversarial Docker proof: cross-job source and host-path reads DENIED; egress to the host gateway and `169.254.169.254` DENIED while a declared sibling stays reachable; memory-quota breach → real cgroup OOM (`OOMKilled`, 137) → `blocked`; non-root confirmed, `/etc` and `/work/source` writes denied, no docker socket / `DOCKER_*` env / `docker` CLI in the worker; a live injection payload in source → `ARXIC-WORKER-INJECTION-NEUTRALIZED` with the frozen policy returned unchanged and the run completing. **The CI failure that preceded this was a real portability bug, not a flake:** the dev host uid is 1000, equal to the container's `--user 1000:1000`, so `0700` `mkdtemp` dirs were readable locally while the CI runner's differing uid made every `/work/source` read fail — which is also why the injection test saw empty diagnostics (the payload was never read). Fixed test-side (source dirs `chmod 0755`, files `0644`) and the read-only-write assertion loosened to exit-code plus a busybox/GNU substring; **the hardcoded `'1000:1000'` default remains and is carried as a follow-up**. Composing the orchestrator pipeline inside the sandbox is deferred to CLI/orchestrator wiring. **M1 14/15.** Next: #27 M1-EXIT. |
| 2026-08-07 (8) | **#95 worker sandbox host-uid default + tightened read-only assertion DONE (#96).** `@arxic/environment/worker-sandbox.ts` no longer hardcodes `workerUser` to `'1000:1000'`; `defaultWorkerUser()` resolves `${process.getuid()}:${process.getgid()}` on POSIX so a bind-mounted source stays readable for whoever owns it, with a non-root fallback where `getuid`/`getgid` are undefined. `assertSafeSpec`'s root rejection is unchanged and still runs before any `docker run` — a host process running as root resolves to `0:0` and fails closed rather than silently running the worker as root. **No isolation property changed.** Critically, #93's masking `chmod 0755/0644` was **reverted** in both real-world suites, so every real-Docker test again exercises the real `0700` host-owned fixture condition; a new red-first test reads a `0700`/`0600` source under the default and asserts `id -u`/`id -g` match the host — red against the old hardcoded uid on any host whose uid is not 1000. The read-only-source assertion was **tightened**, not left broad: `/read-only file system/i` only, because busybox reuses `can't create` for permission errors and the old alternation could pass on a permissions failure while a missing `--read-only` mount went undetected. Independent hy3 + deepseek review APPROVE; they flagged a pre-existing gap where the root-rejection regex misses `uid:0` (root group) for an explicitly supplied `workerUser` — carried as a follow-up. Gates green; real Docker sandbox 16/16, worker-client 10/10. |
| 2026-08-07 (9) | **#97 M1-14 stage-4 inference follow-ups DONE (#98).** `minLength: 1` on the stage-4 schema's `id`/`intent` means an empty string is now rejected by real AJV at the adapter boundary and flows into the existing retry-then-`blocked` path, instead of `mapStage4Candidates` substituting a fabricated `'authentication.inferred'`/`'submit form'` — the project does not assert more than the evidence supports, and two empty candidates no longer collapse to one duplicated workflow id. A new `ARXIC-ORCH-INFERENCE-ERROR` diagnostic distinguishes "the adapter threw" from "the output was invalid", so a provider outage is no longer misreported as malformed model output; the truth state stays `blocked` either way. The redaction constraint held — the diagnostic is message-free/cause-redacted, so improving attribution did not open a channel for prompt bytes or credentials to reach diagnostics, artifacts, or checkpoints. Real Tree-sitter + real `sg` + the real reference app + a real HTTP model endpoint prove the hypothesized success path and the blocked malformed/throw paths with no prompt, credential, or canary leakage. |
| 2026-08-07 (10) | **#88 Auth domain pack: evidence-driven candidates DONE (#99).** `authCandidates()` no longer hardcodes reference-app routes. It derives login/logout/password-change/reset/TOTP candidates from a per-app `AuthSurface` — observed entry/success states, success assertions, and supported/unsupported capability flags — supplied as **data** by `@arxic/real-world-testkit` (`FixtureApp.authSurface`), with no app-name branching anywhere in `packages/**/src`. Candidates stay `hypothesized` at most (ADR §2); only the stage-10 verifier originates `verified`. A new `capabilityBlocker` + `ARXIC-AUTH-CAPABILITY-UNSUPPORTED` diagnostic, checked **before** the evidence and fixture gates, makes a structurally absent capability surface as an explicit `blocked` — never a dropped candidate and never a `contradicted` that hides a missing capability. Real-Chromium proof across both apps: reference unchanged (login/logout/password-change `verified`; reset×2/TOTP fixture-`blocked`); Express `vulnerable-auth-app` now verifies login/logout — previously `contradicted` on a 404 `goto /login` — and honestly blocks password-change/TOTP as unsupported. **`manifest.contradicted === 0` for both apps**, which is the over-fit the #91 spike filed and measured rather than massaged. Compiler and verifier untouched; the `@arxic/reconciler` real-world test follows the new `authCandidates(surface)` signature. |
| 2026-08-07 (11) | **#86 + #87 + #89 compiler generality DONE (#100).** Three issues, one branch, one PR — all three live in `spec-generator.ts`/`compile-policy.ts`, so splitting them across agents would have put three writers in one file. **#89:** the compile-policy escape hatch is now a keyhole — `findUnapprovedNonSemanticLocators()` accepts only the exact reviewed `page.locator('form')` shape, so any *other* CSS locator is still rejected even in a file that already carries a rationale. The gate was narrowed, not weakened. **#87:** entry navigation resolves from the observed runtime URL (later transitions keep state-derived routes), and `url:` assertions compile to `` new RegExp(`^${escapeRegExp(expectedUrl)}(?:[?#].*)?$`) `` — anchored at both ends with the path escaped literally, so `/` matches `/?message=Logged%20in` but **not** `/other`. Tolerant of query/fragment without letting a genuinely wrong route pass. **#86:** submit form scoping gains the semantic submit-button matcher plus an exact form-count guard; the residual two-form ambiguity on the Express app surfaces as an explicit `blocked` rather than a silent wrong pick. Real Playwright 1.62.1 Chromium against the Express app observed both Email-labelled forms; both fixture apps stay compiler-discoverable. Dispositions `observed` and `blocked` — nothing here assigns `verified`. Notably, **#100 and #99 independently fixed the same broken Express login path from different packages and agreed**: #100's first CI failure was its stale base still asserting `contradicted` where its own compiler fix produced `verified`, which is exactly what #99's rewritten test expects. Two agents, separate lanes, no communication, converging — stronger evidence than either PR alone. **Second parallel batch complete: 4/4 merged.** |
| 2026-08-07 (12) | **#102 (CONFIG-SEAM-RECONCILE) CLI/worker config type reconciliation DONE (#105).** The CLI's provisional `ParsedConfig` is removed and `apps/cli` now imports the worker seam's `ArxicConfig` (`apps/worker/src/run-spec.ts`) — one type, one owner. Owner chosen as `@arxic/worker` on the dependency direction (CLI → seam → `@arxic/contracts`); `@arxic/contracts` was argued and rejected because the seam's own header forbids adding to contracts without a new ADR. The PROVISIONAL marker is deleted, not reworded. `RunExecutor` and `LocalRunExecutor` are behaviour-identical — a type reconciliation, not a rewiring (the worker-backed executor is #103/M2). To unify with zero validation-behaviour change, `ArxicConfig`'s policy/models literals were **widened** to the surface the CLI validator already accepted, bridged by a validated-boundary cast in `validate.ts` whose membership is runtime-checked immediately above it. Every `ARXIC-CONFIG-*` fail-closed path holds, proved by the **unmodified** `config.test.ts` and `real-world.test.ts`. Acknowledged trade: widening removes the worker's compile-time literal backstop, a real if small loss of type-level documentation, accepted in exchange for one-type/one-owner. **The reconciliation surfaced a genuine semantic divergence (→ #104), which is the most valuable thing this slice produced** — see the security-gap note at the top of this file. |
| 2026-08-07 (13) | **#101 (ROOT-GROUP-REGEX) worker sandbox rejects root-group `workerUser` DONE (#106).** `assertSafeSpec` now rejects a root identity in **either** the uid or the gid half of a caller-supplied `workerUser`, before any `docker run`: the `root` name in any case, and every integer form Docker parses as zero (`0`, `00`, `+0`, `-0`). **The slice found a wider hole than the one filed:** Docker maps an *omitted* half to 0, so `1000:` (gid 0), `:` (uid 0) and an empty `workerUser` (uid 0) all silently produced a root identity and passed the old check. Verified empirically against real Docker 29.7.1 and the official `docker run` reference rather than inferred. 14 matrix rows now reject in ~6ms with zero Docker calls, and a new positive test proves a caller-supplied non-root `workerUser` is accepted and runs as exactly that uid:gid. Red-first: pre-fix, the `1000:0` row started a **live container** instead of throwing. `defaultWorkerUser()` unchanged, no isolation property weakened; the real-Docker suite re-proved non-root, read-only rootfs, writable tmpfs, readable `0700` host-private source, cross-job/host-gateway/cloud-metadata egress denial, OOM quota kill, per-job network isolation, and idempotent cleanup. Known limit, documented in code: image-local name aliases resolving to uid 0 (e.g. `toor`) cannot be caught without resolving against the image's account DB, which would break the reject-before-any-Docker-call property. **Method note worth keeping:** the two independent reviewers *disagreed* — hy3 APPROVE, deepseek REQUEST CHANGES — and deepseek's blocking finding (the empty-component bypass) reproduced against real Docker and was correct. Consensus alone would have shipped the hole; the empirical check overrode it. Prefer ground truth to agreement. |
| 2026-08-07 (14) | **#104 CLI policy validation narrowed to the worker's accepted values DONE (#107).** `validateConfig` now accepts only `policy.mutation: 'leased-fixtures-only'` and `policy.externalNetwork: 'deny'`, and `ArxicConfig` carries both as singleton literals — a config the worker would refuse at run time no longer typechecks. ADR §19's example config and §575's lease-scoped-mutation invariant admit exactly one value each, so `'allow'` was never legitimate; every config in the repo already used the accepted pair, so nothing broke. Two independent reviews, both of which changed the change: **hy3** flagged that `} as ArxicConfig` was the only whole-object type assertion in the repo and defeated structural checking of the entire returned config — replaced with three scoped assertions at the points control flow actually proves (`policy.mutation`, `policy.externalNetwork`, `models.sourceRetention`), each preceded by a diagnostic-pushing check and the `diagnostics.length > 0` early return. Removing the blanket cast immediately earned its keep: `tsc` surfaced `models.sourceRetention` as an unchecked `string` flowing into a `'disabled' \| 'retained'` field, invisible until then. The repo now has zero whole-object casts. **codex** returned REQUEST_CHANGES and refuted a claim in my own severity note (see the crawler retraction at the top of this file → #108), and caught that the `ArxicConfig` doc comment still said the safe subset was enforced "not by this type" — untrue the moment the type was narrowed, and exactly the sentence that would justify widening it back. **Method note:** hy3's first pass raised a soundness finding that did not exist; it had reasoned correctly from an inaccurate paraphrase of the return statement that I supplied. Given the real source it retracted three of four findings. The reviewer that read the files (codex) found the thing that mattered. Give reviewers the code, not a summary. |
| 2026-08-08 | **#27 (M1-EXIT) gate MEASURED — does not pass yet.** The merged tree (`fed2358`) was measured against all 14 ADR §23 criteria for both fixture apps, with **no new product code and no weakened tests** — the PR's entire diff is two doc files. Full suite run twice from clean state, both green (81 files / 534 tests, exit 0 each). Result: `reference-auth-app` **14/14 MET**; `vulnerable-auth-app` **13/14 MET**. The three #91 gaps were re-measured rather than assumed: **§23.4 now MET for Express** — the #87/#100 compiler fix resolves entry navigation from the observed runtime URL (`generality.real-world.test.ts` asserts `goto(origin + '/')`, not `goto(origin + '/login')`) and the verifier verifies Express login in two clean Chromium passes; **§23.14 MET for both** — per-adapter contract gates pin exact engine versions (Playwright 1.62.1 rejects drift) and pass; **§23.12 stays UNMET for Express** — failed-run-preserves-prior-bundle is proven for reference (`m0-pipeline`) and app-agnostically (`bundle-promoter`) but never exercised end-to-end against Express → **#109** (test gap, not a product defect). Criteria 6/7 are correctly `blocked` for Express (no provisioned inbox, no TOTP capability) and count MET — not massaged by provisioning what the app lacks. §23.10 MET for both **with the Service Worker path explicitly not covered** (#108). No app-specific code in the M1 generator path; the only app-specific non-test source is the M0-era `m0-pipeline`, which the two-app flow does not use. Two soft METs the integrator should keep in view: §23.4's reconciler half is vacuous for Express (it has no runtime-only routes), and §23.14 proves the gates exist and pass rather than that a major upgrade passed them. **Integrator's call: exit requires #109** — the mechanism being app-agnostic is inference, and this project does not accept inference in place of observation. |
| 2026-08-09 | **#111 (M1-TRACE-SANITIZATION) Playwright trace retention boundary DONE (PR #117).** New shared Service `@arxic/playwright-trace-sanitizer` replaces raw-trace retention with a deterministic action-timeline projection (closed vocabulary — projection not filtering) + adjacent provenance + independent bounded re-inspection; verifier/M0/exploration capture and bundle assembly/promotion all use it and fail closed on malformed/residual/mislabeled/oversized/forged artifacts. reviewer-hy3 APPROVE + ocr-delegate consensus; two blocking fixes pre-merge: **B1** charter-§1 duplicated capture logic extracted into Service `retainCaptureArtifacts` (structured return); **B2** a #111 regression (dropped screenshot↔checkpoint binding + unsound positional `slice()`) restored with fail-closed injected seam. CI then caught a **determinism bug**: `yazl@3.3.1` DOS timestamps were host-timezone-dependent (PST-generated evidence failed the canonical rebuild under UTC) — patched yazl to UTC (`patches/yazl@3.3.1.patch`), regenerated the 3 evidence ZIPs (entry bytes unchanged), added a red-first cross-timezone fixed-point test. CI run `31318752890` `success` (full suite, real Mailpit, SBOM, command-guard). Honest residual: discovery assumes Action-owned roots + quiescent workspace; same-UID intermediate-directory replacement stays UNVERIFIED (NOT race-safe). Pixel privacy is #115, not this slice. `docs/_slice-notes/M1-TRACE-SANITIZATION.md` folded in and deleted. |
| 2026-08-10 | **#115 (M1-SCREENSHOT-PRIVACY) screenshot pixel privacy DONE (PR #118, CI `success`).** New `@arxic/playwright-screenshot-privacy` Service (action-owned masking/approved-region policy + `.privacy.json` provenance + strict PNG/attestation validation) composed into the shared `retainCaptureArtifacts` capture Service; verifier + M0 retain only policy-attested (masked) screenshots, assembly/promotion independently validate PNG bytes + provenance, and capture fails closed (`SCREENSHOT_PRIVACY_REQUIRED`) without a policy — #111's trace sanitization/checkpoint binding/canonical determinism preserved (extended, not forked). reviewer-hy3 consensus caught a blocking P1 (m0 bundle dropped `screenshot-capture-runtime` → fixed to include runtime + `playwright-config`); CI then caught the cross-package blast radius (auth-domain-pack/reconciler now require the operator policy — added `AuthDomainPackOptions.screenshotPrivacyPolicy` production hook) and a CI-slower-FS timeout (raised). Workspace package count 22→23. Pre-existing unmasked `docs/evidence/M1-15/step-00-*.png` deleted. **Honest residual:** arbitrary page pixels / IDAT steganography cannot be mechanically proven secret-free — retained screenshots require independent HUMAN visual sign-off (tracked, not blocking code). `docs/_slice-notes/M1-SCREENSHOT-PRIVACY.md` folded in and deleted. |
| 2026-08-10 | **#112 (M1-BUNDLE-INTEGRITY) staged workflow/manifest integrity DONE (PR #119, CI `success`).** A cross-field validator rejects mismatched workflow↔manifest `id`/`status` (truth-state) before promotion (`ARXIC-PROMOTION-VALIDATION-FAILED`, prior public bytes preserved); a deterministic `verified-projection` projects the verifier outcome coherently into BOTH workflow and manifest (run/gate/coverage/resolved-evidence/artifact-hash) — an LLM still cannot originate `verified`, only the deterministic verifier result drives the projection. Rebased+composed over #111/#115: promotion gate order preserved (caller → integrity → trace → screenshot → freeze → atomic); `verified-projection` carries all new artifact kinds (`trace`/`trace-sanitization-report`/`screenshot`/`screenshot-privacy-report`/`screenshot-capture-runtime`/`playwright-config`). reviewer-hy3 APPROVE (no blocking; non-blocking follow-ups noted: denominator derivation, M0 synthesized gate-result, orchestrator runtime-evidence asymmetry). Full local `pnpm test` 91 files / 758 tests; CI run `31359038661` `success`. No VERSION bump (consistent with #111/#115 — deferred to the M1-EXIT release). `docs/_slice-notes/M1-BUNDLE-INTEGRITY.md` folded in and deleted. |
| 2026-08-10 | **#108 (M2-SERVICE-WORKERS) Service Worker containment DONE (PR #113, CI `success`).** Crawlee discovery sets `serviceWorkers: 'block'` (keeping `useIncognitoPages: false`), closing the gap where a registered SW could take over network requests and hide cross-origin/non-safe requests from `page.route()` (bypassing `ARXIC-SURFACE-001`/`-008` without a diagnostic). Real-Chromium proof: an allowed hostile worker reaches both same-origin-POST and cross-origin sinks; with the fix, discovery reaches neither and emits both blocked diagnostics. Rebased over merged #111/#115/#112. The previously-retained raw trace AND the supplementary screenshot were both removed (raw traces never retained; the screenshot would need #115 attestation and the proof is the diagnostic assertions). CI run `31362554647` `success`. `docs/_slice-notes/M2-SERVICE-WORKERS.md` folded in and deleted. |
| 2026-08-10 | **#114 (DEPENDABOT) remediation DONE (PR #120, CI `success` under Node 22.22.0).** Upgraded Nodemailer 7→9.0.5, Next 15.5→16.3.0 (PostCSS 8.5.23 + sharp 0.35.3), Testcontainers 11.14→12.1.0 (Dockerode 4→5, **uuid absent**); Node floor ≥22.22, CI pinned 22.22.0; no pnpm overrides. Migrations validated against official upstream sources: Next-16 ESLint flat config + `next lint` removed + middleware→`proxy.ts`; rate-limit counters made process-global (`globalThis`) to survive Turbopack module-dedup across fixture resets (per-window limit property preserved). Real-Docker Testcontainers/Mailpit smoke (Mailpit vars unset, random ports) + real sharp image-optimization proof (resized PNG). Resolutions verified via `pnpm why`. Dependabot alert remeasurement **pending GitHub re-scan** (vulnerable versions gone from the merged graph — not instant). |
| 2026-08-10 | **#109 (M1-EXIT-23-12) §23.12 reproof DONE + #27 M1-EXIT PASSES (PR #121, CI `success`).** `promotion-real-world.test.ts` drives BOTH fixture apps (incl. Express) through compile → two-pass real-Chromium verify → coherent projection (#112) → promote B1 → blocked subsequent promotion (`ARXIC-PROMOTION-ATOMIC-REPLACE-FAILED`, no receipt) → independent `readFile(publicPath) === promotedBytes`. Adapted to the safe pipeline: action-owned `masked-page` screenshot policy (#115), accepted trace/sanitization artifacts (#111), #112 coherence. Full §23 remeasurement: **reference 14/14 · vulnerable 14/14 MET** — §23.12 now MET for both; §23.3/§23.4 Express accepted MET (generic/vacuous, same standard as the 2026-08-08 #110 baseline); §23.13 SBOM tracked as a post-M1 packaging follow-up (CI-produced, not yet in the bundle). VERSION 0.0.0 → 0.2.0 (M1-EXIT per RELEASES.md). Carried non-blocking follow-ups: §23.13 SBOM-in-bundle; Dependabot re-scan; human visual inspection of retained screenshots (#115 residual); `plan`↔`plan.md` binding gap. `docs/_slice-notes/M1-EXIT-23-12.md` folded in and deleted. |
| 2026-08-10 | **#103 (M2-WORKER-CLI) honest groundwork landed (PR #122, CI `success`; #103 stays OPEN).** `arxic run` accepts `--executor local\|worker` (local default); a CLI-owned `WorkerRunExecutor implements RunExecutor` drives the existing `WorkerClient` with fail-closed startup/stream/approval/cancel/cleanup/protocol classification (`ARXIC-EXEC-WORKER-*`, prose-safe) and writes the same failed-run ADR §20.1 schema, honestly blocking with `ARXIC-EXEC-WORKER-PROTOCOL` rather than faking a completed run. Real Docker proves selection + deterministic cleanup; #26 isolation proofs (non-root, read-only source, default-deny egress, no socket, quotas) intact. **Full stages-0–12 sandbox execution remains blocked** on a Node-22 pipeline worker image (workspace/native/browser/`git`/`sg`), writable artifact + internal-network peer transport, and a structured pipeline-result protocol payload (RunState/checkpoints/artifacts/receipt) — a larger M2 build. Groundwork merges as an M2 increment; the previously-invisible deferral is now visible. |
| 2026-08-10 | **#116 ADR-004 (Proposed) intent-backed pseudocode design landed (PR #123, CI `success`; #116 stays OPEN for implementation).** ADR-004 (`docs/adr/004-intent-backed-pseudocode.md`) decides `IntentSpec` = (b) unfrozen app-local proposal layer (future `@arxic/intent`) — NOT a frozen contract, NOT a Workflow extension; oracle provenance `domain-rule\|repository-specification\|human-approved` (acceptance) vs `observed-only` (characterization only) — source-derived behavior can't launder into an acceptance oracle; owners/dependency direction (`@arxic/intent` Service + orchestrator Action); the 8-step flow mapped to stages/packages; Gherkin (optional/pinned/digest+lineage only, never compiled into Workflow), locator (semantic + stable execution locator resolve to same element), sensitivity (mutation-style probe) boundaries — all clean-room. Full implementation + two-app proof are a tracked M2 build; the ADR authorizes the design direction only. |
| 2026-08-10 | **#116 slice A — `@arxic/intent` Service mechanics landed (PR pending, CI pending; #116 stays OPEN).** First implementation increment ADR-004 §8 authorizes. New private package `@arxic/intent`: `IntentSpec`/`OracleSpec`/`IntentLineage` types (unfrozen, app-local — no contracts change, no JSON Schema) + Service capability blocks (`validateOracle`, `resolveAssertionKind`, `resolveAssertion`, `normalizeIntentSpec`, `buildIntentSpec`, `canonicalizeIntentSpec`, `detectStaleness`) — composable, structured returns, no run-level classification (that is slice B's orchestrator job). Settles the ADR-004 open questions for this slice: assertion `kind` is DERIVED from oracle kinds, never input (source-as-acceptance is structurally impossible); acceptance requires ≥1 independent oracle **and** non-empty source+runtime evidence (ADR §2 — `ARXIC-INTENT-SOURCE-ACCEPTANCE`); divergent acceptance outcomes on the same id → `contradicted` (`ARXIC-INTENT-ORACLE-CONFLICT`); duplicate ids / stale lineage / malformed oracles / non-64-lowercase-hex digests → `blocked` (`ARXIC-INTENT-INVALID`/`-STALE`); hostile extra keys (incl. `truthState:"verified"`) stripped by closed-world reconstruction (B1); `canonicalJson` rejects Date/Map/Set/BigInt instead of silently colliding (B2). Depends only on `@arxic/contracts`; inlined `canonicalJson`. Real-data proof drives the real `authCandidates` pack over both apps' real captured `authSurface` — reference `url:/` + Express `text:Logged in` both normalize to acceptance via a pack-rule identity oracle (digest = sha256 of the identity triple, NOT the observation — no laundering); observed-only → characterization; the two apps' divergent outcomes on the same id → contradicted. Live-browser proof deferred to slice F. **Independent review (reviewer-hy3): REQUEST CHANGES → APPROVE** — six empirically-reproduced blocking findings fixed (verified-key leak, canonical collision, zero-evidence acceptance, tautological loop-close, circular oracle, lost diagnostics); targeted confirm re-verified each fix on disk, no new hole. Gates green (typecheck/lint/format/test 98 files / 807 tests; 34 in `@arxic/intent`). ADR-004 stays **Proposed** — maintainer acceptance is a separate call. |

---

_**Slice completion ritual is MANDATORY (charter §8).** When you finish ANY slice: run the gates, prove real-world (non-synthetic), then sync ALL docs — flip the tracker here, move 🔖 RESUME HERE, add a session-log line, staleness-sweep (`rg` the slice id + TODOs), link + close the GitHub issue, and commit + **push to `main`**. Unsaved = undone. Stale docs are a defect._
