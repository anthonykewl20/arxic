# M1-15 retained exploration evidence

`exploration-trace.zip` was migrated through the final shared projector from
the prior sanitized current-tree artifact; the original raw capture is not
retained. Its adjacent sidecar passes independent inspection, and the ZIP
contains only `trace-001.trace` with three retained, remapped actions. The
pinned Trace Viewer load is exercised by
`packages/verifier/src/real-world.test.ts`.

The two named route screenshots predate the trace-sanitization slice and are
separate visual evidence; trace inspection makes no claim about screenshot
pixels. The current-tree trace is sanitized, while historical raw commits still
require an authorized pre-public history purge.
