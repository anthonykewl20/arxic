# Run artifact curation

Committed: run.json, config.json, diagnostics.jsonl, intents.json, plan.md,
playwright.config.ts, stages/*, artifacts/{00,03..13}.json, fixtures/*,
tests/*, ../promoted/directus-dg12-run19.bundle.json (+ directory form).

Run context: campaign round 19 (post-#325). Outcome verified, completed,
PROMOTED. #306 AC-4 PROOF: stage-8 decisions contain ZERO
ARXIC-EXPLORATION-TRANSITIONS-UNOBSERVED records (runs 14/17/18 each carried
exactly four false ones) while all four drive steps logged successful
observations — observation-time binding + trailing-slash normalization
field-confirmed on the real target. The shared retention root
(verification-artifacts/) holds run-19 bytes; stages/*.json carry this run's
shas.
