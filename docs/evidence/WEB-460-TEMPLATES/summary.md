# Literal template discovery and inventory identities — #460

Implementation in progress; exact-head installed CI is pending. Source/browser proof before the documentation rebase used base `ec26b186` with dirty implementation. The implementation is now `c4651c5c` on merged main `0785fcb7`; fresh regression passed: 14 tests / 5 files in 110.61 s, including the existing full workbench journey and installed selection contract. TypeScript/lint passed and the license gate rejected zero packages. No human release inspection or production-readiness verdict is claimed.

The real committed Express reference source and its running page corroborate thirteen literal EJS controls: four forms, five inputs and four buttons. Each declaration retains its exact source commit, SHA-256 and line range, with hypothesized truth. No EJS code executes during analysis. Attribute names are emitted, never values. HTML/HTM and default EJS delimiters are supported; custom delimiters, Vue/Svelte, arbitrary generated markup, runtime states and business semantics are outside this proof. Template code, malformed HTML/EJS, scripts, inert templates, foreign markup, unknown event attributes and parser budgets remain explicit gaps.

Initial template tests failed before implementation. The real dashboard then exposed seven duplicate source IDs from nested same-line conditional expressions. The filter received the correct kind but React retained seven stale condition rows: twenty displayed instead of thirteen. An independent identity assertion failed with 82 unique IDs for 89 declarations. Stable occurrence disambiguation preserves every declaration while making IDs unique. Existing noncolliding IDs remain unchanged. Historical stored inventories are immutable; rediscover to obtain corrected identities. A speculative dropdown callback change failed to help and was removed.

All eight targeted source/browser cases across three files passed in 13.27 seconds after the identity fix, including the unchanged thirteen-row assertion. The retained Chromium journey checks source hashes against the actual running reference page, desktop 1440 and mobile 390, first/last declaration navigation and explicit coverage gaps. Agent image inspection includes the original failed filter capture and the corrected mobile declarations. Screenshots have adjacent masking provenance and a sanitized action timeline; no raw traces are retained.

The existing Express tests now expect `template-expressions-not-evaluated` instead of `unsupported-framework`; the coverage gap is preserved, not waived. The installed-selection contract fails before adding the seventeenth file. The same browser journey is required in default installed acceptance and the exhaustive Firefox/WebKit partitions. Final installed execution, wider themes/personas/states, historical inventory migration and full #402 remain outstanding.

The same real source/reference-page/dashboard journey passes in Firefox (1 case, 13.09 s) and WebKit (1 case, 11.65 s). Fresh Chromium plus existing workbench regression passed 14 cases / 5 files in 110.61 s. Agent inspection also covered Firefox desktop and Chromium/WebKit final mobile rows; all new final dashboard audits report zero violations, zero overflow and passing explicit checks. This is light-theme desktop/mobile evidence, not the full product matrix.

CI follow-up: run 34098206322 failed the reporter interruption test because its child completed before inspection. All four installed Firefox/WebKit partitions passed, but the run remains failed. [Retained reproduction and correction](ci-reporter/summary.md) keeps the no-result assertion and deadlines unchanged. Fresh exact-head CI is required.
