# 0.1.1-HARDENING-P3 — staged doc updates (charter §10.2)

Issue: none (review-residual hardening batch) · PR: pending · Disposition: mixed

## 1. `docs/SYNC.md` — tracker row (replace the existing row verbatim)

```
No tracker row exists for this review-residual batch. Add: 0.1.1 hardening P3 — verifier precedence, result-tar rejection, worker-image trigger coverage, canonical payload ordering guard, and locator-policy test readiness hardening.
```

## 2. `docs/SYNC.md` — session-log row (append to the table)

```
| 2026-08-15 | **0.1.1-HARDENING-P3 review-residual hardening.** Canonical verification now gives fail-closed gates precedence over flaky/app-defect evidence; result-tar validation rejects setuid/setgid members and padded GNU L paths; worker-image detection covers every directly consumed workspace input; the legacy locale ordering payload has an ASCII-key guard. The Playwright locator-policy test now gives static Chromium controls a bounded 3-second readiness window, so its deliberate execution-ambiguity condition is not pre-empted by transient semantic attachment timeout. Real Docker and Chromium suites exercised the affected paths. Dispositions: blocked and contradicted. |
```

## 3. `CHANGELOG.md` — entry under `## [Unreleased]` → `### fixed`

```
- 0.1.1-HARDENING-P3: prioritize verifier safety gates over runtime contradiction evidence; reject privileged result-tar modes; share unsafe-extraction wording; broaden worker-image source triggering; pin the ASCII-key invariant for legacy canonical worker payload ordering; and harden the Playwright locator-policy test readiness budget against Chromium startup contention.
```

## 4. `VERSION` bump required?

yes → 0.1.2, because verifier dispositions and accepted worker-result archives are externally observable hardening corrections.

## 5. Evidence pointers

- Real-world proof: `packages/verifier/src/real-world.test.ts` — real Chromium runs against both reference apps exercise verifier consumers.
- Real-world proof: `packages/environment/src/__tests__/worker-result-volume.real-world.test.ts` — real Docker result-volume import rejects unsafe archive inputs before host extraction.
- Worker-image trigger inputs: `apps/worker/Dockerfile:39-46` copies the workspace and installs/builds the worker; `apps/worker/build-and-verify.sh:35-73` resolves `@arxic/contracts`, loads Tree-sitter from `packages/source-ua-adapter`, and launches Chromium through `packages/playwright-agent-adapter`; `apps/worker/build-and-verify.sh:48-54` loads `better-sqlite3` from `test-fixtures/vulnerable-auth-app`.
- Locator-policy stability proof: `packages/playwright-agent-adapter/src/__tests__/exploration-driver.test.ts` — five post-fix standalone real-Chromium runs passed; the static unique semantic locator now reliably reaches the intended ambiguous execution locator.
- Artifacts: no new retained browser artifacts; existing real-world suites retain only their sanctioned sanitized evidence.
- Gates: typecheck PASS · lint PASS · targeted verifier/environment/m0-pipeline/orchestrator-langgraph/auth-domain-pack/worker PASS · full test PASS (125 files, 1077 tests) · format PASS.

## 6. Sad paths proved (each mapped to a truth state, charter §4)

| Trigger                                                         | Expected disposition                                                  | Test                                                                         |
| --------------------------------------------------------------- | --------------------------------------------------------------------- | ---------------------------------------------------------------------------- |
| Mixed passing/failing runs also record forbidden network errors | blocked; network diagnostic has precedence over flaky evidence        | `packages/verifier/src/index.test.ts`                                        |
| Partial or zero run set                                         | blocked; required-run gate has precedence over runtime classification | `packages/verifier/src/index.test.ts`                                        |
| Tar member has setuid or setgid mode                            | rejected before extraction                                            | `packages/environment/src/__tests__/tar-stream-validation.test.ts`           |
| GNU L record carries bytes after `<path>\0`                     | rejected before extraction                                            | `packages/environment/src/__tests__/tar-stream-validation.test.ts`           |
| Static semantic control is delayed during Chromium startup      | test waits boundedly, then reaches the intended execution ambiguity   | `packages/playwright-agent-adapter/src/__tests__/exploration-driver.test.ts` |
