# M2-PLAN-BINDING — staged doc updates (charter §10.2)

Issue: unfiled follow-up · PR: integrator-owned · Disposition: mixed

## 1. `docs/SYNC.md` — tracker row (replace the existing row verbatim)

No existing tracker row; add the carried follow-up as completed:

```
| unfiled | [M2-PLAN-BINDING] Bind staged plan text to a declared plan artifact | ☑ done |
```

## 2. `docs/SYNC.md` — session-log row (append to the table)

```
| 2026-08-12 | **M2-PLAN-BINDING plan/artifact coherence DONE.** Bundle promotion now hashes the UTF-8 staged plan and blocks promotion when it differs from a declared `plan.md` or `kind: plan` artifact, while producers with no plan artifact remain compatible. Real Chromium promotion proofs against both reference apps preserve prior bytes after binding rejection; the real-filesystem suite separately preserves prior bytes after atomic-replace failure. Dedicated `ARXIC-PROMOTION-PLAN-BINDING-FAILED` diagnostics loop-close through the frozen validator. |
```

## 3. `CHANGELOG.md` — entry under `## [Unreleased]` → `### Fixed`

```
- M2-PLAN-BINDING: blocked promotion of a staged bundle whose plan text is desynchronized from its declared plan artifact, without requiring plan artifacts from producers that do not emit one; refs §23.12.
```

## 4. `VERSION` bump required?

no — integrity hardening within the current pre-release milestone; the integrator can reassess the release bump.

## 5. Evidence pointers

- Real-world proof: `packages/bundle-promoter/src/__tests__/promotion-real-world.test.ts` — real Chromium verifies bundles against both reference apps, then a desynchronized plan is blocked and prior public bytes remain byte-identical.
- Atomic-replace proof: `packages/bundle-promoter/src/__tests__/real-world.test.ts` — real staged bytes and filesystem LKG failure preserve prior bytes with a coherent/no-plan-artifact bundle.
- Artifacts: existing sanitized verifier artifacts generated by the real-world suite when `ARXIC_EVIDENCE_DIR` is supplied; no new retained UI artifact in this local run.
- Gates: typecheck ☑ · lint ☑ · format ☑ · test (104 files / 898 tests passing) ☑ · license gate (782 packages / 0 rejected) ☑

## 6. Sad paths proved (each mapped to a truth state, charter §4)

| Trigger                                                                 | Expected disposition                                       | Test                                                                          |
| ----------------------------------------------------------------------- | ---------------------------------------------------------- | ----------------------------------------------------------------------------- |
| A declared `plan.md`/plan artifact hash no longer matches `bundle.plan` | `blocked`; no public replacement                           | `sad-paths.test.ts` and both real-app cases in `promotion-real-world.test.ts` |
| Atomic replacement fails after a coherent candidate reaches storage     | `blocked`; prior public bytes preserved                    | `real-world.test.ts` (also the focused LKG sad path)                          |
| An m0-style bundle has plan text but no plan artifact                   | accepted by validation/promotion; no truth-state elevation | `sad-paths.test.ts`                                                           |
