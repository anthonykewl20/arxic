# WEB-402-DRIFT-REBIND — staged doc updates (charter §10.2)

Issue: #402 · Disposition: observed implementation (local green ×2; CI on the PR head is the remaining gate — an LLM cannot assign `verified`).

## 1. `docs/SYNC.md` — tracker row (replace the existing row verbatim)

Supersedes the WEB-402-DRIFT-REVALIDATE row if it is still pending fold.

```text
| #402 | Web product release: comprehensive frontend intent campaigns and AI visual auditing | In progress; drift rebind — a drifted recurring campaign re-binds onto the new source instead of dying: the pre-fire guard enqueues a fresh real discovery and the campaign is remapped onto the new commit by content-derived inventoryRowId identity, per-row executions are re-established and the cron slot re-arms; stop is only the fallback (selection exhausted, discovery failed, rebinding impossible) |
```

## 2. `docs/SYNC.md` — session-log row (append to the table)

```text
| 2026-09-08 | **#402 (WEB-402-DRIFT-REBIND) drifted recurring campaigns re-bind by row identity; stop is the fallback.** Phase (b) of the per-fire source-drift follow-up on top of #483's guard: a drifted DUE recurring campaign no longer stops by default — `guardDueCampaigns` starts a REBIND (`campaign.rebinding: { discoveryRunId }`, additive JSON field, no migration; campaign id unchanged): the slot is disarmed (`nextFireAt → null`), an audited `campaign.rebind-started` is written and a fresh REAL discovery run is enqueued (guard skips campaigns already rebinding, so no second discovery; no fire can happen while `rebinding` is set). `drain()` gained a completion hook run after EVERY recorded run: `processRebinds()` finds each campaign whose rebind discovery finished and remaps it onto the new commit — survivors (selected inventoryRowIds still present in the fresh inventory; ids are content-derived and stable across re-scans) re-acquire per-row 'agent' runs with `workflowScope { campaignId, inventoryRowId, sourceCommit: NEW }` and `rows[].runId` set (the fire path only executes rows carrying a runId), `discoveryRunId`/`sourceCommit` move to the new pin, `rows` are replaced with the fresh inventory rows, `rebinding` is cleared, the cron slot re-arms with the same `nextSlot(cron)` computation tick() uses, and `campaign.rebound` (+ one `campaign.rebound-row-dropped` per dropped selection, compound subject `campaignId/rowId` after the `baseline.approved` precedent — the audit schema has no detail column) is audited. Fallbacks stop the campaign: selection exhausted on the new inventory (`campaign.rebind-exhausted`), failed/cancelled/inventory-less discovery (`campaign.rebind-failed`), still-dirty source at rebind time (a dirty tree cannot be pinned — firing on it would doom every run to the 409 backstop), or rebinding impossible at guard time (no execution settings / queue cannot take the discovery → the #483 `stopDriftedSchedules` path is kept). Capacity follows queueCampaign's whole-campaign rule: when the survivors cannot fit the 20-active cap the rebind DEFERS (no partial remaps) and retries on the next drain completion. #483's journey is RE-SPECIFIED, not loosened: the drifted-commit journey now asserts rebind-started/rebinding-set/zero-doomed-runs (zero-doomed-runs stays load-bearing, sharpened to "nothing executes against the drifted pin — the rebind's own survivor run on the NEW commit is the only new row-scoped execution"); the dirty-tree journey now ends in the honest rebind-failed stop; the clean-source positive control is unchanged. Real-world proof: `campaign-drift-rebind.real-world.test.ts` — real Workbench, real reference-auth-app checkout, real discovery, real yearly-cron campaign, REAL `git` drift between fires (comment-append commit keeping the route; file-removal commits dropping one/both selected routes; a real workspace-vanish discovery failure), asserting rebind → remap → re-arm → fire on the new commit with zero 409 refusals, exhausted/partial/failure fallbacks, and stable campaign identity. Red first (all 4 journeys: `rebinding` undefined — the guard stopped as in phase (a); a first red also caught a test-side stale-`nextFireAt` read, fixed before green). Regressions: campaign-drift-revalidate 3/3, campaign-recurrence 3/3, campaign-row-history 1/1, campaign-ui 1/1, inventory-ledger-ui 1/1, campaigns unit 7/7, workspace 5/5. Gates local: apps/web typecheck ☑ · repo eslint ☑ · format ☑ (full repo after note); CI on the PR head is the remaining gate. Known gaps (recorded, not hidden): audit has no survivor/dropped count column (counts are proven via campaign rows + per-dropped-row audits); a rebind deferred at full queue capacity is not retried if the queue drains exclusively through `cancel()` (drain completions are the retry trigger); the UI does not yet surface the rebinding state (the campaign panel renders the disarmed schedule; a panel view is owed); guard enqueues one discovery per drifted campaign (no cross-campaign discovery sharing). |
```

## 3. `CHANGELOG.md` — entry under `## [Unreleased]` → `### added`

```text
- WEB-402-DRIFT-REBIND drift rebind for recurring campaigns (#402): a drifted DUE recurring campaign re-binds instead of dying — the pre-fire guard disarms the slot, enqueues a fresh real discovery and, on completion, remaps the campaign onto the new commit by content-derived inventoryRowId identity (survivors re-acquire per-row executions, the cron slot re-arms, fires continue on the new source, campaign id unchanged); stop is the fallback (selection exhausted, failed discovery, unpinnable dirty source, rebinding impossible), with `campaign.rebind-started`/`rebound`/`rebound-row-dropped`/`rebind-exhausted`/`rebind-failed` audits; proven against the real fixture repo with real git drift between fires.
```

## 4. `VERSION` bump required?

No — the fold integrator owns `VERSION`; the capability rides the current `0.0.301` line.

## 5. Evidence pointers

- Real-world proof: `apps/web/src/__tests__/campaign-drift-rebind.real-world.test.ts` — real discovery over the real reference-auth-app fixture, real yearly-cron campaign, real `git` drift between fires (append-commit / route-file-removal / workspace-vanish), real discovery + agent engines; no browser/server, `ARXIC_MAILPIT_*` unset, no hardcoded ports.
- Artifacts: `docs/evidence/WEB-402-DRIFT-REBIND/red.txt` (first red: `rebinding?.discoveryRunId` undefined in all 4 tests — the guard stopped as in phase (a)) + `green.txt` (4/4, repeat green).
- Gates: typecheck ☑ · lint ☑ · format ☑ · test (slice 4/4 ×2, siblings 21/21) ☑ · license gate → CI.

## 6. Sad paths proved (each mapped to a truth state, charter §4)

| Trigger                                                                        | Expected disposition                                                                                                    | Test                                                                                      |
| ------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- |
| HEAD moved past the pin; all selected rows survive the fresh inventory         | Rebind: discovery enqueued, slot disarmed, zero doomed runs, then remap + re-arm + fire on the NEW commit, zero 409s    | rebind journey test 1 (real comment-append commit)                                        |
| Drift removes the only selected route                                          | Rebind starts, discovery completes, selection exhausted → campaign stops (`campaign.rebind-exhausted`), zero fires      | rebind journey test 2 (real file-removal commit)                                          |
| Drift removes one of two selected routes                                       | Partial rebind: survivor remapped and fires, dropped row audited (`campaign.rebound-row-dropped`), campaign stays alive | rebind journey test 3 (real file-removal commit)                                          |
| Rebind discovery fails (real engine failure: workspace vanishes before launch) | Campaign stops (`campaign.rebind-failed`), no fires                                                                     | rebind journey test 4 (real blocked discovery run)                                        |
| Dirty tree at rebind completion                                                | Unpinnable source → stop (`campaign.rebind-failed`); firing would doom runs to the 409 backstop                         | campaign-drift-revalidate dirty-tree journey (re-specified)                               |
| No execution settings / queue cannot take the discovery at guard time          | Phase (a) fallback: `stopDriftedSchedules` stop, `campaign.schedule-drift-stopped`                                      | guard `startRebindOrStop` fallback branch (code path, not journey-covered — gap recorded) |
| Fire while rebinding (tick at the due slot)                                    | Nothing fires: `nextFireAt` is null and the guard will not re-arm until the rebind settles                              | both drift journeys (zero-doomed-runs stays load-bearing)                                 |
| Guard re-run while a rebind is in flight                                       | Skipped — no second discovery enqueued                                                                                  | guard `campaign.rebinding` skip branch                                                    |
| Queue at capacity when the rebind completes                                    | Whole-campaign defer: `rebinding` stays set, remap retried on the next drain completion (never partial)                 | `processRebinds` capacity branch (code path, not journey-covered — gap recorded)          |
| Campaign id / rows identity across a rebind                                    | id unchanged; only survivors carry `runId` (rows without runId never fire)                                              | rebind journeys 1 and 3 (`rebound.id` + rows assertions)                                  |
| Residual race: drift lands after the guard, before/during a fire               | `drain()` 409 `sourceDriftRefusal` + `stopDriftedSchedules` backstop retained unchanged                                 | existing campaign-recurrence drift journey stays green                                    |
