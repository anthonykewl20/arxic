# 0.1.1-RELEASE-MATRIX — staged doc updates (charter §10.2)

Issues: refs #183 · refs #190 · PR: pending · Disposition: mixed (the deterministic worker verifier produced `verified`; remote matrix and worker-image CI evidence remain pending).

## 1. `docs/SYNC.md` — tracker rows (append under Milestone 0.1.1)

```
| #183 | [1.0/verify] Confirm worker-backed full pipeline completeness (stages 0-12 in sandbox) | ☑ done |
| #190 | [1.0] Multi-Node + multi-OS release test matrix | ☑ done |
```

## 2. `docs/SYNC.md` — session-log row (append to the table)

```
| 2026-08-15 | **#183 + #190 (0.1.1-RELEASE-MATRIX) worker-pipeline proof and release matrix shipped.** A real-Docker worker sandbox run completed stages 0–12 against the vulnerable reference fixture, reached deterministic `verified`, and promoted a bundle while re-proving non-root, read-only-rootfs, no-socket, internal-network, and denied-egress invariants. The non-required release matrix schedules Node 22/24 on Ubuntu, macOS, and Windows; every cell installs native dependencies and Chromium, then runs lint/typecheck/build/provenance/packed-CLI smoke, while the Docker+Mailpit core suite is explicitly Ubuntu-only. Windows/macOS results are UNVERIFIED pending the first Actions run, and Windows packed-CLI smoke awaits the Lane E2 cross-platform tarball-smoke fix. |
```

## 3. `CHANGELOG.md` — entries under `## [Unreleased]`

```
### internal
- 0.1.1-RELEASE-MATRIX: add a non-required Node 22/24 × Ubuntu/macOS/Windows release-test matrix that proves frozen install, native/browser installation, CLI build, version provenance, and fresh packed-CLI execution; Docker+Mailpit core tests remain explicitly Ubuntu-only (refs #190).
- 0.1.1-RELEASE-MATRIX: record a fresh real-Docker worker sandbox proof that the stages 0–12 pipeline reaches a verified, promoted bundle and preserves the worker isolation invariants (refs #183).
```

## 4. `VERSION` bump required?

No — release verification and CI coverage do not change the published CLI behavior.

## 5. Evidence pointers

- Real-world proof: `apps/cli/src/__tests__/worker-real-world.test.ts` ran locally with Docker 29.7.2: 2/2 tests passed in 20.97 s (23.19 s total). The blocked unreachable-target case did not promote; the successful vulnerable-auth-app + real Chromium run completed stages 0–12, returned `verified`, and produced a promotion receipt/bundle.
- Isolation evidence asserted by the successful run: read-only rootfs, non-root user, `CAP_DROP=ALL`, no Docker socket bind, internal network, denied metadata-endpoint egress, source-hash tamper rejection, and a hostile source `README.injection.md` treated as data.
- CI coverage: the existing required `ci` job already runs `pnpm test`, which includes this suite on Ubuntu. The non-required path-filtered `worker-image` job runs the strict image suite; no duplicate `ci.yml` line is needed for this verification-only slice. Remote CI has not yet run for this branch.
- Matrix: `.github/workflows/release-test.yml` expands to Node 22/24 × Ubuntu/macOS/Windows. Windows and macOS legs are UNVERIFIED until the first Actions run. Chromium installation is explicit on every cell. Docker+Mailpit `pnpm test` runs only on Ubuntu; macOS/Windows retain all installability/buildability/provenance/tarball gates rather than treating the Docker suite as passed. The Windows packed-CLI smoke step depends on the Lane E2 cross-platform `scripts/tarball-smoke.mjs` fix landing in the publish-quickstart lane.
- Node pin disclosure: matrix `22` resolves to the latest 22.x (at present at or above the `>=22.22` engines floor); required `ci.yml` pins 22.22.0 exactly. This accepted drift provides matrix breadth while retaining the exact baseline gate.
- Publish dependency hand-off (P3): `release.yml` currently runs in parallel with `release-test.yml` for tag pushes, because separate GitHub Actions workflows cannot express a cross-workflow `needs`. The release-workflow owner is on record to consume the matrix result before relying on it for publishing.
- Suggested landing-agent issue comment for refs #183: `Local real-Docker proof passed: 2/2 worker e2e tests (20.97 s test time) exercised the sandboxed stages 0–12 path against vulnerable-auth-app and real Chromium. The success case returned verified and promoted a bundle; the unreachable-target sad path remained blocked without a receipt. The run asserted read-only rootfs, a non-root user, CAP_DROP=ALL, no Docker socket, an internal network, denied egress, source-hash tamper rejection, and source injection treated as data. Required ci already executes pnpm test on Ubuntu; worker-image CI remains the strict image check.`
- Gates: typecheck passed · lint passed · format passed · targeted worker e2e (2 passing) passed.

## 6. Sad paths proved (each mapped to a truth state, charter §4)

| Trigger                                                            | Expected disposition                                                  | Test                                                                                 |
| ------------------------------------------------------------------ | --------------------------------------------------------------------- | ------------------------------------------------------------------------------------ |
| Worker target is unreachable on its internal network               | `blocked`, with no promotion receipt                                  | `worker-real-world.test.ts` unreachable-target case (passed)                         |
| Worker result source hash is altered after collection              | fail-closed source rejection                                          | `worker-real-world.test.ts` result-normalization tamper assertion (passed)           |
| Source contains a hostile instruction file                         | source remains content-as-data; verified pipeline result is unchanged | `worker-real-world.test.ts` committed `README.injection.md` real-Docker run (passed) |
| Windows/macOS cannot provide the Docker+Mailpit test prerequisites | suite is explicitly Ubuntu-scoped; other release gates still execute  | `release-test.yml` documented conditional core-suite steps                           |
