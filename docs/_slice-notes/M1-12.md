# M1-12 — staged doc updates (charter §10.2)

Issue: #26 · PR: #93 · Disposition: blocked (all sad paths) / mixed (clean run completes)

## 1. `docs/SYNC.md` — tracker row (replace the existing row verbatim)

```
| #26 | [M1-12] Worker (ephemeral isolated) | ☑ done |
```

## 2. `docs/SYNC.md` — session-log row (append to the table)

```
| 2026-08-07 (6) | **#26 (M1-12) Worker (ephemeral isolated worker) DONE.** `@arxic/environment` gains the isolation mechanics (service): `docker-cli.ts` (structured Docker CLI wrapper — array args, no shell), `worker-sandbox.ts` (`createWorkerSandbox` — internal Docker network, non-root `--user 1000:1000`, `--read-only` rootfs, `--tmpfs /work`, read-only source bind, `--memory/--memory-swap/--pids-limit/--cpus` quotas, `--security-opt no-new-privileges`, `--cap-drop ALL`, **no socket / no daemon env / no privileged / no host network**; `assertSafeSpec` rejects unsafe specs before any Docker call; idempotent `stop()` records `ARXIC-WORKER-CLEANUP-FAILED`), and `worker-diagnostics.ts` (7 stable `ARXIC-WORKER-*` codes + validating factory, loop-closed). `@arxic/worker` gains the action layer: `worker-policy.ts` (`validateWorkerSecurity` fail-closed on socket / daemon env / privileged / host-net / root / host-creds (`/etc/shadow`, `/etc/passwd`, `~/.ssh`, `id_rsa`) / dangerous caps / writable-source; `freezePolicy` immutable `RunPolicy`; `ingestContentAsData` content-is-data) and `worker-client.ts` (`createLocalWorkerClient` implementing the #84 `WorkerClient` seam; `classifySandboxState`/`classifyExecResult` map outcomes to truth states — OOM→`ARXIC-WORKER-QUOTA-EXCEEDED`, timeout→`ARXIC-WORKER-TIMEOUT`, generic exit→`ARXIC-WORKER-RUN-FAILED`, `ARXIC-WORKER-ISOLATION-VIOLATED` reserved for creation/escape; `stream()` ingests source content as data and **always tears down** the sandbox on completion). Real-world adversarial proof (real Docker, image `node:20-alpine`, unique `arxic-*` names, `afterAll` cleanup, zero leaked resources): cross-job source + host-path reads DENIED (`No such file`); egress to the host bridge gateway and `169.254.169.254` DENIED (`Network unreachable`) while a declared sibling is reachable; memory-quota breach → real cgroup OOM (`OOMKilled=true`, supervisor kill 137) → `blocked`/`ARXIC-WORKER-QUOTA-EXCEEDED`; non-root (`id -u`=1000), `/etc` AND `/work/source` writes DENIED, source readable, no docker socket / `DOCKER_*` env / `docker` CLI in the worker; cross-job network isolation (worker on job B's net cannot reach job A's sibling); live prompt-injection payload in source → `ARXIC-WORKER-INJECTION-NEUTRALIZED` with the frozen policy returned by reference unchanged and the run completing. CI-found host/permission gap fixed: source dirs chmod 0755 + files 0644 so the uid-1000 container reads source on the CI runner (whose uid ≠ 1000, unlike the dev host); read-only-write assertion loosened to exit-code + a busybox/GNU substring. Two independent reviewers (reviewer-hy3 + reviewer-deepseek) reviewed the diff; all findings addressed (mislabelled non-zero exit; clean-run sandbox leak; wired `freezePolicy`/`ingestContentAsData` into the live path; dead-code `ARXIC-WORKER-EGRESS-DENIED` + `dockerRun` removed; `ARXIC-WORKER-TIMEOUT` wired). Composing the orchestrator pipeline (stages 0–12) inside the sandbox is deferred to CLI/orchestrator wiring. **M1 13/15.** Next: #43 + M1-EXIT. |
```

## 3. `CHANGELOG.md` — entry under `## [Unreleased]` → `### added`

```
- M1-12 Worker (#26): `@arxic/environment` gains the isolation service blocks (`docker-cli.ts`, `worker-sandbox.ts`, `worker-diagnostics.ts`) and `@arxic/worker` gains the action layer (`worker-policy.ts`, `worker-client.ts`) for an ephemeral, non-root, quota-enforced, default-deny-egress worker that hosts an isolated run. `createWorkerSandbox()` (service) launches a Docker container on an `--internal` network with `--user 1000:1000`, `--read-only` rootfs, `--tmpfs /work`, a read-only source bind, memory/swap/pids/cpus quotas, `no-new-privileges`, `cap-drop ALL`, and no socket / daemon env / privileged / host-network access; `assertSafeSpec` rejects unsafe specs before any Docker call, and `stop()` is idempotent and records `ARXIC-WORKER-CLEANUP-FAILED`. `validateWorkerSecurity` (action) fail-closes operator config that would weaken isolation (socket, daemon env, privileged, host-net, root, host credentials, dangerous capabilities, writable source); `freezePolicy` captures an immutable `RunPolicy` and `ingestContentAsData` treats all ingested content as data, recording `ARXIC-WORKER-INJECTION-NEUTRALIZED` without ever mutating the policy. `createLocalWorkerClient` implements the #84 `WorkerClient` seam (`start`/`stream`/`inspect`/`approve`/`cancel`); `stream()` ingests source text as data and deterministically tears down the sandbox on completion; outcome classification maps quota/timeout/run/exit failures to `blocked` with stable `ARXIC-WORKER-*` diagnostics. Real-world adversarial proof against real Docker proves cross-job source/host-path reads and outbound egress (host gateway + cloud metadata) are denied, a memory-quota breach terminates the worker, the sandbox enforces non-root + read-only rootfs/source + tmpfs + no daemon access, cross-job networks are isolated, and a live injection payload is neutralized with the policy unchanged. Frozen §10 contracts and the LangGraph topology are unchanged; `run-spec.ts` types stay app-local.
```

## 4. `VERSION` bump required?

No. Internal worker/isolation infrastructure; not user-observable. The milestone-exit bump (0.2.0 = M1-EXIT #27) covers M1.

## 5. Evidence pointers

- Real-world proof: `packages/environment/src/__tests__/worker-sandbox.real-world.test.ts` and `apps/worker/src/__tests__/worker-client.test.ts` — real Docker daemon, image `node:20-alpine`; real internal-network egress attempts to the host bridge gateway + `169.254.169.254`; real cgroup OOM (`OOMKilled=true`, exit 137); real cross-job isolation + cross-job network isolation; real non-root/read-only/tmpfs/no-daemon probes; live injection neutralization from a source file. Verified zero leaked containers/networks after the suite.
- Independent review: two reviewers (reviewer-hy3 + reviewer-deepseek) over the diff (REQUEST CHANGES → all findings addressed → focused re-review RESOLVED); Docker `--internal` default-deny validated against official Docker networking docs + empirical probes.
- Gates: typecheck ☑ · lint ☑ · format ☑ · test (413 passing) ☑ · license gate ☑ (0 rejected).
- CI: `gh pr checks 93` green against base `468841a` (origin/main).

## 6. Sad paths proved (each mapped to a truth state, charter §4)

| Trigger                                                                                                 | Expected disposition                                                             | Test                                                                                                                                       |
| ------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| job A reads job B's source / host path                                                                  | blocked (`No such file`)                                                         | `worker-sandbox.real-world.test.ts` "denies cross-job source and host-path reads"                                                          |
| outbound egress to host gateway / `169.254.169.254`                                                     | blocked (`Network unreachable`/timeout)                                          | `worker-sandbox.real-world.test.ts` "allows a declared sibling but denies host-gateway and metadata egress"                                |
| memory quota exceeded                                                                                   | blocked (`ARXIC-WORKER-QUOTA-EXCEEDED`, OOMKilled/137)                           | `worker-sandbox.real-world.test.ts` "terminates a memory quota breach"; `worker-client.test.ts` "classifies a real OOM-terminated sandbox" |
| wall-clock / exec timeout                                                                               | blocked (`ARXIC-WORKER-TIMEOUT`)                                                 | `worker-client.test.ts` classifyExecResult timeout case                                                                                    |
| worker exits non-zero / keepalive dies                                                                  | blocked (`ARXIC-WORKER-RUN-FAILED`)                                              | `worker-client.test.ts` "worker failure classification"                                                                                    |
| prompt-injection payload in source                                                                      | run completes, policy immutable (`ARXIC-WORKER-INJECTION-NEUTRALIZED`, observed) | `worker-policy.test.ts` neutralization; `worker-client.test.ts` "neutralizes a real injection payload read from source"                    |
| docker socket / daemon env / privileged / host-net / root / host-creds / caps / writable-source in spec | blocked (`ARXIC-WORKER-CONFIG-UNSAFE`), no Docker resources created              | `worker-policy.test.ts`; `worker-client.test.ts` "blocks an unsafe spec before any Docker resource is created"                             |
| unsafe sandbox spec (root user, non-`arxic` network, daemon env, source tmpfs, non-positive quota)      | rejected before Docker (construction-enforced)                                   | `worker-sandbox.real-world.test.ts` "worker sandbox construction is enforced safe"                                                         |
| cleanup after success/cancel/stop-failure                                                               | deterministic teardown, idempotent; failure → `ARXIC-WORKER-CLEANUP-FAILED`      | `worker-sandbox.real-world.test.ts` "removes the worker and network, and stop is idempotent"                                               |
