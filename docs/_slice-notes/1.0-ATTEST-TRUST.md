# 1.0-ATTEST-TRUST — staged doc updates (charter §10.2)

Issue: #177 · PR: pending · Disposition: blocked / observed

## 1. `docs/SYNC.md` — tracker row (replace the existing row verbatim)

```
| #177 | [1.0-ATTEST-TRUST] Attestation trust hardening | ☑ done |
```

## 2. `docs/SYNC.md` — session-log row (append to the table)

```
| 2026-08-13 | **#177 (1.0-ATTEST-TRUST) Attestation trust hardening DONE.** Non-local targets now require an HMAC receipt, independently issued nonce, independently discovered build digest, and operator-owned origin allowlist; target-served origins are advisory. Real HTTP handshakes against both reference apps preserved the local-test path as observed; unsigned, forged, replayed, tampered, self-listed, missing-policy, and lineage-mismatch inputs are blocked. |
```

## 3. `CHANGELOG.md` — entry under `## [Unreleased]` → `### security`

```
- 1.0-ATTEST-TRUST Attestation trust hardening (#177): require signed non-local receipts and independent nonce, build-digest lineage, and operator origin policy while preserving real local-test reference-app handshakes.
```

## 4. `VERSION` bump required?

yes → integrator selects the 1.0 release version because this security policy is user-observable.

## 5. Evidence pointers

- Real-world proof: `packages/environment/src/__tests__/real-world.test.ts` — real Node HTTP handshake against the booted Next.js reference app and Express vulnerable app; both local-test attestations passed.
- Artifacts: no browser screenshots, traces, or UI artifacts apply; the proof is an HTTP attestation handshake.
- Gates: typecheck ☑ · lint ☑ · format ☑ · test (fixture suites and 33 worker tests passing) ☑ · license gate (806 packages, 0 rejected) ☑

## 6. Sad paths proved (each mapped to a truth state, charter §4)

| Trigger                                                        | Expected disposition         | Test                                                                                    |
| -------------------------------------------------------------- | ---------------------------- | --------------------------------------------------------------------------------------- |
| Unsigned or forged non-local receipt                           | blocked                      | `packages/environment/src/__tests__/sad-paths.test.ts`                                  |
| Target self-lists a foreign origin absent from operator policy | blocked                      | `packages/environment/src/__tests__/sad-paths.test.ts`                                  |
| Attested build digest differs from discovered/staged digest    | blocked                      | `packages/environment/src/__tests__/sad-paths.test.ts`                                  |
| Receipt is tampered or replayed under a stale nonce            | blocked                      | `packages/environment/src/__tests__/sad-paths.test.ts`                                  |
| Non-local operator allowlist, key, nonce, or digest is absent  | blocked                      | `packages/environment/src/__tests__/sad-paths.test.ts` and `attestation-policy.test.ts` |
| Real local-test reference app supplies an unsigned receipt     | observed / allowed preflight | `packages/environment/src/__tests__/real-world.test.ts`                                 |
