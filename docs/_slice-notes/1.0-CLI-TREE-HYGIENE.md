# 1.0-CLI-TREE-HYGIENE — staged doc updates (charter §10.2)

Issues: #187, #210 · PR: pending · Disposition: mixed

## 1. `docs/SYNC.md` — tracker row (replace the existing row verbatim)

```
| #187 | [1.0-CLI-TREE-HYGIENE] CLI default run output does not dirty the source tree | ☑ done |
| #210 | [1.0-CLI-TREE-HYGIENE] Exclude local Vitest auth SQLite artifacts from the CLI real-world source copy | ☑ done |
```

## 2. `docs/SYNC.md` — session-log row (append to the table)

```
| 2026-08-14 | **#187/#210 (1.0-CLI-TREE-HYGIENE) CLI run-output and test-source hygiene DONE.** Default CLI output now goes to `ARXIC_STATE_DIR/runs/<sha256(resolved-repository-directory)[:16]>`, or `~/.arxic/runs/<key>` when unset, leaving the source tree clean while preserving same-repository/run-id checkpoints across invocations; a moved repository intentionally receives a new root. The real Chromium/reference-app pipeline promoted two sequential default-output runs. The real-world copy now excludes only `.vitest-auth.db*` and `auth.db*`, and its setup proves stale oversized host artifacts do not enter scanning without writing tracked fixtures. Source scanning detects genuinely dirty bytes; ast-grep blocks their disposition with `ARXIC-RULES-DIRTY-TREE`. Deterministic verifier outcome: `verified`; source dirty disposition: `blocked`. |
```

## 3. `CHANGELOG.md` — entry under `## [Unreleased]` → `### fixed`

```
- 1.0-CLI-TREE-HYGIENE (#187, #210): moved implicit CLI run records to a stable, per-repository state root outside the source repository and excluded documented local Vitest auth SQLite artifacts from the real-world source-copy harness, preventing self-created dirty-tree blocks, lost checkpoint resume, and host-state oversize scan failures.
```

## 4. `VERSION` bump required?

yes → patch version, because the default CLI output location is user-observable bug-fix behavior.

## 5. Evidence pointers

- Real-world proof: `apps/cli/src/__tests__/real-world.test.ts` — real Next.js reference app, Crawlee, Tree-sitter, ast-grep, and Chromium; two sequential default-output runs each deterministically produced a verified bundle while the copied source repository remained clean.
- Resume proof: `apps/cli/src/__tests__/run.test.ts` — two `runAction` invocations with the same run id use a real `FileStageCheckpointer`; the second load finds the prior stage and emits `ARXIC-EXEC-RESUMED` from the test executor's actual checkpoint branch.
- Artifacts: sanitized run artifacts use the stable state root; tests override `ARXIC_STATE_DIR` to an ephemeral temporary base. Raw trace archives are not retained or attached.
- Gates: typecheck ☑ · lint ☑ · format ☑ · full test suite ☑ · license gate ☑

## 6. Sad paths proved (each mapped to a truth state, charter §4)

| Trigger                                                                     | Expected disposition                                                                                   | Test                                                                                                                                       |
| --------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------ |
| A second default-output invocation with the same run id                     | Existing stage checkpoint is loaded and the test executor emits `ARXIC-EXEC-RESUMED`; no silent redo   | `apps/cli/src/__tests__/run.test.ts` — `reuses a stable out-of-tree default root to resume the same run id`                                |
| Source repository `.gitignore` contains `.arxic/` or contains no such entry | Default output is outside the repository; both runs succeed and `git status --porcelain` remains empty | `apps/cli/src/__tests__/run.test.ts` — parameterized default-output repository test                                                        |
| A genuinely modified source file                                            | `blocked` dirty-tree disposition; source references are withheld                                       | `packages/ast-grep-adapter/src/__tests__/sad-paths.test.ts` — `emits no source references for a dirty git tree` (`ARXIC-RULES-DIRTY-TREE`) |
| Host contains oversized `.vitest-auth.db-wal` or `auth.db-wal`              | Neither file is copied or enumerated; the real run remains deterministically `verified`                | `apps/cli/src/__tests__/real-world.test.ts` — stale host artifacts created in setup and filtered by `committedFixtureCopy`                 |
