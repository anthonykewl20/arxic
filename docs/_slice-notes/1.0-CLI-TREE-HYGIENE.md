# 1.0-CLI-TREE-HYGIENE — staged doc updates (charter §10.2)

Issues: #187, #210 · PR: pending · Disposition: mixed

## 1. `docs/SYNC.md` — tracker row (replace the existing row verbatim)

```
| #187 | [1.0-CLI-TREE-HYGIENE] CLI default run output does not dirty the source tree | ☑ done |
| #210 | [1.0-CLI-TREE-HYGIENE] Exclude local Vitest auth SQLite artifacts from the CLI real-world source copy | ☑ done |
```

## 2. `docs/SYNC.md` — session-log row (append to the table)

```
| 2026-08-14 | **#187/#210 (1.0-CLI-TREE-HYGIENE) CLI run-output and test-source hygiene DONE.** Default CLI output now goes to the system temporary directory, leaving the source tree clean without relying on its `.gitignore`; the real Chromium/reference-app pipeline promoted two sequential default-output runs. The real-world copy now excludes only `.vitest-auth.db*` and `auth.db*`, and its setup proves stale oversized host artifacts do not enter scanning. Source scanning still classifies a genuinely dirty source path as `blocked`. Deterministic verifier outcome: `verified`; source dirty disposition: `blocked`. |
```

## 3. `CHANGELOG.md` — entry under `## [Unreleased]` → `### fixed`

```
- 1.0-CLI-TREE-HYGIENE (#187, #210): moved implicit CLI run records outside the source repository and excluded documented local Vitest auth SQLite artifacts from the real-world source-copy harness, preventing self-created dirty-tree blocks and host-state oversize scan failures.
```

## 4. `VERSION` bump required?

yes → patch version, because the default CLI output location is user-observable bug-fix behavior.

## 5. Evidence pointers

- Real-world proof: `apps/cli/src/__tests__/real-world.test.ts` — real Next.js reference app, Crawlee, Tree-sitter, ast-grep, and Chromium; two sequential default-output runs each deterministically produced a verified bundle while the copied source repository remained clean.
- Artifacts: ephemeral sanitized run artifacts are produced under the system temporary run directory by the test; raw trace archives are not retained or attached.
- Gates: typecheck ☑ · lint ☑ · format ☑ · test (975 passing) ☑ · license gate ☑

## 6. Sad paths proved (each mapped to a truth state, charter §4)

| Trigger                                                                     | Expected disposition                                                                                   | Test                                                                                                                                     |
| --------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------- |
| A second default-output run after a first run                               | Deterministic verifier `verified`; no CLI-created dirty-tree block                                     | `apps/cli/src/__tests__/real-world.test.ts` — `promotes two consecutive default-output runs against the real reference repository`       |
| Source repository `.gitignore` contains `.arxic/` or contains no such entry | Default output is outside the repository; both runs succeed and `git status --porcelain` remains empty | `apps/cli/src/__tests__/run.test.ts` — parameterized default-output repository test                                                      |
| A genuinely modified source file                                            | `blocked` dirty provenance; committed evidence is not manufactured                                     | `packages/source-ua-adapter/src/__tests__/sad-paths.test.ts` — `manifests dirty bytes without manufacturing committed source provenance` |
| Host contains oversized `.vitest-auth.db-wal` or `auth.db-wal`              | Neither file is copied or enumerated; the real run remains deterministically `verified`                | `apps/cli/src/__tests__/real-world.test.ts` — stale host artifacts created in setup and filtered by `committedFixtureCopy`               |
