# WEB-402-PERSONA-VARIANTS — staged doc updates (charter §10.2)

Issue: #402 · Disposition: observed implementation (local green — real fan-out/durability/validation/unset-secret/rebind-guard journeys against a real reference-auth-app checkout, plus a real Chromium dialog journey across a real workbench→server handoff; CI on the PR head is the remaining gate — an LLM cannot assign `verified`).

## 1. `docs/SYNC.md` — tracker row (replace the existing row verbatim)

No dedicated `| #402 |` row exists in the current milestone tracker (status lives in the RESUME paragraph). Staged status text for the integrator to fold there:

```text
| #402 | Web product release: comprehensive frontend intent campaigns and AI visual auditing | In progress; persona execution variants (slice 1) now run on ONE-SHOT campaigns: `Campaign.variants` (additive JSON, persona kind only, ≤4, slug keys, ARXIC_SECRET_ refs stored as names only) fans each selected row out into a default run plus one agent run per variant carrying `workflowScope.variantKey`; `variantEnvironment` merges the variant persona credentials over the launch env (never serialized), unresolved variants/unset secrets block the run honestly, recurring × variants and rebind × variants are refused as slice-1 fallbacks |
```

## 2. `docs/SYNC.md` — session-log row (append to the table)

```text
| 2026-09-09 | **#402 (WEB-402-PERSONA-VARIANTS) persona execution variants for one-shot campaigns.** Slice 1 of the #402 variants design: a campaign may now declare up to 4 persona variants (`Campaign.variants`, additive JSON, no migration, no new endpoints — kind `persona` only, keys `/^[a-z0-9-]+$/` unique, credentials stored as `ARXIC_SECRET_` ref NAMES, never values). `queueCampaign` validates each refusal as a distinct 400 before anything is enqueued (duplicate keys, >4 variants, bad key charset, non-persona kind, non-`ARXIC_SECRET_` credential refs, and cron × variants → "Variant campaigns run once; recurring variants are not implemented yet"); the whole-campaign capacity reservation becomes rows × (1 + variants) against the 20-active cap; the fan-out enqueues the DEFAULT run first (exactly the pre-variants shape) then one agent run per variant in declared order with `workflowScope.variantKey` set, inside the existing transaction, recording per-row `runIds` plus the existing default `runId`. `drain()` resolves agent-run environments through the new pure exported `variantEnvironment(run, campaign, settings, env)` in workbench.ts: default runs get the untouched `executionEnvironment` result; variant runs get the variant's `ARXIC_INPUT_PERSONA_EMAIL/PASSWORD` merged over it via the same `secretEnvironment` resolution execution.ts already uses — values are read from process env at launch only and never enter project/run JSON. An unresolvable campaign/variant or an unset secret throws into the existing blocked-run outcome with a clear message ("Campaign variant could not be resolved: …; refusing to run as the default persona" / "A selected secret reference is not available on this server") — never a silent default-persona run. Rebind fallback (slice 1b deferral): `startRebindOrStop` (and defensively `rebindCampaign`) refuse a campaign with non-empty `variants` into the existing `stopRebind(campaign, 'campaign.rebind-failed')` path BEFORE any rebind discovery is enqueued. UI: the workflow-selection form gains a variants editor (add/remove persona variants, label + email/password ARXIC_SECRET_ ref inputs; the client derives the variant key as a deterministic slug of the label and the server's 400s surface through the existing error alert), and the campaign detail attributes each variant run's outcome next to the default's on a `data-variant-outcome="{key}:{state}"` element (optional fields narrowed into locals before JSX, #482/#489). Real-world proof (red first, both artifacts in docs/evidence/WEB-402-PERSONA-VARIANTS): `persona-variants.real-world.test.ts` — real Workbench.open harness on a real reference-auth-app checkout: fan-out journey asserts exactly 3 agent runs for 1 row × 2 variants with declared order `[undefined, 'persona-a', 'persona-b']`, per-row `runIds`, `campaign.runIds`, then REOPENS the workbench over the same state directory and asserts rows/runIds/scope survive; validation matrix journey drives every distinct 400 through the real API path with zero runs enqueued, plus the 429 fan-out capacity refusal (18 real filler queue entries); unset-secret journey (AC4) asserts the variant run ends blocked on the secret message while the default and the other variant's runs end blocked at the model stage with a DISTINCT summary; rebind-guard journey gives a variant campaign a real git drift and a (hand-injected, since recurring × variants is refused at enqueue) due cron slot, asserting `campaign.rebind-failed`, `nextFireAt` null, no `rebinding`, and no discovery enqueued. `persona-variants.test.ts` unit-proves `variantEnvironment`: unmerged base for default runs, correct merged `ARXIC_INPUT_PERSONA_*` for variant runs, values confined to env overrides (run JSON carries only ref names), unresolvable-variant and unset-secret refusals. `persona-variants-ui.real-world.test.ts` — real Chromium + real server (port 0): creates a variant campaign through the REAL dialog (two persona variants, label + refs), exercises the server-400 path for a label that slugs to an empty key through the existing error alert, then asserts per-variant outcome attribution (`[data-variant-outcome]` ×2, each settling to a terminal state) with zero page errors. AC2 boundary (recorded, not faked): distinct-authenticated-identity proof through a live engine requires paid inference — NOT in this slice; the credential path is proven by the pure `variantEnvironment` unit plus the real enqueue→drain launch path ending in the recorded blocked outcome. Gates local: apps/web typecheck ☑ · repo eslint ☑ · format ☑ (full worktree, after this note). Regressions: campaign-drift-rebind 4/4, campaign-drift-revalidate 3/3, campaign-ui 1/1, workspace 5/5, campaigns unit 10/10. Known gaps (slice 1b): recurring (cron) × variants refused at enqueue; rebind-by-variant identity falls back to `campaign.rebind-failed`; variant kinds beyond `persona` (flag/state) unimplemented; distinct-login-path personas unimplemented; distinct-authenticated-identity AC2 proof deferred (paid inference). |
```

## 3. `CHANGELOG.md` — entry under `## [Unreleased]` → `### added`

```text
- WEB-402-PERSONA-VARIANTS persona execution variants (#402): one-shot campaigns can declare up to 4 persona variants (additive JSON, ARXIC_SECRET_ ref names only); each selected row fans out into a default agent run plus one per variant carrying `workflowScope.variantKey`, capacity reserves rows × (1 + variants) against the 20-active cap, `variantEnvironment` merges the variant persona credentials into the launch env at drain time (values never serialized; unresolved variants or unset secrets block the run instead of falling back to the default persona), recurring × variants and rebind × variants are refused as honest slice-1 fallbacks, and the workflow-selection dialog gains a variants editor with the campaign detail attributing per-variant outcomes on `data-variant-outcome` elements; proven with real fan-out/durability/validation/unset-secret/rebind-guard journeys and a real Chromium dialog journey.
```

## 4. `VERSION` bump required?

No — the fold integrator owns `VERSION`; the capability rides the current line.

## 5. Evidence pointers

- Real-world proof: `apps/web/src/__tests__/persona-variants.real-world.test.ts` — real Workbench.open, real reference-auth-app checkout, real discovery, real restart durability, real git drift for the rebind guard.
- Real-world proof: `apps/web/src/__tests__/persona-variants-ui.real-world.test.ts` — real Chromium + real server (port 0): variant campaign created through the real dialog; per-variant outcome attribution asserted; zero page errors.
- Unit: `apps/web/src/__tests__/persona-variants.test.ts` — `variantEnvironment` merge/refusal contract.
- Artifacts: `docs/evidence/WEB-402-PERSONA-VARIANTS/red.txt` (pre-implementation failures, verbatim) + `green.txt` (two consecutive post-implementation green runs).
- Gates: typecheck ☑ · lint ☑ · format ☑ (full worktree, after this note) · test (slice suites 10/10 green ×2; regressions 23/23) · license gate → CI.

## 6. Sad paths proved (each mapped to a truth state, charter §4)

| Trigger                                                         | Expected disposition                                                                                                              | Test                                                     |
| --------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------- |
| Duplicate variant keys                                          | 400 'Variant keys must be unique', zero runs enqueued                                                                             | `persona-variants.real-world.test.ts` validation journey |
| 5+ variants                                                     | 400 'A campaign supports at most 4 variants'                                                                                      | same journey                                             |
| Key outside `/^[a-z0-9-]+$/` (incl. UI label slugging to empty) | 400 'Variant keys use lowercase letters, digits and dashes' (UI: existing error alert)                                            | same journey + `persona-variants-ui.real-world.test.ts`  |
| Non-persona kind                                                | 400 'Unsupported variant kind'                                                                                                    | validation journey                                       |
| Credential ref not an `ARXIC_SECRET_` string                    | 400 'Variant credentials must reference ARXIC_SECRET_ environment names'                                                          | validation journey                                       |
| cron × variants                                                 | 400 'Variant campaigns run once; recurring variants are not implemented yet'                                                      | validation journey                                       |
| rows × (1 + variants) over the 20-active cap                    | 429 whole-campaign refusal, nothing enqueued                                                                                      | validation journey (18 real filler entries)              |
| Variant credential unset at drain                               | Variant run ends `blocked` on the secret message; default/other runs keep distinct summaries — never a silent default-persona run | unset-secret journey (AC4)                               |
| Campaign/variant unresolvable at drain                          | `blocked` with 'Campaign variant could not be resolved…'                                                                          | `persona-variants.test.ts` unit                          |
| Drifted variant campaign with a due slot                        | `campaign.rebind-failed` stop, `nextFireAt` null, no rebind discovery enqueued (slice-1 fallback)                                 | rebind-guard journey                                     |
| Server restart between fan-out and inspection                   | rows/`runIds`/`workflowScope.variantKey` survive verbatim over the same state directory                                           | fan-out durability journey (AC1)                         |
| Credential values in serialized records                         | Only ref names reach project/run JSON; values live in launch env overrides only                                                   | `persona-variants.test.ts` serialization unit            |
