# M1-EXIT-23-12 — staged doc updates (charter §10.2)

Issue: #109 · PR: not opened · Disposition: hold — WIP checkpoint only

## 1. `docs/SYNC.md` — tracker row (replace the existing row verbatim)

```text
| #27 | [M1-EXIT] Gate: two apps replay, no app-specific code | ☐ #109 remains pending #111, #112, and #115; regenerate safe evidence before remeasurement |
```

## 2. `docs/SYNC.md` — session-log row (append to the table)

```text
Do not add a completion row yet. This WIP proof must be rebased over #111, #112,
and #115, then regenerated with policy-compliant evidence and rerun through CI.
```

## 3. `CHANGELOG.md` — entry under `## [Unreleased]` → `### internal`

```text
Do not add a changelog entry yet; #109 remains a dependency-blocked WIP checkpoint.
```

## 4. `VERSION` bump required?

no — this closes a test-observation gap and does not change user-observable behavior; the M1-EXIT version decision remains with the integrator.

## 5. Evidence pointers

- Real-world proof: `packages/bundle-promoter/src/__tests__/promotion-real-world.test.ts` — real Chromium drove both real fixture apps twice, followed by real filesystem promotion and failure.
- Artifacts: `docs/evidence/M1-EXIT-23-12/summary.md` records the provisional run. Unsafe pre-policy screenshots and raw traces were removed pending regeneration through #111 and #115.
- Gates: typecheck ☑ · lint ☑ · format ☑ · test (focused: 2 passing; full: 536 passing) ☑ · license gate (752 packages, 0 rejected) ☑
- Layering: the test composes the real verifier result into a coherent verified staged bundle at the action/orchestration level; `BundlePromoterAdapter` remains the unchanged service capability under test.
- Scope/risk: this proves a failed subsequent promotion through the composed compiler/verifier/promoter seams, exactly as #109 requests; it is not a second full orchestrator run. The promoter's pre-existing validator checks the manifest but does not enforce cross-field workflow/manifest id or status consistency, so this test asserts that consistency before promotion; no product hardening or follow-up issue was authorized in this slice.
- Non-gate attempt: standalone `reference-auth-app` tests did not start because that script requires an externally provisioned Mailpit at `localhost:8025`; the mandated Mailpit variables remained unset and no shared service was created. The focused proof and full 536-test repository suite use isolated fixture runtimes and passed.

## 6. Sad paths proved (each mapped to a truth state, charter §4)

| Trigger                                                                                                                      | Expected disposition                                                                         | Test                                                                                                                                              |
| ---------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| A subsequent promotion cannot replace the existing LKG snapshot because the snapshot destination is deliberately a directory | `blocked` with `ARXIC-PROMOTION-ATOMIC-REPLACE-FAILED`; no receipt and no public replacement | `promotion-real-world.test.ts` runs the same test body for both `FIXTURE_APPS` and compares the public file to independently captured prior bytes |

Red-first/tautology proof: before the green run, the test temporarily wrote the
distinct subsequent candidate bytes over the public path immediately before the
preservation assertion. The focused command failed for both apps at that exact
byte-equality assertion (received candidate bytes versus expected prior bytes). The
sabotage was then removed; no production implementation changed.

### Staleness sweep

Command: `rg -n -i '#109|M1-EXIT-23-12|§23\.12|failed[- ]run[^\n]*preserv|failed runs preserve prior promoted bundle|prior promoted bundle|promotion-real-world' .` plus `rg -n 'M1-EXIT-23-12|#109|TODO|FIXME' .`.

- `docs/SYNC.md:17`, `docs/SYNC.md:106`, and `docs/SYNC.md:187` still describe #109/§23.12 as open or unproven. They are intentionally untouched in this parallel worktree; the integrator must apply sections 1–2 above and remove #109 from the open-work prose while remeasuring #27.
- `CHANGELOG.md:16` still records §23.12 as UNMET and #109 as pending. It is intentionally untouched; the integrator must apply section 3 above.
- `docs/evidence/M1-EXIT/measurement-summary.md:35-38` is the historical 13/14 baseline. A dated supersession note now points to this slice's proof while preserving the baseline.
- `docs/adr/001-arxic-architecture.md:751` is the unchanged acceptance criterion and remains accurate.
- `docs/SYNC.md:191` and `docs/engineering-charter.md:155,160` are generic slice-ritual/`TODO` process instructions, not pending #109 work; they remain accurate.
- `packages/bundle-promoter/README.md` now identifies the generic two-app real-world proof and its action/service layering. Existing `real-world.test.ts` and `sad-paths.test.ts` hits remain accurate lower-level coverage.
- `docs/evidence/M1-EXIT-23-12/`, this note, and `promotion-real-world.test.ts` are the new self-referential hits and agree on the command, artifact paths, and dispositions.
- No relevant `TODO` or `FIXME` was found; no package contract, ADR decision, schema, `VERSION`, or user-observable behavior changed.
