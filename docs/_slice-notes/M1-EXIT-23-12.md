# M1-EXIT-23-12 — staged doc updates (charter §10.2)

Issue: refs #109 · PR: not opened · Disposition: local gates green (full suite 765/765 +
recursive typecheck + root lint + format:check + license gate) and independent review complete;
committed locally, not pushed/merged. `#27` stays open.

This note is the only doc deliverable for this parallel worktree (charter §10.2). It does not
touch `docs/SYNC.md`, `CHANGELOG.md`, or `VERSION`; the integrator folds sections 1–3 in at
merge and owns the final `#27` remeasurement.

## 1. `docs/SYNC.md` — tracker row (replace the existing row verbatim)

```text
| #27 | [M1-EXIT] Gate: two apps replay, no app-specific code | ☐ measured, does not pass: §23.12 reproof regenerated for both apps through the #111/#112/#115 pipeline (#109, local gates green — full suite + license + format — pending CI + integrator remeasurement) |
```

## 2. `docs/SYNC.md` — session-log row (append to the table)

```text
| 2026-08-10 (2) | **#109 (M1-EXIT-23-12) §23.12 reproof REGENERATED (not merged).** `packages/bundle-promoter/src/__tests__/promotion-real-world.test.ts` drives both `FIXTURE_APPS` through compile → two-pass real-Chromium verify → `projectVerifiedBundle` (#112 coherence) → promote B1 → a blocked subsequent promotion (`ARXIC-PROMOTION-ATOMIC-REPLACE-FAILED`, no receipt) → independent reread asserting exact B1 byte identity. Adapted to the merged pipeline: action-owned `masked-page` `ScreenshotPrivacyPolicy` (#115, required by the verifier), accepted `trace` + `trace-sanitization-report` artifacts (#111), and the promoter's integrity/trace/screenshot gate order. Safe evidence retained under `docs/evidence/M1-EXIT-23-12/`. Gates: full suite 765/765 (94 files, 318.40s), recursive typecheck (25/26), root lint, format:check, license gate (782/780/2/0) all green locally; focused §23.12 suite 2/2. `#27` stays open; integrator owns the full remeasurement (CI not yet run on this head). |
```

## 3. `CHANGELOG.md` — entry under `## [Unreleased]` → `### internal`

```text
- M1-EXIT-23-12 failed-run-preserves-prior-bundle reproof (#109): regenerate the two-app compile → two-pass verify → promote → blocked-no-receipt → byte-identity proof through the merged trace-sanitization (#111), bundle-integrity (#112), and screenshot-privacy (#115) pipeline; action-owned `projectVerifiedBundle` + `masked-page` screenshot policy; no app-specific code.
```

## 4. `VERSION` bump required?

no — this closes a real-world test/evidence gap and changes no user-observable behavior; the
M1-EXIT version decision remains with the integrator.

## 5. Evidence pointers

- Real-world proof: `packages/bundle-promoter/src/__tests__/promotion-real-world.test.ts` — one
  `describe.each(FIXTURE_APPS)` body drives both real fixture apps through real Chromium twice,
  real atomic promotion, and a real forced pre-replace filesystem failure.
- Artifacts: `docs/evidence/M1-EXIT-23-12/{reference,vulnerable}-auth-app/verification/run-{1,2}/`
  — masked-page screenshots + `.privacy.json` (#115) and sanitized action timelines +
  `.sanitization.json` (#111). No raw trace or unredacted screenshot is retained. Each app also
  has a `promotion-outcome.json` snapshot of the §23.12 result (promoted vs subsequent checksum,
  the blocked `ARXIC-PROMOTION-ATOMIC-REPLACE-FAILED` diagnostic, clean final listing).
- Full §23 14-criteria × 2-app MET/UNMET/PARTIAL table with proof pointers:
  `docs/evidence/M1-EXIT-23-12/summary.md`.
- Gates actually run this slice: **full suite** (`env -u ARXIC_MAILPIT_SMTP -u ARXIC_MAILPIT_API
pnpm test` → exit 0, 94 files / 765 tests, 318.40s) ☑ · recursive typecheck (`pnpm typecheck &&
pnpm -r typecheck`, 25/26 packages) ☑ · root ESLint (`pnpm lint`) ☑ · `pnpm format` +
  `format:check` (full repo, last line below) ☑ · license gate (`node scripts/license-gate.mjs`:
  782 total / 780 allowed / 2 excepted / 0 rejected) ☑ · focused real-world suite (2 passing, both
  apps) ☑. CI has not run on this head (not pushed/merged); the integrator's CI run is the
  remaining gate.
- Independent review: `reviewer-hy3` ran the suite green and traced every gate; its weakening
  finding (exact artifact counts) and cheap-strengthening findings (receipt, #112 coherence on
  the shipped bytes, failure-message pin to `${publicPath}.lkg`, residue listing) were applied.
  Its informational notes (masked screenshots are low-pixel-content by design; the `plan`↔`plan.md`
  binding gap that lets B2 reach atomic-replace) are recorded in `summary.md`.
- Layering: the test composes the deterministic verifier result into one coherent verified
  staged bundle at the action/orchestration level via `projectVerifiedBundle`; the verifier
  owns the `masked-page` screenshot policy and trace sanitization; `BundlePromoterAdapter`
  remains the unchanged service capability under test, and its gate order
  (caller → integrity `#112` → trace `#111` → screenshot `#115` → freeze → atomic publish) is
  exercised end-to-end by the subsequent candidate that carries the same accepted artifacts.
- Scope/risk: this proves the §23.12 mechanism through the composed compiler/verifier/promoter
  seams for both apps, exactly as #109 requests; it is not a second full orchestrator run. No
  product code changed; no contract, schema, `contracts/**`, or `schemas/**` file was modified.

`pnpm format:check` last line (run after this note was written):

```text
All matched files use Prettier code style!
```

## 6. Sad paths proved (each mapped to a truth state, charter §4)

| Trigger                                                                                                                      | Expected disposition                                                                         | Test                                                                                                                                                 |
| ---------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| A subsequent promotion cannot replace the existing LKG snapshot because the snapshot destination is deliberately a directory | `blocked` with `ARXIC-PROMOTION-ATOMIC-REPLACE-FAILED`; no receipt and no public replacement | `promotion-real-world.test.ts` runs the same body for both `FIXTURE_APPS`; the public file is reread independently and compared to captured B1 bytes |

The load-bearing assertions are `failed.receipt === undefined`, the single
`ARXIC-PROMOTION-ATOMIC-REPLACE-FAILED` diagnostic, and `readFile(publicPath) === promotedBytes`
after the failure. Removing or widening any of them voids the §23.12 proof. (This session adapted
the existing green test to the merged pipeline; no additional red-first sabotage run was performed
this pass — the assertions themselves are the gate.)

### Staleness sweep

Command: `rg -n -i '#109|M1-EXIT-23-12|§23\.12|failed[- ]run[^\n]*preserv|prior promoted bundle|promotion-real-world' docs/ packages/bundle-promoter/README.md` plus `rg -n 'docs/_slice-notes/M1-EXIT\.md' docs/`.

- `docs/SYNC.md:17`, `docs/SYNC.md:106`, and `docs/SYNC.md:187` still describe #109/§23.12 as
  open and `13/14 for Express`. Intentionally untouched in this parallel worktree; the integrator
  applies sections 1–2 above and removes #109 from the open-work prose when remeasuring #27.
- `CHANGELOG.md` still records §23.12 as UNMET/pending. Intentionally untouched; the integrator
  applies section 3 above.
- `docs/evidence/M1-EXIT/measurement-summary.md:35-37` recorded §23.12 UNMET for Express and a
  stale pointer to `docs/_slice-notes/M1-EXIT.md §6` (that slice note was folded in on merge and
  no longer exists). Updated in this slice: §23.12 is now evidenced for both apps here, and the
  pointer redirects to `docs/evidence/M1-EXIT-23-12/summary.md`.
- `docs/adr/001-arxic-architecture.md:751` is the unchanged acceptance criterion and remains
  accurate.
- `packages/bundle-promoter/README.md:3,25` accurately describe the §23.12 mechanism and the
  two-app real-world proof; the rebase conflict that added the #111 trace-gate paragraph was
  resolved by retaining both paragraphs.
- `docs/evidence/M1-EXIT-23-12/` (this summary + retained artifacts), this note, and
  `promotion-real-world.test.ts` are the self-referential hits and agree on the command, artifact
  paths, and dispositions.
- No relevant `TODO`/`FIXME` was found; no contract, schema, `VERSION`, or user-observable
  behavior changed.
