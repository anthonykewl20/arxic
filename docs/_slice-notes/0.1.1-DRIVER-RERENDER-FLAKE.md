# 0.1.1-DRIVER-RERENDER-FLAKE — staged doc updates (charter §10.2)

Issue: unfiled CI stabilization lane · PR: unfiled · Disposition: observed

## 1. `docs/SYNC.md` — tracker row (replace the existing row verbatim)

```
No tracker row: this unfiled CI-stabilization lane has no existing tracker row.
```

## 2. `docs/SYNC.md` — session-log row (append to the table)

```
| 2026-08-15 | **0.1.1-DRIVER-RERENDER-FLAKE CI stabilization.** PlaywrightExplorationDriver now rechecks the identity-checked handle after focus handlers run, and the real-Chromium regression proof makes the replacement occur at that action boundary rather than by a wall-clock timer. Observed: 10/10 focused-file runs and the full test suite passed. |
```

## 3. `CHANGELOG.md` — entry under `## [Unreleased]` → `### fixed`

```
- 0.1.1-DRIVER-RERENDER-FLAKE: fail closed when a focus-time rerender detaches an identity-checked exploration control before its native action.
```

## 4. `VERSION` bump required?

```
no — internal CI reliability correction; no externally observable release behavior was added.
```

## 5. Evidence pointers

- Real-world proof: `packages/playwright-agent-adapter/src/__tests__/exploration-driver.test.ts` — real headless Chromium triggers a focus-time replacement after same-element resolution and proves the driver reports the detached-handle failure instead of filling the replacement.
- Artifacts: no retained artifacts; this focused fail-closed test uses no `evidenceDir`, so it produces no trace or screenshot artifact.
- Gates: focused file 10/10 passing · package suite 45 passing · full test suite 1,010 passing · typecheck passing · lint passing · format passing.
- The previous timer-based test had a false-success window: under contention both locators could resolve the replacement element and the test could incorrectly pass; the deterministic focus-triggered rerender closes it.

## 6. Sad paths proved (each mapped to a truth state, charter §4)

| Trigger                                                                                                | Expected disposition                                                                                                                          | Test                                                                           |
| ------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------ |
| The original control's focus handler replaces it after same-element resolution and before native fill. | blocked: the action reports that the identity-checked execution element is no longer attached; no replacement fill is reported as successful. | `fails closed when a rerender replaces the identity-checked execution element` |

Residual: the focus+isConnected boundary is still a check-then-act point — arbitrary async DOM mutation AFTER the check remains an inherent, accepted TOCTOU boundary of browser automation.
