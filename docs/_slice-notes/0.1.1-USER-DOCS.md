# 0.1.1-USER-DOCS — staged doc updates (charter §10.2)

Issue: #188 · Disposition: mixed

## 1. `docs/SYNC.md` — tracker row (replace the existing row verbatim)

```text
| #188 | [0.1.1] Versioned user docs + API reference; fix stale README | partial — delivered: versioned user-docs landing + parser-exact CLI/config reference + README fixes; deferred: generated API-reference pipeline + docs:build CI gate (see Deferrals) |
```

## 2. `docs/SYNC.md` — session-log row (append to the table)

```text
| 2026-08-15 | **#188 (0.1.1-USER-DOCS) user documentation PARTIAL.** Delivered a versioned 0.1.1 user-doc landing page plus parser-derived CLI and configuration references, and corrected README status/roadmap/repository-map statements. The generated API-reference pipeline and docs build CI gate remain deferred because this docs-only lane did not add build tooling or CI wiring. |
```

## 3. `CHANGELOG.md` — entry under `## [Unreleased]` → `### added`

```text
- 0.1.1-USER-DOCS (#188): add the versioned user-documentation landing page and code-derived CLI/configuration references; correct stale README executor and roadmap status claims.
```

## 4. `VERSION` bump required?

No — these pages document the existing 0.1.1 surface (`VERSION:1`;
`package.json:3`; `apps/cli/package.json:3`).

## 5. Evidence pointers

- Docs delivered: `docs/README.md`, `docs/cli-reference.md`, and
  `docs/configuration.md`; each is stamped `Version: 0.1.1` and the landing page
  defines the snapshot/current-pointer convention.
- CLI parser and help: `apps/cli/src/args.ts:17-77`; `apps/cli/src/cli.ts:19-37,97-98`.
- CLI exits and default state root: `apps/cli/src/cli.ts:25-79`; `apps/cli/src/run.ts:23-40,104-140`.
- CLI environment variables and their activation rules: `apps/cli/src/local-executor.ts:224-250`; `apps/cli/src/run.ts:133-140`.
- Worker selection and lockstep operator setting: `apps/cli/src/cli.ts:82-95`;
  `apps/worker/src/worker-client.ts:101-117`;
  `docs/operator/worker-deploy.md:22-53`.
- Run-record files and fields: `apps/cli/src/run-directory.ts:16-81`.
- Configuration schema and defaults: `apps/cli/src/config/validate.ts:12-218,244-356`;
  `apps/worker/src/run-spec.ts:48-88`.
- README status, quickstart, and roadmap edits: `README.md:6,12,21-24,49-57,71-77`;
  current executor behavior is `apps/cli/src/cli.ts:82-95` and worker result
  handling is `apps/cli/src/worker-executor.ts:48-230`.
- Gates: lint ☐ · format ☐. Full test suite intentionally not required for docs-only files.

## 6. Sad paths proved (each mapped to a truth state, charter §4)

| Trigger                                                                                 | Expected disposition                            | Test / source evidence                                            |
| --------------------------------------------------------------------------------------- | ----------------------------------------------- | ----------------------------------------------------------------- |
| Unknown command, unknown option, missing `--config`, invalid executor, or unsafe run ID | `blocked`, exit 2 before a run directory exists | `apps/cli/src/args.ts:17-91`; `apps/cli/src/cli.ts:25-30`         |
| Missing, unreadable, malformed, or invalid YAML configuration                           | `blocked`, exit 2 before a run directory exists | `apps/cli/src/config/parse.ts:10-52`; `apps/cli/src/run.ts:23-33` |
| A run is not `verified`                                                                 | Run record is written when writable; exit 1     | `apps/cli/src/run.ts:104-120`                                     |
| Worker transport/protocol/source-integrity failure                                      | `blocked`                                       | `apps/cli/src/worker-executor.ts:129-230,315-360`                 |

Deferrals: the issue also requests a generated API reference and a CI
`docs:build` gate. Neither exists in the baseline, and this lane was explicitly
restricted to `docs/**`, `README.md`, and no code changes; those code/CI changes
remain unimplemented. No existing user guide owned by another slice,
`docs/SYNC.md`, `CHANGELOG.md`, `VERSION`, or `RELEASES.md` was edited.
