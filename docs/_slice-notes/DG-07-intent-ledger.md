# DG-07-intent-ledger — staged doc updates (charter §10.2)

Issue: #251 · PR: (opened by the orchestrator after review) · Disposition: verified (real local Chromium lane + real Docker worker lane; CI pending on the PR)

> Charter §10: `docs/SYNC.md`, `CHANGELOG.md`, and `VERSION` are folded in by
> the integrator at merge — this note carries the staged entries.

## 1. `docs/SYNC.md` — tracker row (replace the existing row verbatim)

```
| #251 | [DG-07] Intent ledger as first-class bundle artifact + `arxic intents` | ☑ done |
```

## 2. `docs/SYNC.md` — session-log row (append to the table)

```
| 2026-08-19 | **#251 (DG-07) intent ledger DONE.** The ADR-008 Decision 1 product artifact exists: `packages/intent/src/ledger.ts` builds a DETERMINISTIC intent ledger over stage artifacts 13→04→09→10 (zero model/network calls; byte-stable modulo `generatedAt`; run-volatile `estimatedCostUsd`/`dedupe`/request ids excluded), validated by the new closed 2020-12 schema `schemas/intent-ledger/intent-ledger.schema.json` (`arxic-intent-ledger-v1`), staged at every run root as `intents.json` (D-3), shipped hash-covered in BOTH lanes (local: bundle-root file under `manifest.fileHashes` + `checksums.sha256` via `assembleBundle`; worker: ledger content embedded inside the frozen `promoted/RUNID.bundle.json`, covered by the receipt `checksumSha256` — no manifest schema change, D-1/D-2), and rendered read-only by the new `arxic intents PATH [--json]` command (run dir either lane layout or assembled bundle dir; SP-2/SP-3/SP-5 refusals; zero writes). 100% inventory join (C-7 — rows without proposals keep their disposition); truth state `verified` derivable ONLY from the deterministic stage-10 verifier artifact (a proposal claiming it fails the build closed, ADR-001 §2); replay status per D-4 (`attempted:*` only on the promoted candidate). C-6 redaction wiring is machine-enforced: `scanTextForSecrets` over the exact ledger bytes at build time (a planted bearer token blocks staging with `ARXIC-PROMOTION-REDACTION_FAILED` and writes nothing) + `scanBundleForSensitiveData` wired INTO `assembleBundle` (findings throw + remove the partial output dir). Proven by the extended real-world suites: local (real reference-auth-app + stub model + real Chromium + Mailpit Testcontainer) asserts bundle-root coverage, 100% join, zero findings, render-with-zero-writes, and the G-5 two-run determinism byte-compare; worker (real Docker) asserts the ledger inside the frozen bundle bytes + checksumSha256 coverage + render from the imported nested layout. **Next: DG-08 follow-ups / #255.** |
```

## 2b. `docs/SYNC.md` — session-log row for the REMEDIATION round (append after the row above)

```
| 2026-08-19 | **#251 (DG-07) review remediation.** Two convergent reviews (security + final-gate) returned FIX-THEN-SHIP; three findings fixed. **P1 (security):** the worker lane staged its ledger with `skipIfPresent`, so a schema-valid FORGED `intents.json` planted at a caller-controlled run root (`arxic run --out ... --run-id ...`) was kept verbatim — no rebuild, no redaction scan, fabricated `verified` truth states and secret strings riding the promoted frozen bundle (bypassing C-5/C-6a). The worker lane now ALWAYS rebuilds from the imported stage artifacts and scans the rebuilt bytes (local verify callback mirrored); defense in depth, `stageIntentLedger`'s skipIfPresent branch now also scans the KEPT bytes and fails closed on findings. Regression: `worker-executor.test.ts` plants a schema-valid forged ledger (fabricated `verified` + real-scanner-visible bearer token) and drives the real worker-lane execute path — the derived rebuild replaces it byte-exactly (modulo `generatedAt`), so `withEmbeddedLedger` cannot embed forged content. **P2:** `isSourceRefs` now requires `typeof extractor === 'string'` — a 13.json sourceRef missing `extractor` (required by EvidenceRefSource) returns the structured `ARXIC-INTENT-LEDGER-INPUT_INVALID` instead of a raw TypeError from `consumerRowId`. **P3:** `arxic intents` renderTable replaces C0/C1 control chars (incl. `\n` `\r` ESC, Unicode category `\p{Cc}`) with spaces in model-derived fields (surface, domain, intent) BEFORE pad/truncate — newline/ANSI injection can no longer break the one-line-per-row table layout. Out-of-surface review suggestions (token-shape patterns in `redaction-gate.ts`, pre-existing absolute-path fileHashes note) recorded as FINDINGs on #251 for a follow-up slice. |
```

## 3. `CHANGELOG.md` — entry under `## [Unreleased]` → `### added`

```
- DG-07 intent ledger (#251): the intent ledger is a first-class, hash-covered bundle artifact and ships a read-only `arxic intents <path> [--json]` command. A deterministic builder (`packages/intent`, zero model/network calls) joins the stage-13 Domain Inventory with stage-04 proposals, the stage-09 compiled candidate, and the stage-10 verifier output into `intents.json` (closed 2020-12 schema `arxic-intent-ledger-v1`), written at every run root, staged before freeze in both lanes (local: bundle-root file under `manifest.fileHashes`+`checksums.sha256`; worker: embedded inside the frozen single-file bundle, covered by the receipt checksum), and rendered by the new command over run dirs (either lane layout) or assembled bundle dirs with stable `ARXIC-INTENT-LEDGER-*` refusals. 100% inventory join with honest dispositions; `verified` only ever derived from deterministic verifier output; replay status per ADR-008 D-4; redaction machine-enforced at build time and at assembly (`scanBundleForSensitiveData` now runs inside `assembleBundle`).
```

## 4. VERSION bump required?

no — new user-facing command surface is additive tooling on the 0.1.x lane; versioning stays with the owner's release decision per RELEASES.md (integrator to confirm at fold-in)

## 5. Evidence pointers

- Real-world proof (local lane): `apps/cli/src/__tests__/real-world.test.ts` — real reference-auth-app on an ephemeral port + real Chromium + real Mailpit Testcontainer + local stub model; the promoted bundle ships bundle-root `intents.json` under `manifest.fileHashes` + `checksums.sha256`; 100% join against `artifacts/13.json`; `scanTextForSecrets`/`scanBundleForSensitiveData` zero findings; `arxic intents` renders run dir + assembled bundle dir with zero writes; G-5 determinism: two consecutive runs' ledgers byte-identical after `generatedAt` normalization.
- Real-world proof (worker lane): `apps/cli/src/__tests__/worker-real-world.test.ts` — real Docker worker sandbox; ledger content inside the frozen `promoted/RUNID.bundle.json` (top-level `intentsLedger` + `intent-ledger` artifact ref + `fileHashes` entry); receipt `checksumSha256` == sha256(frozen bytes); `arxic intents` renders from the imported run dir via the nested `artifacts/checkpoints/<RUNID>/artifacts/13.json` layout; no assembled directory in this lane.
- Unit suites: `packages/intent/src/__tests__/ledger.test.ts` (schema mutation rejection AC-1; builder join + C-7; C-5 verified-only-from-verifier; SP-1 dangling evidence; per-lane resolution + SP-2; SP-4 scan-gated write; determinism modulo generatedAt), `apps/cli/src/__tests__/intents.test.ts` (parser + render + SP-2/SP-3/SP-5 + SP-4 with the REAL scanner), `packages/bundle-promoter/src/__tests__/assembly.test.ts` (bundle-root staging + checksums + C-6b sweep block).
- Artifacts: ledger at each run root (`intents.json`); promoted bundle paths recorded by the receipt in `run.json`.
- Gates: typecheck ✓ · lint ✓ · format ✓ (re-run after this note was written) · test (full suite incl. both real-world lanes) ✓ · license gate ✓ (no new external dependencies)

## 6. Sad paths proved (each mapped to a truth state, charter §4)

| Trigger                                                                                      | Expected disposition                                                                                               | Test                                                                                                                               |
| -------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------- |
| Proposal cites an EvidenceRef unresolvable through the stage-13 projection (SP-1, C-4)       | build fails closed `ARXIC-INTENT-LEDGER-EVIDENCE-UNRESOLVED`; no `intents.json`; promotion blocked                 | `ledger.test.ts`, `intents.test.ts`                                                                                                |
| `arxic intents` on a dir with no resolvable `artifacts/13.json` (SP-2, C-1)                  | exit non-zero `ARXIC-INTENT-LEDGER-INVENTORY-MISSING`; no partial table                                            | `intents.test.ts`, `ledger.test.ts` (ambiguous nested layout also fails closed)                                                    |
| Ledger JSON schema-invalid or unknown `schemaVersion` (SP-3, AC-1)                           | refuse to render: `ARXIC-INTENT-LEDGER-SCHEMA-INVALID` / `ARXIC-INTENT-LEDGER-VERSION-UNKNOWN`; no partial output  | `ledger.test.ts`, `intents.test.ts`                                                                                                |
| Planted bearer token in a proposal string (SP-4, C-6)                                        | staging + assembly blocked `ARXIC-PROMOTION-REDACTION-FAILED`; secret never ships; partial bundle dir removed      | `ledger.test.ts`, `intents.test.ts`, `assembly.test.ts`                                                                            |
| Garbage `PATH` (nonexistent dir, plain file, empty string) (SP-5)                            | exit 2 usage shape `ARXIC-CLI-USAGE`; zero writes; no stack trace                                                  | `intents.test.ts`, `real-world.test.ts`                                                                                            |
| Stage-04 proposal claims `truthState: verified` (C-5, ADR-001 §2)                            | build fails closed `ARXIC-INTENT-LEDGER-INPUT-INVALID` — model output can never assert verified                    | `ledger.test.ts`                                                                                                                   |
| Dangling inventory-row citation in stage-04                                                  | build fails closed `ARXIC-INTENT-LEDGER-INPUT-INVALID`                                                             | `ledger.test.ts`                                                                                                                   |
| Worker ledger staging fails on a verified candidate (C-2)                                    | promotion blocked via the existing worker failure path; ledger diagnostics recorded                                | `worker-executor.ts` wiring (covered by construction + unit-level staging tests)                                                   |
| Caller-planted schema-valid forged `intents.json` at a worker-lane run root (P1 remediation) | NEVER kept: worker lane always rebuilds from imported artifacts + scans; skipIfPresent keeps only scan-clean bytes | `worker-executor.test.ts` (real execute path, forged ledger overwritten byte-exactly), `ledger.test.ts` (kept-secret fails closed) |
| 13.json sourceRef missing `extractor` (P2 remediation)                                       | structured `ARXIC-INTENT-LEDGER-INPUT_INVALID`; never a raw TypeError                                              | `ledger.test.ts`                                                                                                                   |
| Control chars (`\n`, ESC/ANSI, NUL) in model-derived ledger fields (P3 remediation)          | rendered as spaces on a single table line; no raw escape bytes in output                                           | `intents.test.ts`                                                                                                                  |
