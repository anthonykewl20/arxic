# 1.0-SYMLINK-SAFE — staged doc updates (charter §10.2)

Issue: #175 · PR: pending · Disposition: blocked

## 1. `docs/SYNC.md` — tracker row (replace the existing row verbatim)

```
| #175 | [1.0-SYMLINK-SAFE] Symlink-safe source collection (realpath containment + pre-size check) | ☑ done |
```

## 2. `docs/SYNC.md` — session-log row (append to the table)

```
| 2026-08-14 | **#175 (1.0-SYMLINK-SAFE) Symlink-safe source collection DONE.** SourceUaAdapter and AstGrepAdapter reject tracked absolute/relative/deep/looping symlinks and non-regular files, enforce resolved-root containment and pre-read size quotas, and perform bounded no-follow reads; real Tree-sitter and real sg proofs safely scan a crafted repository containing escaping links and an oversized file. Unsafe and oversized inputs are **blocked**. |
```

## 3. `CHANGELOG.md` — entry under `## [Unreleased]` → `### security`

```
- 1.0-SYMLINK-SAFE Symlink-safe source collection (#175): source collection now rejects symlinks and non-regular files, verifies realpath containment beneath the resolved repository root, checks quotas before bounded no-follow reads, and passes only safely copied bytes to real sg; real Tree-sitter and sg tests cover escaping links and oversized files.
```

## 4. `VERSION` bump required?

no — this is release hardening within the pending 1.0 milestone.

## 5. Evidence pointers

- Real-world proof: `packages/source-ua-adapter/src/__tests__/source-safety.test.ts` — real Tree-sitter scans a crafted git repository while rejecting absolute, relative, deeply nested, and looping symlinks, a FIFO, and an oversized file.
- Real-world proof: `packages/ast-grep-adapter/src/__tests__/source-safety.test.ts` — real `sg` scans safely staged bytes while escaping/looping symlinks and an oversized file are excluded and diagnosed.
- Artifacts: Vitest console results only; this is non-UI behavior, so no screenshots or traces apply.
- Gates: typecheck ☑ · lint ☑ · typecheck:packages ☑ · format ☑ · test ☑ · license gate ☑

## 6. Sad paths proved (each mapped to a truth state, charter §4)

| Trigger                                                       | Expected disposition              | Test                                                                                                                 |
| ------------------------------------------------------------- | --------------------------------- | -------------------------------------------------------------------------------------------------------------------- |
| Tracked absolute symlink escapes the resolved source root     | blocked                           | `SourceUaAdapter hostile source containment…rejects absolute…symlinks`; `AstGrepAdapter hostile source containment…` |
| Tracked relative or deeply nested symlink escapes the root    | blocked                           | Both source-safety suites                                                                                            |
| Tracked symlink loop cannot resolve                           | blocked                           | Both source-safety suites                                                                                            |
| Tracked file exceeds configured byte quota                    | blocked                           | Both source-safety suites                                                                                            |
| Tracked path is a FIFO/non-regular file or external hard link | blocked                           | FIFO proof plus external-hard-link rejection in both source-safety suites                                            |
| Safe regular file accompanies hostile entries                 | hypothesized source evidence only | Both real-engine source-safety suites                                                                                |
