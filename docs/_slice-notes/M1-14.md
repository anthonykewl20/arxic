# M1-14 — staged doc updates (charter §10.2)

Issue: #42 · PR: #92 · Disposition: mixed (hypothesized candidates; blocked on invalid/injection; observed on empty neighbourhood)

## 1. `docs/SYNC.md` — tracker row (replace the existing row verbatim)

```
| #42 | [M1-14] Stage 4: LLM candidate inference (no promotion) | ☑ done |
```

## 2. `docs/SYNC.md` — session-log row (append to the table)

```
| 2026-08-07 | **#42 (M1-14) Stage 4: LLM candidate inference (no promotion) DONE.** `@arxic/orchestrator-langgraph` gains `inference.ts` (service layer) producing hypothesized workflow candidates from the smallest sufficient source-evidence neighbourhood via the `ModelAdapter` structured-output boundary: deterministic/deduped/bounded source-only neighbourhood selection, content-as-data messages (metadata projection — no source bytes or operator prompt), candidate mapping to a full schema-valid Workflow with `status` forced `hypothesized` and deduped pattern-valid evidence IDs, and a single-request adapter call that short-circuits an empty neighbourhood to zero candidates. `#infer` delegates into it (orchestrator owns retry + truth-state classification); `defaultInference` no longer fabricates (zero candidates when no model is configured). Candidates are emitted `hypothesized` only (never `verified`); invalid structured output retries then `blocked` with no promotion; prompt-injection payloads are treated as data. Real Tree-sitter (`SourceUaAdapter`) + real `sg` (`AstGrepAdapter`) over the real `reference-auth-app` + a real `node:http` OpenAI-compatible endpoint prove ≥1 hypothesized candidate survives stage-6 reconciliation; malformed output → bounded retries → blocked. Gates green (typecheck/lint/format/test 387/license 0 rejected). Dispositions: hypothesized, observed, blocked. **M1 11/15.** Next: #25. |
```

## 3. `CHANGELOG.md` — entry under `## [Unreleased]` → `### added`

```
- M1-14 Stage-4 LLM candidate inference (#42): `@arxic/orchestrator-langgraph` gains `inference.ts` (service layer) producing hypothesized workflow candidates from the smallest sufficient source-evidence neighbourhood via the `ModelAdapter` structured-output boundary. Neighbourhood selection is deterministic, deduped, and bounded (source-only); inference messages are content-as-data (a metadata projection — no source bytes, repo, commit, blobSha256, or operator prompt); candidate mapping emits a full schema-valid Workflow with `status` forced to `hypothesized` and deduped pattern-valid evidence IDs. The orchestrator's `#infer` delegates into it and owns the retry loop + truth-state classification (single retry boundary; the adapter is called once per attempt with `maxRetries:0`). Candidates are emitted `hypothesized` only (never `verified` — the stage-4 schema has no status field); invalid structured output retries then blocks with `ARXIC-ORCH-MODEL-RETRIES` and no promotion; prompt-injection payloads in source evidence are treated as data and blocked if echoed; an empty neighbourhood short-circuits to zero candidates (completed, `observed`). `defaultInference` no longer fabricates a candidate when no model is configured (returns zero candidates; configure `modelAdapter` + `model` to produce candidates). Real Tree-sitter + real `sg` over the real reference-auth-app + a real OpenAI-compatible HTTP endpoint prove real hypothesized candidates survive reconciliation.
```

## 4. `VERSION` bump required?

no — pre-1.0; internal capability in a private package (the CLI/worker that expose it are not yet shipped). No user-observable change.

## 5. Evidence pointers

- Real-world proof: `packages/orchestrator-langgraph/src/__tests__/inference-real-world.test.ts` — real Tree-sitter (`SourceUaAdapter`) + real `sg` (`AstGrepAdapter`) over the real `test-fixtures/reference-auth-app` source + a real `node:http` OpenAI-compatible endpoint on an ephemeral `freePort()`; a full `LangGraphOrchestrator({modelAdapter, model})` run (no `inferCandidates` injection) against the real built+served app exercises real Crawlee/Chromium at stage 5 and proves stage-4 candidates survive stage-6 reconciliation.
- Artifacts: per-run `FileStageCheckpointer` dir — `artifacts/04.json` (stage-4 `InferenceResult`), `artifacts/06.json` (stage-6 `CoverageMatrix`), `stages/04.json` (checkpoint with `modelRequestId`, no prompt bytes).
- Gates: typecheck ☑ · lint ☑ · format ☑ · test (387 passing) ☑ · license gate ☑

## 6. Sad paths proved (each mapped to a truth state, charter §4)

| Trigger                                                      | Expected disposition                                                                           | Test                                                                                                                                         |
| ------------------------------------------------------------ | ---------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| Invalid structured output every attempt (adapter `ok:false`) | blocked (`ARXIC-ORCH-MODEL-RETRIES`, no promotion)                                             | `inference.test.ts` (stage4Infer → undefined → retry); `inference-real-world.test.ts` (malformed HTTP → 2 bounded requests → failed/blocked) |
| Schema-version drift / retries-exhausted from adapter        | blocked (diagnostics preserved at `runStage4Inference` service boundary)                       | `inference.test.ts` (runStage4Inference `{ok:false,diagnostics}`)                                                                            |
| Injection-like model output (adapter blocks)                 | blocked (`ARXIC-MODEL-STRUCTURED-OUTPUT-INVALID`)                                              | `inference.test.ts` (injection-block diagnostic preserved)                                                                                   |
| Adapter throws / provider error                              | retry then blocked (never throws past the retry loop)                                          | `inference.test.ts` (stage4Infer try/catch → undefined)                                                                                      |
| Model hints `status:'verified'`                              | rejected — candidates always `hypothesized` by construction                                    | `inference.test.ts` (mapStage4Candidates forces hypothesized; parseInferenceResult re-normalizes)                                            |
| Prompt-injection payload in source neighbourhood             | treated as DATA; blocked if echoed (metadata-only projection, no source bytes/operator prompt) | `inference.test.ts` (buildInferenceMessages content-as-data asserts); real-world (canary absent from request body + persisted bytes)         |
| Empty source neighbourhood                                   | observed (zero candidates, completed-not-errored, no HTTP call)                                | `inference.test.ts` (runStage4Inference short-circuit; adapter never called)                                                                 |
| Distinct rules matching the same code span                   | deduped evidence IDs (no inflated coverage)                                                    | `inference.test.ts` (dedup)                                                                                                                  |
| Non-deterministic input ordering                             | reproducible neighbourhood (codepoint sort + dedup + bound)                                    | `inference.test.ts` (selectNeighbourhood determinism)                                                                                        |
