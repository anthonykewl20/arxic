# 0.1.1-HUMAN-FLOW-E2E — staged doc updates (charter §10.2)

Issue: unfiled release gate · PR: pending · Disposition: mixed (the deterministic verifier produced the happy-path `verified` bundle; unreachable origin was `blocked`).

## 1. `docs/SYNC.md` — tracker row (replace the existing row verbatim)

```text
No tracker row exists yet; add an unfiled 0.1.1 human-flow release-gate completion row if the integrator files a tracker item.
```

## 2. `docs/SYNC.md` — session-log row (append to the table)

```text
| 2026-08-16 | **0.1.1-HUMAN-FLOW-E2E release gate DONE.** The packed CLI installs in a clean room, runs the independently installed reference app through a real local OpenAI-compatible model endpoint and Chromium to a promoted verified bundle, and proves unreachable-origin blocking preserves it. Release workflow runs this gate after tarball smoke and before tag/publish. Packed-run defects: omitted rule packs and screenshot runtime source; both now ship in the tarball. |
```

## 3. `CHANGELOG.md` — entry under `## [Unreleased]` → `### changed`

```text
- 0.1.1-HUMAN-FLOW-E2E: add the packed-install, local-app, local-model release gate before publish; promoted local bundles now include the documented inspectable directory layout.
```

## 4. `VERSION` bump required?

```text
no — this hardens the pending 0.1.1 release gate without changing its planned version.
```

## 5. Evidence pointers

- Real-world proof: `scripts/human-flow-e2e.mjs` — packed CLI, isolated npm install, independently installed `reference-auth-app`, real HTTP model stub, real Chromium, promoted bundle and unreachable-origin preservation.
- Artifacts: `docs/evidence/0.1.1-HUMAN-FLOW/` — sanitized diagnostics, promoted manifest, model request log, privacy-attested synthetic screenshots, and final output.
- Gates: targeted tests 24 passing · packed human flow passing (84.532 s) · lint passing · typecheck passing · full test 127 files / 1101 tests passing (617.02 s) · format passing. The full suite includes the license-policy test (0 rejected).

## 6. Sad paths proved (each mapped to a truth state, charter §4)

| Trigger                                                            | Expected disposition                      | Test                                        |
| ------------------------------------------------------------------ | ----------------------------------------- | ------------------------------------------- |
| Unreachable target origin after a successful promotion             | blocked; prior promoted bytes unchanged   | `scripts/human-flow-e2e.mjs` sad-path phase |
| Tarball lacks a required rule pack                                 | blocked before human flow can claim a run | `scripts/tarball-smoke.test.mjs`            |
| Tarball lacks screenshot runtime source used by generated workflow | blocked before human flow can claim a run | `scripts/tarball-smoke.test.mjs`            |
