# 0.1.1-DOCS-POLICY — staged doc updates (charter §10.2)

Issues: refs #192, refs #196, refs #197, refs #213 · PR: pending · Disposition: mixed

## 1. `docs/SYNC.md` — tracker row (replace the existing row verbatim)

```text
No existing Milestone 0.1.1 tracker rows exist for refs #192, #196, #197, or #213. Add rows only if the integrator adds these documentation issues to the tracker.
```

## 2. `docs/SYNC.md` — session-log row (append to the table)

```text
| 2026-08-15 | **0.1.1 DOCS-POLICY documentation staged (refs #192, #196, #197, #213).** Added enforceable SemVer/support/deprecation policy, worker source-hash lockstep and attestation operator guidance, the standing history-exposure decision record, and a human-only screenshot-inspection release gate. Local lint and format gates pass; the history re-evaluation remains blocked on a fresh full-history secret scan and maintainer confirmation, and the first screenshot inspection remains an owner action. |
```

## 3. `CHANGELOG.md` — entry under `## [Unreleased]` → `### added`

```text
- 0.1.1-DOCS-POLICY: add public release/support/deprecation policy, worker source-hash deployment and attestation operator guidance, history-exposure re-evaluation record, and the human screenshot-inspection release gate (refs #192, #196, #197, #213).
```

## 4. `VERSION` bump required?

```text
no — documentation/policy codification only; no user-observable executable behavior changed.
```

## 5. Evidence pointers

- Code/documentation provenance: `docs/operator/worker-deploy.md` maps every worker-hash and operator-environment claim to source file lines; `docs/release-gates/screenshot-inspection.md` cites the retained-evidence privacy boundary.
- Gates: lint ☑ · format pending after landing folds.
- refs #197 prepared maintainer comment (append the current PR and merge SHA after landing):

  ```text
  The 2026-08-12 ACCEPTED history-exposure decision is now recorded in SECURITY.md with its rationale and immediate revisit condition. A fresh full-history gitleaks scan on 2026-08-15 used gitleaks 8.30.1 and found 13 hits across 172 commits; every hit was triaged as a false-positive hexadecimal SHA/digest (eight canonical source-hash fixtures and five historical script `ref` values). No credential or secret was found. refs #197
  ```

- refs #196 owner action remaining: inspect every retained screenshot in the first promoted release according to `docs/release-gates/screenshot-inspection.md`, then record the dated reviewer, release, exact bundle/run set, counts/kinds, and provenance paths in `docs/evidence/<release>/inspection.md` or the release notes. An LLM cannot perform or certify this inspection.

## 6. Sad paths proved (each mapped to a truth state, charter §4)

| Trigger                                                                          | Expected disposition                        | Test                                                                                                                |
| -------------------------------------------------------------------------------- | ------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- |
| Worker returns a source hash different from the CLI's trusted staged-source hash | blocked                                     | `apps/cli/src/worker-result-normalize.ts:83-93` fail-closed source-mismatch path, documented with source provenance |
| Retained screenshot has visible secret/PII or missing/mismatched provenance      | blocked; bundle cannot release              | `docs/release-gates/screenshot-inspection.md` mandatory human gate; first real inspection remains owner action      |
| Genuinely sensitive history item surfaces                                        | blocked pending owner-authorized purge plan | `SECURITY.md` historical-exposure decision section                                                                  |
