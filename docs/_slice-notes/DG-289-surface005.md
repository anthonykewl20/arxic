# DG-289 — staged doc updates (charter §10.2)

Issue: #289 · PR: see EVIDENCE comment on the issue · Disposition: observed (all gates green at the PR head; `verified` is not assignable by an LLM per ADR §2)

## 1. `docs/SYNC.md` — tracker row (replace the existing row verbatim)

```
| #289 | [DG-289] SURFACE-005 serialization-safe browser callbacks + allowedOrigins origin-gate wiring | ☑ done |
```

## 2. `docs/SYNC.md` — session-log row (append to the table)

```
| 2026-08-21 | **#289 (DG-289) local-lane SURFACE-005 fixed + allowedOrigins wired DONE.** The crawl inventory probe (crawlee-adapter adapter.ts:149-184) and the exploration-driver identity probe (:472) were rewritten serialization-safe — no named inner functions or closure captures inside any `page.evaluate` callback, hoisted to module-level exported probes (`pageInventoryProbe`, `elementIdentityProbe`) so the tsx lane's `__name` injection can never reach the page. `config.target.allowedOrigins` now flows local-executor → OrchestratorInput → orchestrator discovery → crawl origin gate (`allowedCrawlOrigins`, fail-closed default `[origin]`) and the exploration PolicyEngine list (`explorationAllowedOrigins`), per DECISION 5360240026 (M0 excluded). Proven by real Chromium: committed no-spend G-4 harness red at baseline product code (crawl-root ARXIC-SURFACE-005 `__name` ReferenceError, routes `[]`, harness sha256 c0c76e90…) → green at the fix head over the reference-auth-app (routes `/ /login /forgot-password`, zero SURFACE-005) and read-only koel at its DG-11 pin (root recorded, zero SURFACE-005); AC-5 binary proven through the full CLI source path (declared second loopback origin's asset fetched + not aborted; undeclared → aborted with the existing diagnostic, zero hits). Full suite 185 files / 1593 tests green; worker lane 42/42 green, zero apps/worker diffs; published CLI tsup build green. **Next: per milestone plan.** |
```

## 3. `CHANGELOG.md` — entry under `## [Unreleased]` → `### fixed`

```
- DG-289 local-lane crawl SURFACE-005 (#289): browser callbacks serialized by `page.evaluate` are now serialization-safe (no named inner functions — the tsx/esbuild `__name` helper can no longer reach the page context and kill source-lane crawls at the root), and `config.target.allowedOrigins` is wired into the runtime origin gates (crawl asset gate + exploration PolicyEngine; fail-closed default unchanged: target origin only). Proven in real Chromium by the committed zero-spend crawl harness (red at baseline, green at fix over the reference app and a read-only koel clone at its DG-11 pin) plus origin-gate admit/refuse tests through the full CLI source path.
```

## 4. VERSION bump required?

no — defect fix + wiring of an already-parsed config field; behavior of published builds unchanged (the published CLI is tsup-built and never had the tsx defect; the allowedOrigins wiring is a runtime-policy fix, not a new user-visible capability).

## 5. Evidence pointers

All gate outputs below ran in the `.worktrees/DG-289` worktree (branch `issue/289`); harness artifacts live ONLY in per-run `mkdtemp` dirs (left in place at run time, never committed).

- Baseline-red (C-2, exact stash mechanics incl. the 6 product files; harness sha256 `c0c76e90fdd3d9876ed9908ec42dd38412ca6dcd356db09d3d74a76d890bac8a` — re-reproduced 2026-08-21 by the resuming session with the FINAL harness so red and green share one harness identity; the earlier session's recorded hashes `ecc99c50…`/`830c92b6…` belonged to intermediate harness revisions and are superseded): harness exit 1, `surfaceRoutes: []`, crawl-root `{"code":"ARXIC-SURFACE-005","message":"Navigation failed after 3 bounded attempts: page.evaluate: ReferenceError: __name is not defined …","severity":"observed","subject":"http://127.0.0.1:43005/"}`, stack at adapter.ts:149:36.
- Fixed-green G-4 `--target ref-app`: harness exit 0, `crawlCompleted: true`, `surfaceRoutes: ["/", "/forgot-password", "/login"]`, `surface005AtRoot: []`, `surface005Elsewhere: 0` (full CLI source-import lane under tsx, loopback stub-model endpoint, zero spend).
- G-4 `--target koel` (read-only clone at pin `dfec91ff290509c622ff7cf392fb5e506841ee2b`, docker `koel-php83:rehearsal`, ephemeral host port, AttestationFront): harness exit 0, root route recorded (`surfaceRoutes: ["/"]` — the DG-11 failure state recorded `[]`), zero SURFACE-005 anywhere. "Beyond root": the unseeded koel SPA shell exposes no crawlable anchors at depth 1, so the honest recorded route set is the root; completion past the root navigation is the observable (DG-11 baseline: root request itself failed).
- Red→green root-cause record (esbuild keepNames over the real sources): baseline adapter callback body contained `__name((element) => …, "labelOf"/"control")` wraps INSIDE the serialized body; post-fix body is clean; exploration-driver:472's inline arrow was already clean at baseline (FINDING — contract narrative overstated that site; hoisted + SP-1-guarded anyway).
- Real-world proof: `packages/crawlee-adapter/src/__tests__/surface005-regression.real-world.test.ts` (tsx child lane with a POSITIVE CONTROL proving the lane still injects `__name`, SP-1 toString assertions, real-Chromium crawl + in-page identity probe), extended `real-world.test.ts` suites (crawl-root zero-SURFACE-005 assertions; element-identity probe evaluated in real Chromium), `apps/cli/src/__tests__/allowed-origins.real-world.test.ts` (AC-5 binary through the full CLI source path), `packages/orchestrator-langgraph/src/__tests__/exploration-origins.test.ts` (PolicyEngine list wiring + fail-closed default through the real PolicyEngine).
- Gates (final head): typecheck ☑ exit 0 · typecheck:packages ☑ exit 0 · lint ☑ exit 0 · format:check ☑ exit 0 (last line in the PR) · full `pnpm test` ☑ 185 files / 1593 tests exit 0 · worker lane `vitest run apps/worker` ☑ 42/42 exit 0, zero apps/worker diffs · published CLI build (`pnpm --filter arxic run build`) ☑ exit 0 — see CCR 5360688858 for the frozen `--filter @arxic/cli` vacuous-match defect · G-4 harness ref-app ☑ / koel ☑ exit 0 · CI on the PR head ☑ (see PR checks).
- G-0 baseline record (read-only, at `main` @ bd3bc0a): DG-11 diagnostics lines 2695 (directus) / 2819 (koel) print the crawl-root SURFACE-005 `__name` ReferenceError; `rg page.evaluate` shows the two callbacks; `rg allowedOrigins` shows the field parsed at config validation but consumed by no runtime gate (local-executor/orchestrator/exploration forwarded only `origin`; the m0 hardcode is excluded by the DECISION).

## 6. Sad paths proved (each mapped to a truth state, charter §4)

| Trigger                                                                            | Expected disposition                                                                                                                                                                                                                    | Test                                                                                                                                                                                     |
| ---------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| SP-1: residual injected-helper reference in a serialized callback (fix incomplete) | regression stays red: tsx-child positive control proves the lane injects `__name`; probe toString assertions + the crawl itself fail loudly; nothing is caught-and-ignored                                                              | `surface005-regression.real-world.test.ts` (tsx child verdict: `controlInjected` true, `probeClean` required, crawl + identity assertions)                                               |
| SP-2: allowedOrigins absent / empty / malformed                                    | fail-closed: runtime gates admit target origin only (byte-identical baseline behavior); malformed/absent declarations rejected at config validation with the stable `ARXIC-CONFIG-INVALID` diagnostic on `config.target.allowedOrigins` | `surface005-regression.real-world.test.ts` (allowedCrawlOrigins unit block), `allowed-origins.real-world.test.ts` (validation block + ABSENT integration), `exploration-origins.test.ts` |
| SP-3: third-party clone fails to boot / pin mismatch / image missing               | harness classifies blocked (exit 2) with an honest machine-readable reason; zero fabricated inventory                                                                                                                                   | `surface005-crawl-harness.ts` `blocked()` paths (koel pin assert, image check, readiness)                                                                                                |
| SP-4: shared-file change regresses the worker lane                                 | G-5 fails                                                                                                                                                                                                                               | `vitest run apps/worker` 42/42 green; `git status` shows zero diffs under apps/worker                                                                                                    |

## 7. Disclosed gaps and contract interactions (honesty section)

- CONTRACT CHANGE REQUESTS (frozen-command defects, resolved forms used; both posted in-issue awaiting owner flip): 5360467441 — the G-4 command's repo-root-relative script path cannot resolve because `pnpm --filter` exec runs with cwd `apps/worker`; executed with the `../../` repo-root-relative form. 5360688858 — `pnpm --filter @arxic/cli run build` matches no package (the CLI is named `arxic`) and exits 0 vacuously; executed as `pnpm --filter arxic run build`.
- The G-4 harness model stub returns a schema-valid EMPTY proposal set (loopback, no upstream, zero spend): stage 4 completes honestly empty and the run is an honest partial — the crawl assertions are unaffected. The exploration-driver's in-page evaluation under tsx is therefore exercised by the G-1 tsx child (real page, real handles) rather than by the G-4 full run.
- FINDING (posted in-issue): the contract narrative's claim that exploration-driver.ts:472 "references `__name`" is contradicted by transform observation (the inline anonymous arrow body was already clean); C-1's required output was delivered there anyway (module-level hoist + SP-1 guard), and the driver's serialized source is now assertable.
- The exploration PolicyEngine receives the declared list (exploration.ts wiring), but every step's `decide()` at this pin passes the TARGET origin, so the declared entries are carried-and-inert until step origins are passed — tested at the real-PolicyEngine seam (`exploration-origins.test.ts`); changing step-origin semantics was deliberately NOT done (out of frozen scope).
- "Absent" allowedOrigins at the CLI layer means the config-level rejection (validation requires the field non-empty and containing the origin — baseline behavior kept byte-identical); the runtime fail-closed default covers programmatic callers that omit the field.
- koel AC-3 "completes beyond root": the unseeded SPA shell has no depth-1 anchors, so the honest route set is `["/"]`; the DG-11 baseline failed AT the root (routes `[]`), so root-recording + zero SURFACE-005 is the observable improvement.
