# 0.1.1-CLI-PUBLISH-FOUNDATION — staged doc updates (charter §10.2)

Issues: #184, #185 · Disposition: mixed

## 1. `docs/SYNC.md` — tracker row (replace the existing row verbatim)

```
| #184 | [0.1.1] CLI publish foundation (re-scoped) | ☑ done |
| #185 | [0.1.1] CLI publish foundation (re-scoped) | ☑ done |
```

## 2. `docs/SYNC.md` — session-log row (append to the table)

```
| 2026-08-15 | **#184/#185 (0.1.1-CLI-PUBLISH-FOUNDATION) DONE (re-scoped).** The consensus-terra 2026-08-15 decision retains source-only private internal workspaces, syncs all non-fixture manifests to `VERSION` 0.1.1, and makes only the bundled ESM `arxic` CLI publishable. CI now proves manifest/producer/CLI-version provenance and installs a packed CLI in a fresh directory; source-mode `--version` resolves `ARXIC_VERSION` from `@arxic/contracts`, and the CLI defers the runtime pipeline module graph so version and help commands do not require bundled source-only schemas. Local provenance and tarball gates passed; refs #184 and #185. |
```

## 3. `CHANGELOG.md` — entry under `## [Unreleased]` → `### changed`

```
- 0.1.1-CLI-PUBLISH-FOUNDATION (#184, #185): re-scoped distribution to the bundled ESM `arxic` CLI only; synchronized non-fixture workspace metadata to `VERSION`, added publish tarball and provenance gates, and kept internal packages source-only and private.
```

## 4. `VERSION` bump required?

No — `VERSION` is already 0.1.1; this slice synchronizes every non-fixture workspace manifest to that existing source of truth.

## 5. Evidence pointers

- Real packaging proof: `scripts/tarball-smoke.mjs` builds only `./apps/cli`, uses `npm pack`, asserts the tar contents exclude source/node_modules and include the binary/license/notice, installs in a fresh temporary directory, and runs `arxic --version`, `arxic --help`, and `arxic run --help`.
- Provenance proof: `scripts/assert-version-provenance.test.mjs` covers manifest drift, literal `0.0.0` producer output, and a wrong built CLI version; `scripts/tarball-smoke.test.mjs` covers wrong version, missing LICENSE, and failed help.
- Measured local CI impact: version provenance 6.41s; tarball smoke 48.91s (55.32s combined), so it remains in the existing 20-minute `ci` job after Test.
- The `pinnedCanonicalSha256` updates in `packages/intent` and `packages/orchestrator-langgraph` real-world tests are rebase-aligned recomputed fixture digests, not version changes. Their tests independently recompute and compare the digest, so an incorrect pin fails.
- Gates: typecheck ☑ (`pnpm typecheck` passed) · lint ☑ (`pnpm lint` passed) · test ☑ (`pnpm test`: 121 files / 1032 tests passed) · provenance ☑ · tarball smoke ☑ · license gate ☑ · format ☑ (`All matched files use Prettier code style!`)

## 6. Sad paths proved (each mapped to a truth state, charter §4)

| Trigger                                                    | Expected disposition | Test                                                                           |
| ---------------------------------------------------------- | -------------------- | ------------------------------------------------------------------------------ |
| A non-fixture manifest drifts from `VERSION`               | blocked CI gate      | `scripts/assert-version-provenance.test.mjs`                                   |
| A production metadata producer emits literal `0.0.0`       | blocked CI gate      | `scripts/assert-version-provenance.test.mjs`                                   |
| Built or packed CLI reports a version other than `VERSION` | blocked CI gate      | `scripts/assert-version-provenance.test.mjs`, `scripts/tarball-smoke.test.mjs` |
| Packed CLI cannot serve `--help` in a fresh install        | blocked CI gate      | `scripts/tarball-smoke.test.mjs`                                               |

Deferrals: npm publication, OIDC provenance, and `NPM_TOKEN` handling remain #189's lane; no release workflow was changed. Any #186 packaging/release follow-up remains outside this slice.
