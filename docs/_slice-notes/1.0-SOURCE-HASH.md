# 1.0-SOURCE-HASH — staged doc updates (charter §10.2)

Issue: #178 · PR: pending · Disposition: blocked

## 1. `docs/SYNC.md` — tracker row (replace the existing row verbatim)

```text
| #178 | [1.0-SOURCE-HASH] Worker sourceSha256 hashes real staged source bytes and is independently reconciled | ☑ done |
```

## 2. `docs/SYNC.md` — session-log row (append to the table)

```text
| 2026-08-13 | **#178 (1.0-SOURCE-HASH) Worker source integrity hardening DONE.** Worker and CLI independently hash a canonical bytewise manifest of the actual staged source files; fabricated, missing, or byte-stale worker hashes are blocked with `ARXIC-EXEC-WORKER-SOURCE-MISMATCH` and cannot promote. Real `arxic-worker:dev` Docker execution against vulnerable-auth-app proved agreement and rejection of a tampered result. |
```

## 3. `CHANGELOG.md` — entry under `## [Unreleased]` → `### security`

```text
- 1.0-SOURCE-HASH Worker source integrity (#178): replace the config-projection digest with a canonical manifest over real staged bytes, independently recompute it in the CLI, and block promotion on any worker hash mismatch; real Docker proof covers agreement and a fabricated result.
```

## 4. `VERSION` bump required?

no — include in the integrator's pending 1.0 release bump.

## 5. Evidence pointers

- Real-world proof: `apps/cli/src/__tests__/worker-real-world.test.ts` — real `arxic-worker:dev` Docker sandbox ran the vulnerable-auth-app pipeline, independently matched the host hash, then rejected a transport-consistent fabricated worker hash.
- Artifacts: ephemeral worker result volume and run directory; no retained UI evidence added by this non-UI integrity slice.
- Gates: typecheck ☑ · lint ☑ · format ☑ · test (fixture suites + 37 worker tests passing) ☑ · license gate ☑

## 6. Sad paths proved (each mapped to a truth state, charter §4)

| Trigger                                                                | Expected disposition                             | Test                                                                                           |
| ---------------------------------------------------------------------- | ------------------------------------------------ | ---------------------------------------------------------------------------------------------- |
| Fabricated worker hash, including internally consistent verifier field | blocked, no promotion                            | `worker-result-normalize.test.ts`; real Docker tamper assertion in `worker-real-world.test.ts` |
| One staged file's bytes change                                         | blocked when worker reports the prior hash       | `source-tree-hash.test.ts` plus reconciliation test                                            |
| Missing source hash                                                    | blocked as malformed protocol                    | `worker-result-normalize.test.ts`                                                              |
| Empty source tree                                                      | deterministic content hash                       | `source-tree-hash.test.ts`                                                                     |
| Large source file                                                      | included byte-for-byte and deterministic         | `source-tree-hash.test.ts`                                                                     |
| Source symlink                                                         | link text is hashed without following the target | `source-tree-hash.test.ts`                                                                     |
