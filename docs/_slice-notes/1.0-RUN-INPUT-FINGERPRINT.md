# 1.0-RUN-INPUT-FINGERPRINT — staged doc updates (charter §10.2)

Issue: #176 · PR: <pending> · Disposition: blocked

## 1. `docs/SYNC.md` — tracker row (replace the existing row verbatim)

```
| #176 | [1.0-RUN-INPUT-FINGERPRINT] Orchestrator run-id reuse rejects changed semantic inputs | ☑ done |
```

## 2. `docs/SYNC.md` — session-log row (append to the table)

```
| 2026-08-14 | **#176 (1.0-RUN-INPUT-FINGERPRINT) Orchestrator run-id reuse rejects changed semantic inputs DONE.** Terminal records persist a canonical SHA-256 input fingerprint; changed source revision, origin, policy, or config/model input, absent legacy fingerprint, persisted artifact-byte hash drift, and non-serializable policy/config semantics now fail closed as `blocked` with loop-closed `ARXIC-ORCH-*` diagnostics (including `ARXIC-ORCH-INPUT-FINGERPRINT-INVALID`). A rejected reuse is in-memory only: it never alters the persisted terminal or legacy record, so a later identical request returns the original receipt. Real reference-auth-app/Chromium/Crawlee proof persists then reuses a terminal run with a changed revision. |
```

## 3. `CHANGELOG.md` — entry under `## [Unreleased]` → `### security`

```
- 1.0-RUN-INPUT-FINGERPRINT (#176): fail closed when a reused orchestrator run id has changed semantic inputs, an unprovable legacy record, or drifted persisted artifact bytes; terminal reuse now verifies its input and artifact SHA-256 bindings before returning a prior result.
```

## 4. `VERSION` bump required?

yes → patch release, because reuse integrity failures are user-observable safety fixes.

## 5. Evidence pointers

- Real-world proof: `packages/orchestrator-langgraph/src/__tests__/real-world.test.ts` — real reference-auth-app, real Chromium/Crawlee, ephemeral port and sqlite; a persisted terminal state is reused with a changed source revision and is blocked.
- Artifacts: temporary test run/checkpoint directories only; no UI surface changed and no evidence artifact is retained.
- Gates: `pnpm install` ☑ · typecheck ☑ · lint ☑ · format ☑ · full `pnpm test` ☑ · license gate ☑

## 6. Sad paths proved (each mapped to a truth state, charter §4)

| Trigger                                                  | Expected disposition      | Test                                           |
| -------------------------------------------------------- | ------------------------- | ---------------------------------------------- |
| Reuse a terminal id with a changed source revision       | blocked                   | `run-reuse.test.ts`; real `real-world.test.ts` |
| Reuse a terminal id with a changed origin                | blocked                   | `run-reuse.test.ts`                            |
| Reuse a terminal id with changed policy                  | blocked                   | `run-reuse.test.ts`                            |
| Reuse a terminal id with changed config/model            | blocked                   | `run-reuse.test.ts`                            |
| Terminal artifact bytes differ from the recorded SHA-256 | blocked                   | `sad-paths.test.ts`                            |
| Legacy persisted state lacks an input fingerprint        | blocked                   | `run-reuse.test.ts`                            |
| Policy/config cannot be canonically JSON serialized      | blocked (`...-INVALID`)   | `run-reuse.test.ts`                            |
| Rejected reuse attempt against terminal or legacy record | blocked; record unchanged | `run-reuse.test.ts`                            |
| Same semantic input plus unchanged artifact bytes        | terminal result returned  | `run-reuse.test.ts`                            |
