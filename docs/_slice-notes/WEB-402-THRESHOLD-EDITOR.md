# WEB-402-THRESHOLD-EDITOR — staged doc updates (charter §10.2)

Issue: #402 · Disposition: observed implementation (local green ×2 — real captures compared against a real approved baseline through real engine runs, and the dialog round-trip driven in real Chromium; CI on the PR head is the remaining gate — an LLM cannot assign `verified`).

## 1. `docs/SYNC.md` — tracker row (replace the existing row verbatim)

Supersedes the WEB-402-REBIND-UI row if it is still pending fold.

```text
| #402 | Web product release: comprehensive frontend intent campaigns and AI visual auditing | In progress; noisy real-world pages stop flagging captures: a per-project visual change ratio (fraction 0–0.5, default 0 = pixel-exact) is edited in the project settings dialog and honored by drain classification — a capture counts as changed only when its changed-pixel ratio strictly exceeds the threshold, while diff pixels, regions and diff files stay recorded for review |
```

## 2. `docs/SYNC.md` — session-log row (append to the table)

```text
| 2026-09-08 | **#402 (WEB-402-THRESHOLD-EDITOR) per-project visual change ratio with settings-dialog editor DONE.** The last open diff-viewer follow-up: today ANY single differing pixel classified a capture 'changed' (drain: `changedPixels ? 'changed' : 'unchanged'`), so noisy pages (timestamps, badges, ads) had no operator recourse. `Project.visualChangeRatio` (fraction 0–0.5, default 0 = exactly today's pixel-exact behavior) joins the validated project whitelist (`visualChangeRatio must be a number from 0 to 0.5`; the integer-only `bounded` helper was deliberately NOT reused — fractions need a custom range check), is edited in the project settings dialog (number input, step 0.001, helper text naming the 0 = pixel-exact / max 0.5 semantics; no new endpoints), and is honored in drain: `status: compared.ratio > ratioGate ? 'changed' : 'unchanged'` with `ratioGate = run.project.visualChangeRatio ?? 0` — with 0/unset this is behavior-identical to `changedPixels ? 'changed' : 'unchanged'` because ratio > 0 ⇔ ≥1 changed pixel, so every existing pixel-exact classification holds unchanged. pixelmatch's per-pixel YIQ tolerance (0.1) is untouched — this is a classification gate, not a color-distance knob. Honesty invariant: `changedPixels`, `ratio`, `diffRegions` and the diff PNG stay recorded even for sub-threshold captures, so the diff viewer keeps showing exactly what moved while the status stays quiet. Real-world proof: `visual.test.ts` extends the existing real comparison journey with a real 8×8 px `body::after` patch on the 800×600 reference fixture (measured honestly: 64 changed pixels, ratio 1.33e-4) — classified 'changed' under the default (today's behavior pinned), then reclassified 'unchanged' with the gate set to EXACTLY the measured ratio (pinning strict `ratio > gate`; anything above follows a fortiori) while `changedPixels > 0`, the diff file parses and `diffRegions` stay present, and the existing big background flip (#c00030, >100,000 px) still classifies 'changed' under the raised gate; `projects.test.ts` (new unit file) pins validation: accepts 0/0.005/0.5, rejects −0.1/0.6/1.5/'abc'/NaN/null with the range message, keeps 'Unknown project setting' for foreign fields; `threshold-editor-ui.real-world.test.ts` (new real-browser journey, real Chromium against the real server over a real state directory) drives the workspace overview → project settings dialog, saves 0.005, reopens and asserts the persisted value round-tripped through the real PUT, enters 7 → the existing `#project-error` alert presents the server's range rejection and the stored value stays 0.005 after reopen — zero hard page errors. Red first: journey failed at `saveProject` with 400 'Unknown project setting' (noise run itself was already 'changed', honestly measured 64 px via a temporary instrumented red run); validation unit cases all failed with 'Unknown project setting'; the dialog journey timed out waiting for the 'Visual change ratio' input. Regressions: ui.real-world dialog journey (both themes) 2/2, diff-viewer-ui 1/1, changed-regions-ui 1/1, capture-gallery-ui 2/2, workflow-captures 2/2 (projectBody consumer), changed-regions 4/4, visual-review 5/5, visual-density 10/10, visual-matrix 9/9, run-history 1/1, workspace 5/5. Gates local: apps/web typecheck ☑ · repo eslint ☑ · format ☑ (full repo after note). Known gaps (recorded, not hidden): the wizard input drops the HTML min/max attributes (kept step/required) so out-of-range values reach the server and present through the existing `#project-error` alert flow instead of being silently blocked by native constraint validation — the server remains the validation authority; the gate snapshots at enqueue (runs already queued keep their enqueue-time project), same as every other project setting. |
```

## 3. `CHANGELOG.md` — entry under `## [Unreleased]` → `### added`

```text
- WEB-402-THRESHOLD-EDITOR per-project visual change ratio (#402): projects gain a `visualChangeRatio` (fraction 0–0.5, default 0 = pixel-exact) validated in the project whitelist, editable in the project settings dialog, and honored by drain classification — a capture counts as changed only when its changed-pixel ratio strictly exceeds the threshold; sub-threshold noise stops flagging captures while diff pixels, regions and diff files stay recorded for review, and pixelmatch's per-pixel tolerance is unchanged; proven with real captures against a real approved baseline (measured 64-px noise reclassified, >100,000-px regression still changed) and a real-browser dialog round-trip.
```

## 4. `VERSION` bump required?

No — the fold integrator owns `VERSION`; the capability rides the current `0.0.301` line.

## 5. Evidence pointers

- Real-world proof: `apps/web/src/__tests__/visual.test.ts` — the existing real comparison journey extended: real reference vulnerable-auth fixture behind a mutating proxy, real Chromium captures, real approved baseline, real pixelmatch comparisons (8×8 px patch measured 64 changed px / ratio 1.33e-4; background flip >100,000 px still 'changed').
- Real-world proof: `apps/web/src/__tests__/threshold-editor-ui.real-world.test.ts` — real server (`startWorkbench`, port 0), real state directory, real Chromium login → workspace overview → project settings dialog → real PUT round-trip → server 400 presentation; zero hard page errors.
- Unit: `apps/web/src/__tests__/projects.test.ts` (new) — validation admit/refuse matrix plus the untouched whitelist rejection.
- Artifacts: `docs/evidence/WEB-402-THRESHOLD-EDITOR/red.txt` (pre-implementation failures with verbatim tails + post-red refinement disclosure) + `green.txt` (green ×2 of the final code, 13/13).
- Gates: typecheck ☑ · lint ☑ · format ☑ · test (slice 13/13 ×2; siblings 42/42) ☑ · license gate → CI.

## 6. Sad paths proved (each mapped to a truth state, charter §4)

| Trigger                                                                    | Expected disposition                                                                                                                     | Test                                                                                         |
| -------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- |
| Project leaves `visualChangeRatio` unset/0 (default)                       | Pixel-exact behavior identical to today: any differing pixel flags 'changed' (ratio > 0 ⇔ ≥1 changed px)                                 | `visual.test.ts` existing unchanged/changed pair (kept intact) + new noise run under default |
| Sub-threshold capture under a raised gate                                  | Status 'unchanged' while `changedPixels > 0`, diff file parses and `diffRegions` stay present — the diff viewer keeps showing what moved | `visual.test.ts` gated rerun (8×8 px patch, gate = measured ratio)                           |
| Changed-pixel ratio exactly equal to the gate                              | Strictly `ratio > gate`: 'unchanged' — the boundary is honored                                                                           | `visual.test.ts` gated rerun with gate set to exactly the measured ratio                     |
| Gate 0.005 with a real layout regression (>100,000 px)                     | Still 'changed' — the gate must not over-block real regressions                                                                          | `visual.test.ts` big-flip run after the gate was raised                                      |
| Out-of-range/negative/non-numeric ratio (−0.1, 0.6, 1.5, 'abc', NaN, null) | 400 `visualChangeRatio must be a number from 0 to 0.5` from project validation                                                           | `projects.test.ts` refuse matrix                                                             |
| Foreign field (e.g. `pixelTolerance`)                                      | Still 400 'Unknown project setting' — the whitelist rejection is untouched                                                               | `projects.test.ts` whitelist case                                                            |
| Operator enters 7 in the dialog and saves                                  | The existing `#project-error` alert presents the server's range error; the stored value stays 0.005; dialog does not persist             | `threshold-editor-ui.real-world.test.ts` sad path                                            |
| Dialog round-trip through the real PUT                                     | Saved 0.005 reopens as 0.005; zero hard page errors                                                                                      | `threshold-editor-ui.real-world.test.ts` happy path + `errors.hard()` check                  |
