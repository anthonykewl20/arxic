# M1-15 — staged doc updates (charter §10.2)

Issue: #43 · PR: #85 · Disposition: mixed (observed + blocked)

## 1. `docs/SYNC.md` — tracker row (replace the existing row verbatim)

```
| #43 | [M1-15] Stage 8: intent exploration + human-approval | ☑ done |
```

## 2. `docs/SYNC.md` — session-log row (append to the table)

```
| 2026-08-07 | **#43 (M1-15) Stage 8: intent exploration + human-approval interrupt DONE.** Implements ADR §8 TARGETED / §9 stage-8 loop (plan → policy check → human approval → explore → journal → budget → normalize to Workflow IR) in `@arxic/orchestrator-langgraph/exploration.ts`; `@arxic/policy-engine` authorizes origin + action class before any execution (forbidden / denied / budget-exhausted / origin-drift → `blocked`); new `PlaywrightExplorationDriver` service in `@arxic/playwright-agent-adapter` executes read-only steps in real Chromium and records runtime `EvidenceRef`s with real `accessibilitySnapshotSha256` (CDP `Accessibility.getFullAXTree`); exploration yields `observed` at best, never `verified`. New `ARXIC-EXPLORATION-*` diagnostics. Real Chromium (Playwright 1.62.1) proof vs `reference-auth-app` attached. Gates green (typecheck/lint/format/test 385/license). **M1 11/15.** Next: #25 (M1-11) — RESUME HERE reconciled by integrator. |
```

## 3. `CHANGELOG.md` — entry under `## [Unreleased]` → `### added`

```
- M1-15 Stage 8: intent exploration + human-approval interrupt (#43): the plan → policy check → human approval → explore → journal → budget → normalize loop lands in `@arxic/orchestrator-langgraph/exploration.ts`; `@arxic/policy-engine` authorizes origin + action class before execution and blocks forbidden / unapproved-privileged / budget-exhausted / origin-drift steps; a new `PlaywrightExplorationDriver` capability in `@arxic/playwright-agent-adapter` drives read-only steps in real Chromium and normalizes observations into runtime `EvidenceRef`s carrying a real `accessibilitySnapshotSha256`. Exploration produces `observed` at best — it may never originate `verified`. New `ARXIC-EXPLORATION-*` diagnostics.
```

## 4. `VERSION` bump required?

No. Pre-1.0, source-only, internal orchestration — not user-observable. `0.2.0` is reserved for M1-EXIT (#27) per `RELEASES.md`.

## 5. Evidence pointers

- Real-world proof: `packages/orchestrator-langgraph/src/__tests__/exploration-real-world.test.ts` — real Chromium (Playwright 1.62.1) driving the real `reference-auth-app` (built + booted, per-run temp sqlite, reset/seed; no `ARXIC_MAILPIT_*`).
- Artifacts: `docs/_slice-notes/M1-15-evidence/exploration-trace.zip`, `step-00-observe-route-login.png`, `step-00-observe-route-account.png`. Observed real Chromium accessibility SHA-256 prefix for `/login`: `572fa81d921f`.
- Gates: typecheck ☑ · lint ☑ · format ☑ · test (385 passing) ☑ · license gate ☑.

## 6. Sad paths proved (each mapped to a truth state, charter §4)

| Trigger                                     | Expected disposition      | Test                                                                                                                                                                                   |
| ------------------------------------------- | ------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| forbidden / unknown policy action           | blocked                   | `exploration.test.ts` — blocks an unknown policy action without executing it                                                                                                           |
| destructive without recorded human approval | blocked                   | `exploration.test.ts` — blocks a destructive action without recorded human approval; `exploration-real-world.test.ts` — blocks a destructive intent until a human approval is recorded |
| budget exhausted mid-plan                   | blocked                   | `exploration.test.ts` — blocks gracefully when budget is exhausted mid-plan                                                                                                            |
| budget missing (zero)                       | blocked                   | `exploration.test.ts` — blocks a read-only plan when its budget is missing at zero                                                                                                     |
| origin / locator drift                      | blocked                   | `exploration.test.ts` — blocks origin drift instead of silently accepting the observation                                                                                              |
| required browser step failed                | blocked                   | `exploration.test.ts` — blocks a failed required browser step                                                                                                                          |
| required transition not observed            | observed (never verified) | `exploration.test.ts` — reports required unobserved transitions only as observed and never verified                                                                                    |
| read-only plan observed within budget       | observed                  | `exploration-real-world.test.ts` — observes the real login accessibility tree in Chromium within budget                                                                                |
