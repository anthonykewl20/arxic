# 0.1.1-RECONCILER-GRAPH-SEAM — staged doc updates (charter §10.2)

Issue: #195 · PR: pending · Disposition: mixed (the reconciliation action retains its existing hypothesized, observed, contradicted, and blocked classifications)

## 1. `docs/SYNC.md` — tracker row (replace the existing row verbatim)

```
| #195 | [1.0/P2] Route reconciler graph access through the evidence-graph service seam | ☑ done |
```

## 2. `docs/SYNC.md` — session-log row (append to the table)

```
| 2026-08-15 | **#195 (1.0/P2) Reconciler graph service seam DONE.** `@arxic/evidence-graph` now owns Graphology topology ingestion and opaque edge traversal; reconciliation retains only domain-specific candidate-to-runtime topology and truth-state classification. The reconciler importer no longer declares Graphology, and the regenerated lockfile removes only that importer entry. Three isolated auth-domain-pack runs and the post-rebase full suite pass, so the prior auth-domain-pack assertion failure is unreproduced. Existing real Tree-sitter, sg, Crawlee, and Chromium proof passes for both reference apps. Dispositions remain hypothesized, observed, contradicted, and blocked. Next: integration review. |
```

## 3. `CHANGELOG.md` — entry under `## [Unreleased]` → `### internal`

```
- 1.0/P2 Route reconciler graph access through the evidence-graph service seam (#195): route reconciler topology ingestion and edge lookup through an opaque evidence-graph capability, removing its direct Graphology dependency while preserving reconciliation outcomes.
```

## 4. `VERSION` bump required?

no — internal layering refactor with no user-observable behavior change.

## 5. Evidence pointers

- Real-world proof: `packages/reconciler/src/__tests__/real-world.test.ts` — real Tree-sitter + `sg` evidence and real Crawlee/Chromium reconciliation pass for `reference-auth-app` and `vulnerable-auth-app`.
- Artifacts: none retained; the existing proof uses temporary directories and this slice changes no UI behavior.
- Lockfile resolution: rebased onto `origin/main` at `e8c7c36`; retained reconciler version `0.1.1` while removing its direct `graphology` dependency. `pnpm install` regenerated `pnpm-lock.yaml`; its diff against `origin/main` removes only the reconciler Graphology importer entry. `pnpm install --frozen-lockfile` passed.
- Auth-domain-pack investigation: `pnpm exec vitest run packages/auth-domain-pack` passed three consecutive isolated runs (10/10 tests each; durations 31.56s, 31.66s, and 31.25s). The post-rebase full `pnpm test` also passed (123 files, 1049 tests). Verdict: the prior `expected verified, received contradicted` failure is unreproduced; no classification or topology behavior was changed for it.
- Follow-up topology coverage: shuffled topology inputs preserve edge-traversal results; empty input has no edges; duplicate edge keys continue to throw because the reconciler pre-deduplicates them before calling the Graphology-backed service.
- Gates: `pnpm lint` passed · `pnpm typecheck` passed · `pnpm --filter @arxic/evidence-graph typecheck` passed · `pnpm exec vitest run packages/reconciler packages/evidence-graph` passed (10 files, 34 tests) · `pnpm test` passed (123 files, 1049 tests) · `pnpm format:check` final line: `All matched files use Prettier code style!`

## 6. Sad paths proved (each mapped to a truth state, charter §4)

| Trigger                                                                       | Expected disposition                 | Test                                                     |
| ----------------------------------------------------------------------------- | ------------------------------------ | -------------------------------------------------------- |
| A reconciler package reintroduces a direct `graphology` dependency or import. | blocked at the service-boundary gate | `packages/reconciler/src/__tests__/service-seam.test.ts` |
| Candidate ids are duplicated.                                                 | blocked                              | `packages/reconciler/src/__tests__/reconcile.test.ts`    |
| Source evidence asserts a route that runtime did not observe.                 | hypothesized                         | `packages/reconciler/src/__tests__/reconcile.test.ts`    |
| Static and runtime route structures conflict.                                 | contradicted                         | `packages/reconciler/src/__tests__/reconcile.test.ts`    |
