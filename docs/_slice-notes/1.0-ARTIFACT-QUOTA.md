# 1.0-ARTIFACT-QUOTA — staged doc updates (charter §10.2)

Issue: #174 · PR: pending · Disposition: blocked (LLM-authored; deterministic verifier owns any `verified` outcome)

## 1. `docs/SYNC.md` — tracker row (replace the existing row verbatim)

```text
| #174 | [1.0-ARTIFACT-QUOTA] Enforce worker artifact quota before unbounded buffering | ☑ done |
```

## 2. `docs/SYNC.md` — session-log row (append to the table)

```text
| 2026-08-14 | **#174 (1.0-ARTIFACT-QUOTA) Enforce worker artifact quota before unbounded buffering DONE.** Worker result bytes now enter a physically size-limited tmpfs, total capacity is checked before import, and per-file/cumulative-byte/file-count limits are enforced incrementally before over-limit chunks are retained. Before any host staging directory or archive extraction exists, an argv-only container-side `find` scan rejects symlinks, hard-linked regular files, and special files; real Docker proved the hostile-symlink path blocked without changing its host target. Completion-marker exits 137/139 and Docker's OOM flag now preserve memory-quota classification. Real Docker 29.7.2 also proved volume-full and oversized-file rejection as blocked with ARXIC-WORKER-QUOTA-EXCEEDED and no worker OOM; the existing non-root, read-only-root/source, no-socket, and no-egress invariants stayed green. |
```

## 3. `CHANGELOG.md` — entry under `## [Unreleased]` → `### security`

```text
- 1.0-ARTIFACT-QUOTA worker artifact quota (#174): bound result writes with a quota-sized tmpfs; reject links and special files inside the container before host extraction; stream imports through incremental per-file, cumulative-byte, and file-count guards before retaining hostile bytes; and preserve OOM classification from Docker state or completion-marker exits 137/139. Real-Docker adversarial tests prove blocked unsafe-entry/quota diagnostics without host writes or worker OOM while preserving isolation.
```

## 4. `VERSION` bump required?

no — the integrator owns release version coordination for this parallel 1.0 hardening batch.

## 5. Evidence pointers

- Real-world proof: `packages/environment/src/__tests__/worker-result-volume.real-world.test.ts` — real Docker 29.7.2 enforced pre-extraction unsafe-entry rejection, physical volume and streaming import limits, and completion-marker OOM classification.
- Artifacts: no UI screenshots/traces; sanitized test output only.
- Gates: typecheck ☑ · lint ☑ · typecheck:packages ☑ · format ☑ · test ☑ · license gate ☑

## 6. Sad paths proved (each mapped to a truth state, charter §4)

| Trigger                                                         | Expected disposition                                          | Test                                                   |
| --------------------------------------------------------------- | ------------------------------------------------------------- | ------------------------------------------------------ |
| Worker writes beyond result tmpfs capacity                      | blocked (`ARXIC-WORKER-QUOTA-EXCEEDED`), no OOM               | `physically blocks a volume-full write...`             |
| One artifact exceeds its streaming limit                        | blocked (`ARXIC-WORKER-QUOTA-EXCEEDED`), no whole-file buffer | `rejects an oversized file during streaming import...` |
| Incremental cumulative bytes exceed budget                      | blocked; prior charged bytes remain bounded                   | `checks cumulative bytes on every chunk across files`  |
| Incremental regular-file count exceeds budget                   | blocked                                                       | `rejects cumulative file count incrementally`          |
| Symlink targeting a host path plus attempted through-link write | blocked before host staging/extraction; host target unchanged | `rejects a hostile symlink before host extraction...`  |
| Completion marker reports exit 137                              | blocked memory-quota classification (`oomKilled: true`)       | `classifies a completion-marker exit 137...`           |
| Hard-linked regular file                                        | blocked before host staging/extraction                        | `rejects hard-linked result files before...`           |
| FIFO or another special file                                    | blocked before host staging/extraction                        | `rejects a FIFO as a non-regular result file`          |
| Traversal, forged hash, or absent manifest                      | blocked; existing fail-closed import unchanged                | existing real-Docker result-volume tests               |
