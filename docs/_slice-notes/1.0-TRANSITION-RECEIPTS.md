# 1.0-TRANSITION-RECEIPTS — staged doc updates (charter §10.2)

Issue: #173 · PR: pending · Disposition: mixed

## 1. `docs/SYNC.md` — tracker row (replace the existing row verbatim)

```
| #173 | [1.0-TRANSITION-RECEIPTS] Real per-transition observation; make missingTransitions gate live; forbidNetworkErrors default | ☑ done |
```

## 2. `docs/SYNC.md` — session-log row (append to the table)

```
| 2026-08-14 | **#173 (1.0-TRANSITION-RECEIPTS) Real per-transition observation DONE.** Generated Playwright fixtures now write nonce-bound, versioned transition/event receipts; the verifier validates the trusted fixture/spec binding, receipt shape, step names, URL witnesses, and structured HTTP/request/console/page events before classification. Receipts are required even for workflows with no required transitions so their event gate stays live; consumed receipt bytes are forbidden-substring scanned then deleted before artifact capture. Missing, malformed, forged, skipped, or redaction-failed receipts block. `forbidNetworkErrors` now defaults on as an intentional user-visible policy change. Real Chromium proves two clean passes against both fixture apps, default HTTP/console error gating, and denial coexistence. Dispositions: verified, contradicted, blocked. |
```

## 3. `CHANGELOG.md` — entry under `## [Unreleased]` → `### security`

```
- 1.0-TRANSITION-RECEIPTS verifier hardening (#173): generated Playwright transition/event receipts now replace exit-status observation and stdout network scanning; receipts are required even when no transitions are required, are redaction-scanned then deleted before capture, and missing or untrusted receipts fail closed. Structured HTTP, request, console, and page errors are forbidden by default; this is an intentional user-visible policy change. Real Chromium proof covers both reference apps and denied hostile egress.
```

## 4. `VERSION` bump required?

no — integration batch versioning is owned by the integrator.

## 5. Evidence pointers

- Real-world proof: `packages/verifier/src/real-world.test.ts` — real Chromium against both reference fixture apps verifies two clean receipt-backed runs and default-gates real HTTP 404 + console errors.
- Artifacts: `docs/evidence/1.0-TRANSITION-RECEIPTS/` — sanitized action timelines with adjacent sanitization provenance and named masked screenshots with capture provenance; no raw trace ZIPs.
- Gates: typecheck ✓ · lint ✓ · format ✓ · test (981 passing) ✓ · license gate ✓

## 6. Sad paths proved (each mapped to a truth state, charter §4)

| Trigger                                                                                        | Expected disposition | Test                                                                                                      |
| ---------------------------------------------------------------------------------------------- | -------------------- | --------------------------------------------------------------------------------------------------------- |
| Exit-zero suite omits a required transition receipt                                            | blocked              | `packages/verifier/src/index.test.ts` — `blocks an exit-zero run...`                                      |
| No-required-transitions workflow omits its still-required event receipt                        | blocked              | `packages/verifier/src/index.test.ts` — `blocks a no-required-transitions workflow...`                    |
| No-required-transitions workflow supplies a valid empty transition list                        | verified             | `packages/verifier/src/index.test.ts` — `requires an event receipt even when...`                          |
| Legacy fixture produces no receipt                                                             | blocked              | `packages/verifier/src/index.test.ts` — `blocks a passing legacy fixture...`                              |
| Malformed, forged, or malformed-URL receipt witness                                            | blocked              | `packages/verifier/src/index.test.ts` — receipt parser and malformed URL fail-closed tests                |
| Receipt JSON contains a persona forbidden substring                                            | blocked              | `packages/verifier/src/index.test.ts` — `blocks and deletes a receipt...`; file is deleted before capture |
| Host exports stale receipt path/nonce for a receipt-free probe                                 | blocked from child   | `packages/verifier/src/index.test.ts` — `removes inherited receipt configuration...`                      |
| Hostile cross-origin request is denied but the receipt-enabled test body tolerates that denial | test run fails       | `credential-egress.real-world.test.ts` — generated `assertNetworkContained` afterEach is authoritative    |
| Real HTTP error response or console error under omitted policy flag (`forbidNetworkErrors` on) | blocked              | `packages/verifier/src/real-world.test.ts` — `records real Chromium HTTP and console errors...`           |
| Application assertion failure                                                                  | contradicted         | existing verifier real-world locator-drift proof                                                          |
