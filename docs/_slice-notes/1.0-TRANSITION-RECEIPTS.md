# 1.0-TRANSITION-RECEIPTS — staged doc updates (charter §10.2)

Issue: #173 · PR: pending · Disposition: mixed

## 1. `docs/SYNC.md` — tracker row (replace the existing row verbatim)

```
| #173 | [1.0-TRANSITION-RECEIPTS] Real per-transition observation; make missingTransitions gate live; forbidNetworkErrors default | ☑ done |
```

## 2. `docs/SYNC.md` — session-log row (append to the table)

```
| 2026-08-14 | **#173 (1.0-TRANSITION-RECEIPTS) Real per-transition observation DONE.** Generated Playwright fixtures now write nonce-bound, versioned transition/event receipts; the verifier validates the trusted fixture/spec binding, receipt shape, step names, URL witnesses, and structured HTTP/request/console/page events before classification. Missing, malformed, forged, or skipped transition receipts block; real Chromium proves two clean passes against both fixture apps plus default HTTP/console error gating. Dispositions: verified, contradicted, blocked. |
```

## 3. `CHANGELOG.md` — entry under `## [Unreleased]` → `### security`

```
- 1.0-TRANSITION-RECEIPTS verifier hardening (#173): generated Playwright transition/event receipts now replace exit-status observation and stdout network scanning; missing or untrusted receipts fail closed and structured HTTP, request, console, and page errors are forbidden by default. Real Chromium proof covers both reference apps.
```

## 4. `VERSION` bump required?

no — integration batch versioning is owned by the integrator.

## 5. Evidence pointers

- Real-world proof: `packages/verifier/src/real-world.test.ts` — real Chromium against both reference fixture apps verifies two clean receipt-backed runs and default-gates real HTTP 404 + console errors.
- Artifacts: `docs/evidence/1.0-TRANSITION-RECEIPTS/` — sanitized action timelines with adjacent sanitization provenance and named masked screenshots with capture provenance; no raw trace ZIPs.
- Gates: typecheck ✓ · lint ✓ · format ✓ · test (981 passing) ✓ · license gate ✓

## 6. Sad paths proved (each mapped to a truth state, charter §4)

| Trigger                                                             | Expected disposition | Test                                                                                            |
| ------------------------------------------------------------------- | -------------------- | ----------------------------------------------------------------------------------------------- |
| Exit-zero suite omits a required transition receipt                 | blocked              | `packages/verifier/src/index.test.ts` — `blocks an exit-zero run...`                            |
| Legacy fixture produces no receipt                                  | blocked              | `packages/verifier/src/index.test.ts` — `blocks a passing legacy fixture...`                    |
| Malformed or forged receipt witness                                 | blocked              | `packages/verifier/src/index.test.ts` — receipt parser and verifier fail-closed tests           |
| Real HTTP error response or console error under omitted policy flag | blocked              | `packages/verifier/src/real-world.test.ts` — `records real Chromium HTTP and console errors...` |
| Application assertion failure                                       | contradicted         | existing verifier real-world locator-drift proof                                                |
