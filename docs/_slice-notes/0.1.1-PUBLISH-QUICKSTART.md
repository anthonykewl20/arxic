# 0.1.1-PUBLISH-QUICKSTART — staged doc updates (charter §10.2)

Issues: #189, #186 · Disposition: mixed

## 1. `docs/SYNC.md` — tracker row (replace the existing row verbatim)

```
| #189 | [0.1.1] Automated npm publish pipeline with provenance / SLSA attestation | ☑ done |
| #186 | [0.1.1] Installable CLI: npx arxic + working end-to-end quickstart | ☑ done |
```

## 2. `docs/SYNC.md` — session-log row (append to the table)

```
| 2026-08-15 | **#189/#186 (0.1.1-PUBLISH-QUICKSTART) DONE.** Release tags now run fail-closed lint, typecheck, full test, provenance, packed-CLI smoke, exact-tag verification, npm `--provenance` publishing, then GitHub Release creation; dispatch is explicitly dry-run-only and release refs are serialized. The npm manifest now declares repository, bugs, and homepage metadata. Tarball smoke invokes the installed package's declared JavaScript bin directly (cross-platform) and proves malformed config yields a structured non-zero diagnostic rather than the former schema-resolution crash. The installable `arxic` quickstart documents attested local configuration and its honest no-model partial outcome. Publication and the literal `npx arxic@latest` path await the maintainer's `NPM_TOKEN` and first tag. |
```

## 3. `CHANGELOG.md` — entry under `## [Unreleased]` → `### added`

```
- 0.1.1-PUBLISH-QUICKSTART (#189, #186): add a fail-closed provenance-enabled npm release workflow and an installable-CLI quickstart with an attested local-target configuration and documented no-model outcome; add npm repository metadata and cross-platform packed-runtime smoke coverage for bundled contract schemas.
```

## 4. `VERSION` bump required?

No — the release automation and documentation publish the existing `arxic` 0.1.1 artifact.

## 5. Evidence pointers

- Tarball smoke: fresh tarball install resolves `package.json` → `bin.arxic` and invokes the JavaScript entry directly, so Windows does not depend on a POSIX `.bin` shim. A malformed temporary YAML config exits non-zero with an `ARXIC-CONFIG-*` diagnostic; a stack/schema-resolution-looking output fails the smoke assertion.
- Result: with `ARXIC_MODEL_BASE_URL` and `ARXIC_MODEL_API_KEY` unset, the packed CLI exited 1 with `status=partial, outcome=blocked`; the attestation stage was present and discovery observed `/login`. It preserved the run directory without a crash or a `verified` claim.
- Workflow validation: YAML was parsed locally; dispatch is intentionally non-publishing and prints its planned publish/release actions. Its real GitHub execution awaits repository secrets and a tag.
- Gates: lint ☑ (`pnpm lint`) · typecheck ☑ (`pnpm typecheck`) · provenance ☑ (`node scripts/assert-version-provenance.mjs`) · tarball smoke ☑ (`node scripts/tarball-smoke.mjs`) · tarball tests ☑ (`pnpm exec vitest run scripts/tarball-smoke.test.mjs`: 1 file / 5 tests) · full test ☑ (`pnpm test`: 121 files / 1048 tests passed) · format ☑.

## 6. Sad paths proved (each mapped to a truth state, charter §4)

| Trigger                                                      | Expected disposition                 | Test                                                                        |
| ------------------------------------------------------------ | ------------------------------------ | --------------------------------------------------------------------------- |
| Tag does not equal `v$(VERSION)`                             | blocked release                      | release workflow exact-tag shell gate                                       |
| `NPM_TOKEN` is absent on a tag run                           | blocked release                      | unconditional `npm publish --provenance` authentication attempt             |
| Manual dispatch                                              | observed dry run, no publication     | workflow dispatch-only description step and publish/release guards          |
| Packed CLI cannot locate checkout-relative contract schemas  | blocked runtime crash (before fix)   | tarball quickstart proof; static schema imports bundle the required schemas |
| Packed CLI emits stack/module/schema-crash output for config | blocked smoke failure                | `scripts/tarball-smoke.test.mjs` crash-looking-output sad path              |
| Packed CLI has no model endpoint or key                      | partial observed/blocked run, exit 1 | tarball quickstart proof against the reference fixture                      |

Deferrals: npm publication, provenance attestation receipt, GitHub Release creation, and literal `npx arxic@latest` activation await maintainer configuration of `NPM_TOKEN` and the first release tag. No CI workflow other than `release.yml`, no release-test workflow, and no `docs/SYNC.md`, `CHANGELOG.md`, or `VERSION` was changed in this lane.
