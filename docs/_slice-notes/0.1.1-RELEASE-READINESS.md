# 0.1.1-RELEASE-READINESS — staged doc updates (charter §10.2)

Issue: refs #196 · PR: pending · Disposition: blocked pending human release-gate execution

## 1. `docs/SYNC.md` — tracker row (replace the existing row verbatim)

```
| #196 | Owner screenshot inspection for the first promoted release | open — human owner step after the first release tag |
```

## 2. `docs/SYNC.md` — session-log row (append to the table)

```
| 2026-08-15 | **Release readiness residuals.** Recorded the non-monotonic tag-history decision, added a screenshot-inspection census/sign-off tool and procedure, and ran the first real release workflow dry run successfully (run 31868969555). The remaining owner action is human inspection of the first promoted release bundle after its tag; refs #196 stays open. |
```

## 3. `CHANGELOG.md` — entry under `## [Unreleased]` → `### internal`

```
- Release readiness: add a deterministic screenshot-inspection manifest and per-file human sign-off template; record the tag-history decision and first release-workflow dry-run result.
```

## 4. `VERSION` bump required?

No — release process tooling and documentation only.

## 5. Evidence pointers

- Release workflow dry run: https://github.com/anthonykewl20/arxic/actions/runs/31868969555 — passed 2026-08-15 (run 31868969555). Describe dry run output: `Dry run complete: would publish arxic@0.1.1 with npm provenance.` and `Dry run complete: would create the matching GitHub Release.`
- Unit proof: `scripts/inspection-manifest.test.mjs` — fabricated screenshots, empty root, missing-root CLI failure, and non-followed file/directory symlinks.
- Artifacts: generated `inspection-manifest.json`, `inspection-manifest.txt`, and `inspection-sign-off.md` are placed alongside the supplied promoted bundle/run/evidence root.
- Full evidence census (generated outputs removed afterward): `node scripts/inspection-manifest.mjs docs/evidence` → `Wrote inspection manifest for 16 screenshot(s).` The actual count is 16, not the expected 8: four each under `1.0-TRANSITION-RECEIPTS`, `M1-EXIT-23-12`, `M1-SCREENSHOT-PRIVACY`, and `M1-TRACE-SANITIZATION`.
- Gates: typecheck not run · lint passed · format passed (last line: `All matched files use Prettier code style!`) · focused scripts test passed · full suite not required because this slice changes only `scripts/inspection-manifest.{mjs,test.mjs}`, `docs/release-gates/screenshot-inspection.md`, `RELEASES.md`, and this slice note.

## 6. Sad paths proved (each mapped to a truth state, charter §4)

| Trigger                                | Expected disposition                                                                          | Test                                                                   |
| -------------------------------------- | --------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------- |
| Inspection root does not exist         | blocked — no census/sign-off artifacts are fabricated                                         | `inspection-manifest.test.mjs`: missing root reports a clear CLI error |
| Evidence root contains no screenshots  | observed — clean empty census, no implied human approval                                      | `inspection-manifest.test.mjs`: empty root                             |
| Screenshot is a non-PNG retained image | observed — listed with hash and byte size; dimensions explicitly skipped rather than invented | `inspection-manifest.test.mjs`: JPEG row                               |
| Screenshot file is a symlink           | blocked — listed but never followed; no target hash or dimensions are invented                | `inspection-manifest.test.mjs`: symlinked file                         |
| Directory is a symlink                 | blocked — never traversed, so outside-root screenshots remain absent                          | `inspection-manifest.test.mjs`: symlinked directory                    |

## #196 decision memo for the orchestrator

The issue DoD literally requires a performed human inspection before the 0.1 release notes can record it (criteria 1 and 3). The gate is now documented and tooled, and release workflow run 31868969555 passed, but neither establishes a human pixel review. Keep the issue open. After the first release tag is created, the reduced owner step is: run `node scripts/inspection-manifest.mjs <promoted-bundle>`, inspect every listed screenshot and adjacent provenance, complete the generated sign-off, and paste the dated sign-off into the release notes.
