# 1.0-CRED-EGRESS — staged doc updates (charter §10.2)

Issue: #172 · PR: pending · Disposition: blocked

## 1. `docs/SYNC.md` — tracker row (replace the existing row verbatim)

```
| #172 | [1.0-CRED-EGRESS] Contain credential egress in generated verification | ☑ done |
```

## 2. `docs/SYNC.md` — session-log row (append to the table)

```
| 2026-08-13 | **#172 (1.0-CRED-EGRESS) Contain credential egress in generated verification DONE.** Generated Playwright fixtures now default-deny requests outside action-owned origins, preflight runtime origins, inspect every redirect, block Service Workers, and emit `ARXIC-COMPILE-ORIGIN-DENIED`; real Chromium proved a foreign redirect reached no foreign server and legitimate login still passed on both reference apps. Disposition: **blocked** for denied egress. |
```

## 3. `CHANGELOG.md` — entry under `## [Unreleased]` → `### security`

```
- 1.0-CRED-EGRESS credential-egress containment (#172): added action-owned origin preflight, generated context-level default-deny interception with redirect revalidation, Service Worker blocking, and stable blocked diagnostics; real Chromium proved foreign redirects send no credential bytes while legitimate same-origin logins remain runnable.
```

## 4. `VERSION` bump required?

yes → next 1.0 release version chosen by the integrator, because this security behavior is user-observable per RELEASES.md

## 5. Evidence pointers

- Real-world proof: `packages/playwright-compiler/src/__tests__/credential-egress.real-world.test.ts` — real Chromium rejects a local server's foreign-origin redirect without contacting the foreign server.
- Real-world proof: `packages/playwright-compiler/src/real-world.test.ts` — real Playwright/Chromium executes generated same-origin login verification against both reference apps.
- Artifacts: transient Playwright output only; no raw trace ZIP retained.
- Gates: typecheck ☑ · lint ☑ · format ☑ · test ☑ · license gate ☑

## 6. Sad paths proved (each mapped to a truth state, charter §4)

| Trigger                                                                | Expected disposition | Test                                                                                                          |
| ---------------------------------------------------------------------- | -------------------- | ------------------------------------------------------------------------------------------------------------- |
| Initial navigation redirects to a foreign origin                       | blocked              | `real Chromium aborts a foreign-origin navigation redirect before credential bytes leave the approved origin` |
| Runtime observation URL is outside the approved allowlist              | blocked              | `blocks a runtime observation whose origin is outside the action-owned allowlist`                             |
| Page request targets a foreign origin                                  | blocked              | generated `context.route('**/*')` policy exercised by redirect proof                                          |
| Service Worker attempts to take over requests                          | blocked              | `generated policy blocks service workers and installs context interception before navigation`                 |
| Page fetch targets a foreign origin while SW registration is attempted | blocked              | `real Chromium blocks a page-context cross-origin fetch and service worker registration`                      |
