# 1.0-CRED-EGRESS — staged doc updates (charter §10.2)

Issue: #172 · PR: pending · Disposition: blocked

## 1. `docs/SYNC.md` — tracker row (replace the existing row verbatim)

```
| #172 | [1.0-CRED-EGRESS] Contain credential egress in generated verification | ☑ done |
```

## 2. `docs/SYNC.md` — session-log row (append to the table)

```
| 2026-08-13 | **#172 (1.0-CRED-EGRESS) Contain credential egress in generated verification DONE.** Generated Playwright fixtures configure compile-approved origins before navigation and combine once-per-test context HTTP(S)/WebSocket routing with Chromium CDP `Fetch` response-stage interception. Playwright request routing cannot intercept redirect hops after `route.continue()`, so CDP rejects foreign 3xx `Location` targets before Chromium follows them while passing all other responses through unchanged. Runtime URLs are compared by canonical origin, Service Workers are blocked, and denials emit `ARXIC-COMPILE-ORIGIN-DENIED`. Diagnostic instrumentation disproved a containment false positive in the failing CLI run: it denied no URL, and both clean Chromium passes succeeded. Stale ignored `.vitest-auth.db{,-shm,-wal}` files had been copied into the temporary source repository, emitted blocked scanner diagnostics, and irreversibly latched promotion ineligible before stage 10. Removing that local contamination restored the CLI `verified` outcome and completed promotion. A separate reference-app logout proof captured its legitimate redirect from declared `http://127.0.0.1:<port>` to `http://localhost:<port>`; the existing narrowly bounded same-port loopback alias admits it, while genuinely foreign origins remain denied. Real Chromium proved 302/303/307/308 credential-bearing foreign redirects, fetch, and WebSocket attempts reached neither foreign server and leaked zero canary bytes while legitimate reference-app flows remained runnable. Disposition: **blocked** for denied egress. |
```

## 3. `CHANGELOG.md` — entry under `## [Unreleased]` → `### security`

```
- 1.0-CRED-EGRESS credential-egress containment (#172): added action-owned canonical-origin preflight, generated once-per-test context-level default-deny HTTP(S) and WebSocket interception, Chromium CDP response-stage foreign-redirect blocking, Service Worker blocking, and stable blocked diagnostics; approved requests continue natively via `route.continue()` and unchanged responses via `Fetch.continueResponse`, a narrowly bounded same-port `localhost`/`127.0.0.1` alias admits the reference app's attested loopback redirect, and real Chromium proved 302/303/307/308 credential-bearing foreign redirects, fetch, and WebSocket attempts send no canary bytes while legitimate same-origin workflows remain runnable.
```

## 4. `VERSION` bump required?

yes → next 1.0 release version chosen by the integrator, because this security behavior is user-observable per RELEASES.md

## 5. Evidence pointers

- Real-world proof: `packages/playwright-compiler/src/__tests__/credential-egress.real-world.test.ts` — real Chromium submits email/password canaries to the approved server, rejects its 302/303/307/308 foreign-origin redirects at the CDP response stage without contacting the foreign server, and blocks page-context fetch and post-fill foreign WebSocket attempts before any request, connection, or canary bytes reach their servers.
- Real-world proof: `packages/playwright-compiler/src/real-world.test.ts` — real Playwright/Chromium executes generated same-origin login verification against both reference apps.
- Artifacts: transient Playwright output only; no raw trace ZIP retained.
- Diagnostic proof: temporary generated-fixture instrumentation observed no denied URL in the failing CLI run; the generated approved set was configured from `http://127.0.0.1:<ephemeral-port>` (the successful diagnostic rerun used port `36443`) and contained that exact origin plus `http://localhost:<same-port>`. Removing ignored `.vitest-auth.db{,-shm,-wal}` files from the reference-app source directory removed the blocked scanner diagnostics and restored promotion. The reference auth-domain logout run independently captured the legitimate denied URL `http://localhost:33555` when the alias was temporarily removed, confirming why the bounded alias must remain.
- Gates: full root test, typecheck, lint, package typecheck, format, and license pass locally.

## 6. Sad paths proved (each mapped to a truth state, charter §4)

| Trigger                                                                         | Expected disposition | Test                                                                                          |
| ------------------------------------------------------------------------------- | -------------------- | --------------------------------------------------------------------------------------------- |
| Credential-bearing POST receives a 302/303/307/308 redirect to a foreign origin | blocked              | parameterized real-Chromium redirect tests prove zero foreign requests and zero canary bytes  |
| Runtime observation URL is outside the approved allowlist                       | blocked              | `blocks a runtime observation whose origin is outside the action-owned allowlist`             |
| Page request targets a foreign origin                                           | blocked              | generated `context.route('**/*')` policy exercised by redirect proof                          |
| Service Worker attempts to take over requests                                   | blocked              | `generated policy blocks service workers and installs context interception before navigation` |
| Page fetch targets a foreign origin while SW registration is attempted          | blocked              | `real Chromium blocks a page-context cross-origin fetch and service worker registration`      |
| Page opens a foreign WebSocket after credential canaries are filled             | blocked              | `real Chromium blocks foreign WebSocket egress after credential canaries are filled`          |
